# 로그 기반 순차 분석·실험 실행 프롬프트

이 문서는 EDA, 데이터 전처리, 피처 엔지니어링, 통계 분석, 모델 학습,
모델 평가, 오류 분석 등 여러 작업을 **계획 → 실행 → 결과 검토 → 해석 → 다음 작업 반영**
순서로 진행하기 위한 범용 프롬프트 모음이다.

## 1. 기본 사용 원칙

- 하나의 작업 단위를 하나의 `분석 ID` 또는 `실험 ID`로 관리한다.
- 새 세션에서는 원칙적으로 ID 하나만 실행한다.
- 현재 ID의 결과를 검토하고 해석하기 전에 다음 ID의 코드를 작성하지 않는다.
- 실행 코드와 결과 해석을 분리한다.
  - 노트북·스크립트: 실행 가능한 코드와 출력
  - 진행 로그: 결과 해석, 판단, 한계, 다음 작업 반영사항
- 계획에 없던 추가 분석은 결과에서 확인된 구체적 근거가 있을 때만 `A` ID로 등록한다.
- 완료하지 않은 항목을 임의로 체크하지 않는다.

권장 ID 형식:

```text
기본 작업: T1-01, T1-02, T2-01
추가 심화 작업: T1-A01, T1-A02
모델 실험: M1-01, M1-02
오류 분석: E1-01, E1-02
```

## 2. 작업 유형별 결과 검토 기준

### EDA·통계 분석

- 시각화를 중심으로 진행한다.
- 분포, 추세, 집단 간 겹침, 이상치, 비선형성, 공간·시간 패턴을 직접 확인한다.
- 결과표와 통계검정은 시각적 발견의 크기와 불확실성을 확인하는 근거로 사용한다.
- 표와 플롯을 모두 검토해야 해석 완료로 처리한다.

### 데이터 전처리·피처 엔지니어링

- 처리 전후 행 수, 키 중복, 결측률, 자료형, 값 범위와 제외 사유를 비교한다.
- 시간·공간·대상 누수 여부를 감사한다.
- 변환 전후 분포와 이상치를 가능한 경우 플롯으로 검토한다.
- 데이터가 조용히 손실되거나 중복되지 않았는지 검증한다.

### 모델 학습·평가

- 베이스라인과 비교한다.
- train/validation/test 분리와 데이터 누수를 먼저 감사한다.
- seed, 하이퍼파라미터, 학습 데이터 버전과 피처 목록을 기록한다.
- 단일 점수만 보지 않고 학습곡선, 검증곡선, 예측 분포, confusion matrix,
  ROC/PR 또는 residual plot 등 문제 유형에 맞는 시각화를 검토한다.
- 평균 성능과 함께 fold·seed별 변동, 신뢰구간 또는 분산을 확인한다.
- 테스트셋은 최종 평가 용도로만 사용하고 반복적인 의사결정에 사용하지 않는다.

### 모델 오류 분석

- 오분류·고잔차 사례를 직접 확인한다.
- 집단·시간·공간·범주별 성능 차이를 시각화한다.
- 데이터 오류, 표현 부족, 분포 이동과 모델 한계를 구분한다.
- 오류 분석에서 도출된 개선안을 다음 실험 ID에 명시적으로 연결한다.

## 3. 최초 로그 설계 프롬프트

프로젝트를 처음 시작하거나 진행 로그가 없을 때 아래 프롬프트를 사용한다.

```text
[프로젝트 루트] 작업을 로그 기반 순차 방식으로 설계해줘.

작업 정보:
- 작업 유형: [EDA / 전처리 / 피처 엔지니어링 / 통계 분석 / 모델 학습 / 평가 / 오류 분석]
- 작업 목적: [구체적인 최종 목적]
- 주요 실행 파일: [노트북 또는 스크립트 절대경로]
- 참고 문서: [README, 명세, 기존 분석 결과 경로]
- 진행 로그를 만들 위치: [절대경로]
- 결과표 저장 위치: [절대경로]
- 플롯·이미지 저장 위치: [절대경로]
- 모델·중간 산출물 저장 위치: [절대경로 또는 해당 없음]

먼저 관련 문서, 코드, 데이터 구조와 기존 결과를 읽고 전체 작업을 파악해라.
그다음 아래 조건을 만족하는 진행예정로그 Markdown 파일을 작성해라.

1. 전체 작업을 결과 해석이 가능한 최소 단위의 순차 ID로 나눠라.
2. 각 ID에 목적, 입력, 작성할 코드, 필수 결과표, 필수 시각화 또는 진단 산출물,
   검증 기준과 완료 조건을 작성하라.
3. 각 ID에 코드·실행·결과 검토·시각 검토·해석·반영 체크박스를 둬라.
4. 현재 ID의 결과 해석이 끝난 뒤 다음 ID 코드를 작성하도록 명시하라.
5. 실행 코드와 해석 기록을 분리하라. 실행 파일에는 코드를 두고 해석은 로그에 기록하라.
6. 결과에 따라 계획에 없던 심화 작업을 추가할 수 있도록 [단계]-A01 형식의
   '결과 기반 추가 심화 분석 큐'와 등록 양식을 만들어라.
7. 추가 심화 작업에는 파생된 기본 ID, 추가 근거, 확인 질문, 필수 시각화,
   보조 지표, 중단·완료 조건을 기록하게 하라.
8. EDA라면 시각화 중심으로, 모델 학습이라면 학습곡선·검증 성능·예측 및 오류
   시각화를 중심으로 검토하게 하라.
9. 데이터 누수, 결측, 중복, 표본 수, 재현성, seed와 실행 오류 검증 항목을 포함하라.
10. 아직 실행하지 않은 항목은 체크하지 말고 계획만 작성하라.

이번 요청에서는 로그 설계까지만 수행하고 첫 ID의 구현은 시작하지 마라.
```

## 4. 최초 ID 실행 프롬프트

로그가 준비된 뒤 첫 작업을 시작할 때 사용한다.

```text
[프로젝트 루트] 작업을 시작해줘.

대상:
- 작업 유형: [작업 유형]
- 실행 파일: [노트북 또는 스크립트 절대경로]
- 진행 로그: [로그 절대경로]
- 이번 작업 ID: [예: T1-01]
- 결과표 경로: [절대경로]
- 플롯·이미지 경로: [절대경로]
- 모델·산출물 경로: [절대경로 또는 해당 없음]

진행 규칙:
1. 프로젝트 지침, README, 대상 진행 로그와 관련 코드를 먼저 읽어라.
2. 이번 세션에서는 지정한 작업 ID만 진행하라.
3. 로그에 기록된 목적, 입력, 산출물과 완료 조건을 기준으로 구현하라.
4. 실제 프로젝트 실행 환경에서 코드를 실행하고 오류가 없도록 수정하라.
5. 결과표, 로그, 플롯, 모델 등 산출물을 지정 경로에 저장하라.
6. 생성한 핵심 플롯과 이미지를 직접 열어 시각적으로 검토하라.
7. 작업 유형에 맞는 검증을 수행하라.
   - EDA: 분포·추세·이상치·집단 차이·비선형 패턴
   - 전처리: 처리 전후 행 수·키·결측·범위·누수
   - 모델 학습: 베이스라인·학습곡선·일반화 차이·fold/seed 변동
   - 모델 평가: 적절한 평가 지표·예측 분포·오류 사례·집단별 성능
8. 수치 결과와 시각화 또는 진단 산출물을 함께 근거로 해석하라.
9. 해석, 한계, 실패한 가설, 다음 ID에 반영할 사항을 진행 로그에 작성하라.
10. 로그의 체크박스는 실제 완료된 항목만 갱신하라.
11. 결과에서 추가 확인이 필요하면 [단계]-A01 형식으로 심화 작업을 등록하라.
    추가 근거가 없으면 추가하지 않은 이유를 기록하라.
12. 심화 작업을 등록했다면 다음 기본 ID보다 먼저 수행하도록 순서를 기록하라.
13. 다음 ID에 반영할 사항은 기록하되 다음 기본 ID의 코드는 작성하지 마라.
14. 기존 사용자 변경사항이나 관련 없는 파일은 수정하지 마라.
```

## 5. 다음 세션 이어서 실행 프롬프트

각 작업 ID를 새 세션에서 이어갈 때 사용한다.

```text
이전 로그 기반 작업을 이어서 진행해줘.

대상:
- 프로젝트 루트: [절대경로]
- 작업 유형: [작업 유형]
- 실행 파일: [노트북 또는 스크립트 절대경로]
- 진행 로그: [로그 절대경로]
- 이번 작업 ID: [다음 기본 ID 또는 등록된 심화 ID]
- 결과표 경로: [절대경로]
- 플롯·이미지 경로: [절대경로]
- 모델·산출물 경로: [절대경로 또는 해당 없음]

프로젝트 지침, README, 진행 로그와 기존 실행 파일을 먼저 읽어라.
이전 ID의 결과 해석, 한계와 '다음 코드 반영사항'을 확인한 뒤 이번 ID에 반영하라.

이번 세션에서는 지정한 ID만 구현하고 실제 환경에서 실행·검증해라.
결과 수치와 저장한 핵심 플롯 또는 진단 이미지를 모두 직접 검토한 뒤 통합 해석을
진행 로그에 작성해라. 완료 체크박스를 실제 상태에 맞게 갱신하고, 결과 기반 추가
심화 작업이 필요한지도 판단해라.

추가 심화 작업이 이미 등록되어 있고 아직 완료되지 않았다면 다음 기본 ID보다
해당 심화 ID를 우선 수행하라. 이번 ID 이후의 기본 작업 코드는 작성하지 마라.
```

## 6. 추가 심화 작업 실행 프롬프트

결과에서 발견된 문제나 패턴을 추가로 확인할 때 사용한다.

```text
진행 로그에 등록된 추가 심화 작업을 수행해줘.

대상:
- 프로젝트 루트: [절대경로]
- 실행 파일: [절대경로]
- 진행 로그: [절대경로]
- 심화 작업 ID: [예: T1-A01]
- 파생 기본 ID: [예: T1-03]

로그에 기록된 추가 근거, 확인 질문, 필수 시각화, 보조 지표와 중단 조건을 먼저 확인해라.
이번 세션에서는 해당 심화 ID만 수행하라.

단순히 유의한 결과를 더 찾기 위한 반복 검정은 하지 마라. 원래 발견된 패턴이
재현되는지, 데이터 품질·층화·비선형성·민감도 중 무엇으로 설명되는지 검증하라.
코드를 실제 환경에서 실행하고 핵심 플롯이나 진단 이미지를 직접 검토하라.

결과와 한계, 원래 기본 ID 해석의 유지·수정 여부, 다음 기본 ID에 반영할 사항을
진행 로그에 작성하고 체크박스를 갱신하라. 다음 기본 ID 코드는 작성하지 마라.
```

## 7. 모델 학습 전용 추가 문구

모델 학습 작업에서는 최초·후속 실행 프롬프트에 아래 내용을 추가한다.

```text
모델 학습 추가 규칙:
1. 학습 전에 target 정의, 관측 단위, train/validation/test 분리 기준과 누수를 감사하라.
2. 전처리와 피처 선택은 학습 데이터에만 fit하고 validation/test에는 transform만 적용하라.
3. 베이스라인 모델과 비교하고 동일한 데이터 분할·평가지표를 사용하라.
4. random seed, 데이터 버전, 피처 목록, 하이퍼파라미터와 패키지 버전을 기록하라.
5. 평균 성능뿐 아니라 fold 또는 seed별 변동을 보고하라.
6. 분류는 confusion matrix, ROC/PR, calibration과 예측확률 분포를 검토하라.
7. 회귀는 residual, 예측값-실제값, 오차분포와 구간별 오류를 검토하라.
8. 학습·검증 곡선을 통해 과적합과 과소적합을 판단하라.
9. 테스트셋 결과를 이용해 하이퍼파라미터나 피처를 반복 조정하지 마라.
10. 모델 저장 전 재현 실행과 입력 스키마 검증을 수행하라.
```

## 8. 작업 완료 감사 프롬프트

한 단계 또는 프로젝트 전체를 종료하기 전에 사용한다.

```text
[프로젝트 또는 단계명]의 완료 상태를 감사해줘.

대상:
- 프로젝트 루트: [절대경로]
- 실행 파일: [절대경로 또는 목록]
- 진행 로그: [절대경로 또는 목록]
- 결과 산출물 경로: [절대경로]

다음을 확인해라.
1. 모든 기본 ID의 코드·실행·결과 검토·시각 검토·해석·반영이 완료됐는가.
2. 등록된 모든 추가 심화 ID가 완료됐는가.
3. 실행 파일에 오류 출력이나 미실행 핵심 셀이 남아 있지 않은가.
4. 결과표, 플롯, 모델과 중간 산출물이 로그 기록과 일치하는가.
5. 데이터 누수, 중복, 결측, 표본 제외와 재현성 검증이 완료됐는가.
6. 결과 해석이 수치와 시각화 또는 진단 산출물에 의해 뒷받침되는가.
7. 과장된 인과 해석이나 검증되지 않은 주장이 없는가.
8. 모델 작업이라면 베이스라인 비교, 분할 원칙, seed 변동과 오류 분석이 완료됐는가.
9. 미완료 항목을 임의로 완료 처리하지 않았는가.

문제가 있으면 로그의 해당 체크박스를 완료로 표시하지 말고 필요한 후속 ID를 등록해라.
모두 충족할 때만 단계 완료로 기록하라.
```

## 9. 복사 전 교체할 항목

프롬프트를 새 프로젝트에서 사용할 때 다음 대괄호 항목을 반드시 바꾼다.

- `[프로젝트 루트]`
- `[작업 유형]`
- `[작업 목적]`
- `[실행 파일]`
- `[진행 로그]`
- `[이번 작업 ID]`
- `[결과표 경로]`
- `[플롯·이미지 경로]`
- `[모델·산출물 경로]`

경로는 가능한 한 절대경로를 사용한다. 실행 파일이 여러 개라면 이번 ID가 수정할
주 실행 파일과 참고만 할 파일을 구분해 적는다.
