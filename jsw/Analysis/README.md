# Analysis 진행 로그

## 2026-06-08

### `2026-06-08_FA.ipynb`

#### 목적
- 매칭 비발생 대조군 없이, 산불 발생 사례만 사용해 FA(요인분석) 입력 데이터를 준비한다.
- `jsw/강원_EDA/README.md`에 정리된 EDA 결합 방식을 그대로 따라 산불 1건을 1행으로 구성한다.

#### 데이터 준비 내용
- `data/강원도_데이터/강원도_산불발생.csv`를 불러와 `발생일시`, `날짜`, `월_key`, `시간_key`, `산불위험계절`, `발생시간대구분`을 생성했다.
- 산불 좌표(`경도`, `위도`)를 `data/강원도_날씨데이터/강원도날씨_격자.geojson`의 기상 격자에 `within` 공간조인하여 `기상셀ID`, `기후권역`을 붙였다.
- 기상셀에 매칭되지 않은 4건(`F_006690`, `F_009678`, `F_010665`, `F_010893`)은 기존 EDA 원칙과 동일하게 제외한다.
- `data/강원도_날씨데이터/강원도날씨_기후지형유형_셀분류.csv`를 결합해 `영동 해안형`, `영서 내륙형`, `고지·산간형`을 붙였다.
- `발생지역번지`가 `산`으로 시작하면 `임야번지(산)`, 아니면 `일반번지`로 분류해 `addr_type`을 만들었다.
- `기후지형유형`과 `addr_type`을 조합해 `기후지형_번지유형` 메타데이터를 추가했다.
- `data/강원도_데이터/산불_공간데이터/강원도_산불_지형특성계산.csv`를 `fire_id` 기준으로 결합해 고도, 경사도, 사면방향, TPI, TWI를 붙였다.
- 토지피복 GPKG와 공간조인하여 `L1_NAME`, `L2_NAME`을 붙였다.
- 도로, 임도, 등산로, 산림지역, 시가화건조지역, 농업지역까지의 최단거리를 계산하고 `log1p` 변환 거리 변수를 만들었다.
- 시간단위 날씨는 발생시각 현재값과 직전 24/48/72시간 rolling 값을 만들었다.
- 일단위/시간단위 집계로 D-1~D-3 최소습도와 강수량합을 붙였다.

#### FA 입력 변수
- FA 입력은 `fa_X_raw`, 결측 없는 입력 후보는 `fa_X_complete`로 준비했다.
- 현재 기본 FA 후보는 날씨 변수, 지형 변수, 접근성 거리 변수로 구성한다.
- `기후지형유형`, `기후권역`, `addr_type`, `기후지형_번지유형`, `L1_NAME`, `L2_NAME`, `원인프록시_500m`, `생활권프록시`는 FA 입력 변수가 아니라 해석 및 층화 비교용 메타데이터로 둔다.

#### 판단 메모
- `기후지형유형`, `영동/영서`, `임야번지(산)/일반번지`는 범주형이므로 기본 FA 입력에 직접 넣지 않는다.
- FA는 연속형 변수의 상관구조를 보는 분석이므로, 범주형 더미를 넣으면 요인이 날씨·지형 구조보다 집단 구분을 설명하는 방향으로 왜곡될 수 있다.
- 따라서 먼저 연속형 변수로 FA를 수행한 뒤, 요인점수를 `기후지형유형`, `addr_type`, `기후지형_번지유형`별로 비교하는 방식이 적절하다.

### `2026-06-08_FA-매칭비발생.ipynb`

#### 목적
- 같은 기상셀·월·시간대의 매칭 비발생 대조군을 만들고, 산불 시점이 평상시보다 얼마나 위험 방향으로 이탈했는지를 FA 입력으로 준비한다.
- 일반 FA와는 별도 노트북으로 분리해, 산불 발생 사례 내부 구조와 매칭 비발생 대비 이탈 구조를 혼합하지 않도록 했다.

#### 데이터 준비 내용
- 기본 산불·기상셀·기후지형유형·지형·토지피복·접근성 결합 방식은 `2026-06-08_FA.ipynb`와 동일하다.
- `hourly_features`에서 산불 발생 시각을 제외한 뒤, 각 산불과 같은 `기상셀ID`, 같은 `월_key`, 같은 `시간_key`를 갖는 비발생 시점을 최대 5개 추출한다.
- 매칭 비발생 평균과 산불 시점 값을 비교해 날씨 변수별 `위험방향_delta`를 만든다.

#### FA 입력 변수
- 매칭비발생 FA의 날씨 변수는 원값이 아니라 `위험방향_delta`를 사용한다.
- 습도와 강수량은 낮을수록 위험하므로 `매칭 비발생 평균 - 산불값`으로 계산한다.
- 풍속과 최대순간풍속은 높을수록 위험하므로 `산불값 - 매칭 비발생 평균`으로 계산한다.
- 지형 변수와 접근성 거리 변수는 일반 FA 노트북과 동일하게 사용한다.
- `fa_X_raw`, `fa_X_complete`, `fa_source`, `matched_controls`를 생성 객체로 둔다.

### 공통 반영 사항

#### 한글 폰트 설정
- `jsw/강원_EDA/How_to_EDA.md`의 맑은고딕 설정 코드를 두 FA 노트북의 환경 설정 셀에 반영했다.
- `sns.set_theme()` 호출 시 폰트가 다시 덮이지 않도록 seaborn 스타일과 matplotlib 폰트 설정을 같은 셀에서 처리한다.
- 정상 실행 시 `현재 matplotlib font.family: ['Malgun Gothic']`, `현재 matplotlib font.sans-serif: ['Malgun Gothic']`이 출력된다.

#### 추가 변수 후보
- EDA 기록상 추가 후보로 검토할 만한 변수는 `당일_일교차_C`, `시점_기온_C`, `당일_평균기온_C`, `최저기온_C`, `최고기온_C`, `풍향_sin`, `풍향_cos`, `최대순간풍향_sin`, `최대순간풍향_cos`, `서풍계열 여부`이다.
- 다만 서풍계열 여부는 0/1 변수라 기본 FA보다는 메타/보조변수 쪽이 낫고, 기온 변수들은 서로 상관이 높을 수 있어서 다음 `# FA` 단계에서 상관행렬/KMO를 보고 넣을지 정하는 게 안전하다.

#### 다음 단계
- `# FA` 챕터에서 상관행렬, Bartlett 검정, KMO, 공통성, 요인 수 후보를 확인한다.
- 기본 FA는 산불 발생 사례만 사용한 `2026-06-08_FA.ipynb`에서 먼저 진행한다.
- 필요 시 `2026-06-08_FA-매칭비발생.ipynb`에서 매칭 비발생 대비 위험 이탈 요인을 별도 분석한다.

### `2026-06-08_FA.ipynb` FA 진행 로그

#### 요인분석적합성 점검
- `fa_X_complete` 기준으로 FA 적합성 점검을 수행했다.
- 적합성 점검 표본 수는 2,670행, 변수 수는 31개이며, 상수/단일값 제거 변수는 없었다.
- KMO 전체 값은 `0.7174`로 `보통 이상` 수준이다.
- Bartlett 구형성 검정 결과는 `chi-square = 72,835.8389`, `p-value = 0.0000`으로 유의했다.
- 따라서 현재 후보 변수군은 요인분석을 진행할 수 있는 상관구조를 가진다.

#### 상관행렬 해석
- 상관행렬에서 풍속, 습도, 강수량, 지형·접근성 블록이 뚜렷하게 나타났다.
- `직전24h/48h/72h 평균풍속`과 `당일_최대순간풍속_m_s`, `시점_풍속_m_s`는 강한 양의 상관을 보여 `지속 강풍` 요인으로 묶일 가능성이 높다.
- `직전24h/48h/72h 최소습도`, `D-1~D-3 최소습도`, `시점_습도_pct`는 `건조/습윤 상태` 요인으로 묶일 가능성이 높다.
- `직전24h/48h/72h 강수량합`, `D-1~D-3 강수량합`은 별도 강수량 블록을 형성했다.
- `log1p_도로_최단거리_m`과 `log1p_시가화_최단거리_m`의 상관이 높아 생활권 접근성 축으로 묶일 가능성이 크다.

#### KMO 하위 변수 메모
- 변수별 KMO가 낮은 주요 후보는 다음과 같다.
  - `D-3_강수량합_mm`: 0.2646
  - `D-2_강수량합_mm`: 0.2766
  - `사면방향_cos`: 0.3778
  - `시점_강수량_mm`: 0.4346
  - `log1p_농업_최단거리_m`: 0.5824
  - `직전72h_강수량합`: 0.5989
- 이 변수들은 바로 삭제하지 않고, 1차 FA 후 공통성/적재량/교차적재를 보고 제거 또는 보조변수 전환을 판단한다.

#### Parallel Analysis 요인 수 결정
- 31개 후보 변수, 2,670개 완전케이스를 기준으로 Parallel Analysis를 수행했다.
- 무작위 반복 수는 500회, 보수 기준은 무작위 고유값 95퍼센타일로 설정했다.
- 결과는 다음과 같다.
  - Parallel Analysis 95퍼센타일 기준: 7개 요인
  - Parallel Analysis 평균 기준: 8개 요인
  - Kaiser 기준(`eigenvalue > 1`): 9개 요인
- 7번째 요인은 실제 고유값 `1.1850`이 무작위 95퍼센타일 고유값 `1.1121`보다 높아 채택했다.
- 8번째 요인은 실제 고유값 `1.0976`이 무작위 95퍼센타일 고유값 `1.0981`보다 아주 근소하게 낮아 보수 기준에서는 제외했다.
- 따라서 본 분석은 우선 `7요인 해`를 기준으로 진행한다.
- 단, 8번째 요인이 경계값에 매우 가까우므로 이후 적재량과 해석 가능성을 볼 때 `7요인 해`와 `8요인 해`를 보조 비교할 수 있다.

#### 회전 방식 비교: Varimax vs Promax
- 7요인 기준으로 `varimax`와 `promax`를 비교했다.
- 비교 결과는 다음과 같다.

| 회전 방식 | 평균 공통성 | 공통성 0.20 미만 변수 수 | 약한 주적재 변수 수 | 교차적재 변수 수 | 최대 요인상관 절대값 | 누적 설명분산 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| varimax | 0.5767 | 5 | 5 | 5 | 0.0000 | 0.5767 |
| promax | 0.5985 | 5 | 6 | 4 | 0.3089 | 0.5985 |

- `promax`의 최대 요인상관 절대값은 `0.3089`로 0.30 기준을 넘었다.
- `promax`는 `varimax`보다 평균 공통성이 높고, 교차적재 후보도 5개에서 4개로 줄었다.
- 날씨·지형·접근성 요인이 서로 완전히 독립이라고 보기 어렵기 때문에, 최종 회전 방식은 `promax`로 선택했다.

#### 회전 결과 주의점
- `promax` 결과에서 일부 변수의 공통성이 1을 넘고 고유성이 음수가 되는 Heywood 징후가 나타났다.
- 이는 `직전24h/48h/72h` 습도·풍속·강수량처럼 서로 매우 높은 상관을 갖는 중복 시간창 변수가 함께 들어간 영향일 가능성이 크다.
- 다음 단계에서는 `7요인 + promax` 해를 기준으로 적재량을 해석하되, 공통성 1 초과, 공통성 0.20 미만, 약한 주적재, 교차적재 변수를 점검한다.
- 이후 중복 시간창 변수는 대표 변수 중심으로 축소하고 재FA를 수행한다.

#### 요인 Loading 해석: 주적재 기준
- `7요인 + promax` 결과의 loading을 먼저 각 변수의 절대 적재량이 가장 큰 주요인 기준으로 해석했다.
- 요인명 초안은 다음과 같이 정리했다.

| 요인 | 주적재 기준 해석명 | 주요 변수 |
| --- | --- | --- |
| F1 | 선행 습도/건조 상태 | 직전24h/48h/72h 최소습도, D-1~D-2 최소습도, 시점 습도 |
| F2 | 지속·당일 강풍 | 직전24h/48h/72h 평균풍속, 당일 최대순간풍속, 시점 풍속 |
| F3 | 최근 24~48h 및 D-1 강수 | 직전24h/48h 강수량합, D-1 강수량합 |
| F4 | 생활권-산림사면 입지 | 시가화거리, 도로거리, 산림거리, TWI, 경사도 |
| F5 | 중기 누적강수 | 직전72h 강수량합, D-2~D-3 강수량합 |
| F6 | 고도-임도 인접 지형 | 임도거리, 고도, 사면방향_sin, TPI |
| F7 | D-3 선행습도 잔여 | D-3 최소습도 |

- F1은 습도값이 높을수록 점수가 높은 방향이므로, 산불 위험 해석에서는 점수가 낮을수록 선행 건조가 강한 상태로 해석한다.
- F2는 지속 평균풍속과 당일 최대순간풍속이 함께 묶여 산불 발생 전후의 지속 강풍 조건을 나타낸다.
- F3와 F5는 모두 강수량 요인이지만, F3는 D-1 및 직전 24~48시간의 단기 강수, F5는 D-2~D-3 및 직전 72시간의 중기 누적강수로 나뉜다.
- F4는 도로·시가화지역과의 거리, 산림거리, TWI, 경사도가 함께 묶인 공간 입지 요인이다.
- F6은 고도와 임도 접근성이 결합된 산지 접근성 요인이지만, 강한 변수 수가 적어 F1~F5보다 안정성은 낮다.
- F7은 D-3 최소습도 단일 변수에 가까운 잔여 요인으로, 최종 요인으로 유지할지는 재FA에서 확인해야 한다.

#### 교차적재 및 약한 적재 변수
- 주적재 기준 해석만으로는 여러 요인에 걸친 변수를 놓칠 수 있으므로 교차적재를 별도로 확인했다.
- 주요 교차적재 변수는 다음과 같다.

| 변수 | 적재 구조 | 해석 주의점 |
| --- | --- | --- |
| `시점_습도_pct` | F1 = 0.5139, F7 = -0.3280 | 발생시각 습도는 선행 습도 요인에 속하지만 D-3 잔여축과도 연결된다. |
| `경사도(도)` | F4 = 0.5166, F6 = 0.4731 | 생활권-산림사면 입지와 고도-임도 지형을 동시에 연결한다. |
| `직전72h_강수량합` | F3 = 0.6378, F5 = 0.7407 | 단기 강수와 중기 누적강수를 동시에 반영한다. |
| `D-3_최소습도_pct` | F1 = 0.5031, F7 = 0.7248 | F7의 핵심 변수지만 F1의 선행 습도 축에도 강하게 걸친다. |

- 약한 주적재 또는 낮은 공통성 변수는 다음과 같다.
  - `log1p_등산로_최단거리_m`: 주적재량 -0.1232, 공통성 0.0373
  - `시점_강수량_mm`: 주적재량 0.1286, 공통성 0.0271
  - `사면방향_cos`: 주적재량 0.0646, 공통성 0.0093
  - `사면방향_sin`: 주적재량 0.3979, 공통성 0.1789
  - `TPI(지형위치지수)`: 주적재량 -0.3743, 공통성 0.2312
  - `log1p_농업_최단거리_m`: 주적재량 -0.1822, 공통성 0.0784
- 이 변수들은 최종 FA에서 제거하거나 메타/보조변수로 전환할 후보이다.

#### 중복 적재 허용 해석
- 이후 `|loading| >= 0.30`인 모든 요인에 변수를 중복 포함하는 방식으로 다시 해석했다.
- 중복 포함 기준에서는 한 변수를 하나의 요인에만 강제로 배정하지 않고, 여러 요인의 의미를 동시에 구성하는 변수로 해석한다.

| 요인 | 중복 포함 기준 해석명 | 안정성 | 주요 주의점 |
| --- | --- | --- | --- |
| F1 | 선행 습도/건조 상태 | 높음 | `D-3_최소습도_pct`가 F7과 중복된다. |
| F2 | 지속·당일 강풍 | 높음 | 풍속 시간창 변수 간 중복이 강하다. |
| F3 | 단기 강수 | 보통 이상 | `직전72h_강수량합`이 F5와 중복된다. |
| F4 | 생활권-산림사면 입지 | 보통 이상 | `경사도(도)`가 F6과 중복된다. |
| F5 | 중기 누적강수 | 보통 | `직전72h_강수량합`이 F3와 중복된다. |
| F6 | 고도-임도 인접 지형 | 낮음~보통 | 보조 적재 변수가 많고 핵심 변수가 적다. |
| F7 | D-3 선행습도 잔여 | 낮음 | 단독/잔여 요인 가능성이 크다. |

- 중복 포함 기준에서 F1은 발생시각부터 직전 72시간, D-1~D-3까지 이어지는 넓은 선행 습도/건조 상태 축으로 해석된다.
- F2는 중복 포함 기준에서도 거의 순수한 강풍 요인으로 남아 가장 안정적이다.
- F3와 F5는 모두 강수량 요인으로, `직전72h_강수량합`이 두 요인을 연결한다. 따라서 최종 변수 축소 시 단기/중기 강수를 분리할지, 하나의 강수 요인으로 통합할지 결정해야 한다.
- F4와 F6은 `경사도(도)`를 통해 연결된다. 경사도는 제거하기보다는 교차변수로 표시하면서 해석하는 것이 적절하다.
- F7은 F1의 습도 축에서 분리된 D-3 최소습도 잔여 성격이 강하므로, 변수 축소 후에도 독립 요인으로 남는지 확인해야 한다.

#### Loading 해석 기반 다음 단계
- 현재 7요인 promax 해는 큰 방향에서는 해석 가능하지만, 최종 구조 확정 전 변수 축소가 필요하다.
- 우선 제거 또는 보조변수 전환 후보는 `시점_강수량_mm`, `사면방향_cos`, `log1p_등산로_최단거리_m`, `log1p_농업_최단거리_m`이다.
- 습도 변수는 `직전24h/48h/72h 최소습도`, `D-1~D-3 최소습도` 중 대표 시간창을 줄여 F1/F7 분리를 완화한다.
- 풍속 변수는 `직전24h/48h/72h 평균풍속` 중 대표 시간창과 `당일_최대순간풍속_m_s` 중심으로 정리한다.
- 강수 변수는 F3/F5 중복을 줄이기 위해 단기 강수와 중기 강수를 의도적으로 분리할지, 대표 강수 변수로 통합할지 결정한다.
- 변수 축소 후 `promax` 회전으로 재FA를 수행하고, F7이 여전히 단일 잔여 요인으로 남는지 확인한다.

#### 변수 축소 원칙 보완
- 요인 Loading 해석은 최종 요인명과 기본 보고에서는 `주적재 기준`을 사용한다.
- 다만 `promax` 회전을 사용했고 일부 변수에 교차적재가 있으므로, `|loading| >= 0.30`의 중복 적재 변수는 별도 교차변수로 표시한다.
- 중복 적재 기준은 요인명 확정보다는 변수 축소, 보조변수 전환, 재FA 설계 판단에 사용한다.
- `사면방향_sin`과 `사면방향_cos`는 사면방향의 순환형 정보를 함께 표현하는 한 세트이므로 한쪽만 제거하지 않는다.
- 1차 FA에서 `사면방향_cos`는 공통성 `0.0093`, 주적재량 `0.0646`이고, `사면방향_sin`은 공통성 `0.1789`, 주적재량 `0.3979`로 둘 다 FA 핵심 요인으로 강하게 설명되지 않았다.
- 따라서 대표 시간창 재FA에서는 `사면방향_sin`, `사면방향_cos`를 둘 다 FA 핵심 변수에서 제외하고 보조 지형 변수로 전환했다.
- 습도·풍속·강수 변수는 중요한 기상 메커니즘을 버리는 것이 아니라, 거의 같은 정보를 담은 시간창 중복을 줄이는 방향으로 대표 변수를 남겼다.

#### 대표 시간창 기준 축소 변수셋
- 축소 FA 후보 변수는 기존 31개에서 18개로 줄였다.
- 완전케이스 표본 수는 기존과 동일하게 `2,670행`이다.
- 누락 후보 변수는 없었다.
- 축소 변수셋은 다음과 같다.

| 구분 | 유지 변수 |
| --- | --- |
| 습도 | `시점_습도_pct`, `직전24h_최소습도`, `D-1_최소습도_pct`, `D-3_최소습도_pct` |
| 풍속 | `시점_풍속_m_s`, `직전24h_평균풍속`, `당일_최대순간풍속_m_s` |
| 강수 | `직전24h_강수량합`, `직전72h_강수량합`, `D-1_강수량합_mm` |
| 지형 | `고도(m)`, `경사도(도)`, `TPI(지형위치지수)`, `TWI(지형다습지수)` |
| 접근성 | `log1p_도로_최단거리_m`, `log1p_임도_최단거리_m`, `log1p_산림_최단거리_m`, `log1p_시가화_최단거리_m` |

- 제외 또는 보조변수 전환 변수는 다음과 같다.

| 변수 | 사유 |
| --- | --- |
| `직전48h_최소습도` | 습도 시간창 중복 축소 |
| `직전72h_최소습도` | 습도 시간창 중복 축소 |
| `D-2_최소습도_pct` | 습도 시간창 중복 축소 |
| `직전48h_평균풍속` | 풍속 시간창 중복 축소 |
| `직전72h_평균풍속` | 풍속 시간창 중복 축소 |
| `직전48h_강수량합` | 강수 시간창 중복 축소 |
| `D-2_강수량합_mm` | 강수 시간창 중복 축소 |
| `D-3_강수량합_mm` | 강수 시간창 중복 축소 |
| `시점_강수량_mm` | 희소한 정각 강수 변수로 공통요인성 낮음 |
| `사면방향_sin` | 사면방향 sin/cos 세트 보조변수 전환 |
| `사면방향_cos` | 사면방향 sin/cos 세트 보조변수 전환 |
| `log1p_등산로_최단거리_m` | 1차 FA에서 공통요인성 낮음 |
| `log1p_농업_최단거리_m` | 1차 FA에서 공통요인성 낮음 |

#### 축소 변수셋 요인분석적합성 점검
- 축소 변수셋 기준 적합성 점검 표본 수는 `2,670행`, 변수 수는 `18개`이다.
- 상수/단일값 제거 변수는 없었다.
- KMO 전체 값은 `0.6815`로 `허용` 수준이다.
- Bartlett 구형성 검정 결과는 `chi-square = 26,923.7264`, `p-value = 0.0000`으로 유의했다.
- 따라서 축소 변수셋도 요인분석을 진행할 수 있는 상관구조를 가진다.
- 높은 상관쌍은 여전히 일부 남아 있다.
  - `log1p_도로_최단거리_m` - `log1p_시가화_최단거리_m`: `0.8842`
  - `직전24h_강수량합` - `D-1_강수량합_mm`: `0.8822`
  - `직전24h_최소습도` - `D-1_최소습도_pct`: `0.8509`
  - `직전24h_평균풍속` - `당일_최대순간풍속_m_s`: `0.7231`
  - `시점_풍속_m_s` - `당일_최대순간풍속_m_s`: `0.7115`

#### 축소 변수셋 Parallel Analysis 요인 수 결정
- 18개 후보 변수, 2,670개 완전케이스를 기준으로 Parallel Analysis를 다시 수행했다.
- 무작위 반복 수는 500회, 보수 기준은 무작위 고유값 95퍼센타일로 설정했다.
- 결과는 다음과 같다.
  - Parallel Analysis 95퍼센타일 기준: 5개 요인
  - Parallel Analysis 평균 기준: 5개 요인
  - Kaiser 기준(`eigenvalue > 1`): 5개 요인
- 세 기준이 모두 `5요인`을 가리키므로, 1차 FA보다 요인 수 판단이 명확하다.
- 주요 고유값 비교는 다음과 같다.

| 요인번호 | 실제 고유값 | 무작위 평균 고유값 | 무작위 95퍼센타일 고유값 | 95퍼센타일 기준 채택 |
| ---: | ---: | ---: | ---: | --- |
| 1 | 3.3007 | 1.1449 | 1.1689 | 채택 |
| 2 | 3.2200 | 1.1179 | 1.1390 | 채택 |
| 3 | 2.7732 | 1.0974 | 1.1140 | 채택 |
| 4 | 1.6404 | 1.0797 | 1.0945 | 채택 |
| 5 | 1.4226 | 1.0627 | 1.0767 | 채택 |
| 6 | 0.9927 | 1.0479 | 1.0603 | 제외 |

#### 축소 변수셋 재FA: 5요인 Promax
- 축소 변수셋에서는 5요인 기준으로 `varimax`와 `promax`를 비교했다.
- 해석 연속성과 요인 간 상관 허용 필요성을 고려해 `promax`를 주 분석으로 사용했다.

| 회전 방식 | 평균 공통성 | 공통성 0.20 미만 변수 수 | 공통성 1 초과 변수 수 | 음수 고유성 변수 수 | 약한 주적재 변수 수 | 교차적재 변수 수 | 최대 요인상관 절대값 | 누적 설명분산 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| varimax | 0.5910 | 1 | 0 | 0 | 1 | 2 | 0.0000 | 0.5910 |
| promax | 0.6058 | 1 | 2 | 2 | 1 | 1 | 0.4102 | 0.6058 |

- `promax`는 `varimax`보다 평균 공통성과 누적 설명분산이 높고, 교차적재 후보가 2개에서 1개로 줄었다.
- 다만 `D-1_최소습도_pct`, `직전24h_최소습도`에서 공통성 1 초과와 음수 고유성이 남아 습도 축에는 약한 Heywood 징후가 있다.
- Promax 요인상관에서는 `RF1`과 `RF4`의 상관이 `-0.410`으로 가장 컸다.
- `RF2`와 `RF5`의 상관은 `-0.241`로, 습도/건조 상태와 지형 접근성 축도 완전히 독립적이지 않다.

#### 축소 변수셋 요인 Loading 해석
- 최종 요인명과 기본 보고는 주적재 기준으로 정리한다.
- `|loading| >= 0.30`인 중복 적재 변수는 교차변수로 별도 표시한다.
- 축소 변수셋의 5요인 구조는 다음과 같다.

| 요인 | 해석명 | 주요 적재 변수 |
| --- | --- | --- |
| RF1 | 생활권 이격-산림사면 입지 | `log1p_시가화_최단거리_m` 0.9221, `log1p_도로_최단거리_m` 0.9130, `log1p_산림_최단거리_m` -0.5380, `TWI(지형다습지수)` -0.5139, `경사도(도)` 0.4216 |
| RF2 | 선행 습도/건조 상태 | `D-1_최소습도_pct` 1.0002, `직전24h_최소습도` 0.9985, `D-3_최소습도_pct` 0.4759, `시점_습도_pct` 0.4127 |
| RF3 | 발생시점·직전24h 강풍 | `직전24h_평균풍속` 0.8734, `시점_풍속_m_s` 0.8720, `당일_최대순간풍속_m_s` 0.8522 |
| RF4 | 단기~72h 누적강수 | `D-1_강수량합_mm` 0.9574, `직전24h_강수량합` 0.9215, `직전72h_강수량합` 0.7738 |
| RF5 | 임도 이격-저고도 지형 | `log1p_임도_최단거리_m` 0.6797, `고도(m)` -0.5610, `경사도(도)` -0.4169 |

#### RF1. 생활권 이격-산림사면 입지
- RF1은 `log1p_시가화_최단거리_m`, `log1p_도로_최단거리_m`이 강하게 양의 적재를 보이고, `log1p_산림_최단거리_m`, `TWI(지형다습지수)`가 음의 적재를 보인다.
- 점수가 높을수록 도로·시가화지역과 멀고, 산림에는 가까우며, TWI가 낮고 경사가 큰 산림 사면 입지에 가깝다.
- 반대로 점수가 낮을수록 도로·시가화지역에 가까운 생활권 접점 성격이 강하다.

#### RF2. 선행 습도/건조 상태
- RF2는 `D-1_최소습도_pct`, `직전24h_최소습도`, `D-3_최소습도_pct`, `시점_습도_pct`가 하나의 습도 축으로 묶인다.
- 적재량 부호상 점수가 높을수록 습윤 상태이다.
- 산불 위험 해석에서는 점수가 낮을수록 선행 건조가 강한 상태로 해석한다.
- 1차 FA에서 F1과 F7로 갈라지던 습도 구조가 RF2 하나로 정리되었다.
- 다만 `D-1_최소습도_pct`와 `직전24h_최소습도`의 상관이 높아 두 변수에서 약한 Heywood 징후가 남았다.

#### RF3. 발생시점·직전24h 강풍
- RF3은 `직전24h_평균풍속`, `시점_풍속_m_s`, `당일_최대순간풍속_m_s`가 모두 0.85 이상으로 강하게 적재된다.
- 발생시점의 바람, 직전 24시간 지속풍, 당일 돌풍이 하나의 안정적인 강풍 축으로 묶였다.
- 1차 FA의 지속·당일 강풍 요인이 축소 변수셋에서도 가장 명확하게 유지되었다.

#### RF4. 단기~72h 누적강수
- RF4는 `D-1_강수량합_mm`, `직전24h_강수량합`, `직전72h_강수량합`이 모두 강하게 적재된다.
- 1차 FA에서 F3 단기 강수와 F5 중기 누적강수로 나뉘던 강수 구조가 하나의 강수 축으로 통합되었다.
- 점수가 높을수록 최근 강수량이 많고, 점수가 낮을수록 최근 강수 부족 상태로 해석한다.
- 산불 위험 해석에서는 RF4 점수가 낮은 방향이 더 건조한 강수 조건에 해당한다.

#### RF5. 임도 이격-저고도 지형
- RF5는 `log1p_임도_최단거리_m`이 양의 적재, `고도(m)`와 `경사도(도)`가 음의 적재를 보인다.
- 점수가 높을수록 임도와 멀고, 고도와 경사가 낮은 방향이다.
- 반대로 점수가 낮을수록 고도가 높고 임도와 가까운 산지 접근성 성격이 강하다.
- RF5는 핵심 변수가 2~3개 수준이므로 RF1~RF4보다 해석 안정성은 낮다.

#### 교차적재 및 약한 주적재 변수
- 축소 변수셋에서 교차적재 후보는 `경사도(도)` 1개이다.
- `경사도(도)`는 `RF1 = 0.4216`, `RF5 = -0.4169`로 두 요인에 모두 적재된다.
- 따라서 경사도는 생활권 이격-산림사면 입지와 임도 이격-저고도 지형을 연결하는 변수로 해석한다.
- `TPI(지형위치지수)`는 주적재량 `0.2638`, 공통성 `0.1582`로 약한 주적재 변수이다.
- 현재 축소 FA에서는 `TPI(지형위치지수)`를 핵심 요인 변수라기보다 보조 지형 변수로 보는 것이 적절하다.

#### 1차 FA 대비 변화
- 기존 F1/F7로 갈라지던 습도 구조가 RF2 하나로 정리되었다.
- 기존 F3/F5로 나뉘던 강수 구조가 RF4 하나로 통합되었다.
- 풍속 요인은 RF3으로 매우 안정적으로 유지되었다.
- 사면방향 `sin/cos`는 둘 다 FA 핵심 변수에서 제외되어 방향 정보의 비대칭 제거 문제를 피했다.
- `log1p_등산로_최단거리_m`, `log1p_농업_최단거리_m`도 제외되어 공통요인성이 낮은 접근성 변수가 요인 해석을 흐리는 문제가 줄었다.
- 7요인 구조에서 5요인 구조로 단순화되면서 보고용 요인 구조가 더 명확해졌다.

#### 축소 변수셋 재FA 결론
- 현재 재FA 구조는 1차 탐색 FA보다 최종 보고용 구조에 더 적합하다.
- 최종 해석은 주적재 기준으로 `RF1`~`RF5` 요인명을 사용하고, `경사도(도)`만 교차적재 변수로 주석 처리하는 방식이 적절하다.
- `TPI(지형위치지수)`는 약한 주적재 변수이므로 최종 단순화 단계에서 제거 또는 보조 지형 변수 전환을 검토한다.
- Heywood 징후를 완전히 줄여야 한다면 습도 대표 변수에서 `D-1_최소습도_pct`와 `직전24h_최소습도` 중 하나만 남기는 추가 민감도 분석을 수행한다.

#### 노트북 출력 표시 보완
- `2026-06-08_FA.ipynb`의 축소 재FA 결과 요약 셀은 긴 문자열이 Pandas 기본 출력에서 말줄임표로 잘리지 않도록 보완했다.
- 요인별 주요 적재 변수, 변수별 주적재량, `|loading| >= 0.30` 적재표, 요인상관 행렬, 교차적재/약한 변수표를 Markdown 표와 `to_string()` 출력으로 함께 표시하도록 수정했다.

#### 요인점수 생성
- 축소 변수셋의 5요인 `promax` 해를 기준으로 factor score를 생성했다.
- 원 부호 점수는 `factor_scores`, 메타데이터 결합본은 `factor_scores_source`로 생성했다.
- 메타데이터 해석과 군집분석에 바로 쓰기 쉽도록 해석 방향을 맞춘 `factor_scores_oriented`, `factor_scores_oriented_source`, `factor_scores_for_clustering`도 함께 생성했다.
- 생성된 CSV는 다음 위치에 저장했다.
  - `jsw/Analysis/outputs/2026-06-08_FA_factor_scores_raw.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_factor_scores_oriented.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_factor_score_direction.csv`
- 생성 결과 검증은 다음과 같다.
  - 원 부호 요인점수: `2,670행 x 21열`
  - 해석 방향 요인점수: `2,670행 x 21열`
  - 방향 해석표: `5행 x 7열`
  - 요인점수 결측: `0`
  - `fire_id` 고유값: `2,670`
- 원 부호 요인점수 컬럼은 `F1_score`, `F2_score`, `F3_score`, `F4_score`, `F5_score`이다.
- 해석 방향 요인점수 컬럼은 다음과 같다.

| 해석 방향 점수 | 의미 |
| --- | --- |
| `생활권접점_score` | 높을수록 도로·시가화지역에 가까운 생활권 접점 성격 |
| `선행건조_score` | 높을수록 선행 건조가 강한 상태 |
| `강풍_score` | 높을수록 발생시점·직전24h 강풍 성격 |
| `강수부족_score` | 높을수록 최근 강수 부족 상태 |
| `산지접근성_score` | 높을수록 고도가 높고 임도에 가까운 산지 접근성 성격 |

- 군집분석 입력은 원변수가 아니라 `factor_scores_for_clustering`의 해석 방향 요인점수를 사용한다.
- 단, `강수부족_score`에는 큰 음수 이상값이 있다.
  - 원점수 기준 `F4_score max = 31.9643`
  - 해석방향 기준 `강수부족_score min = -31.9643`
  - 해당 상위 사례는 `F_010993`, `F_007818`이며 둘 다 `2020-09-03 15:00:00`, `영동 해안형`, `영동`, `일반번지`, `오후(14~17시)`, `기타계절`이다.
- 이 값은 강수량이 매우 큰 사례일 가능성이 높으므로, 이후 군집분석 전에는 원점수 그대로 사용할지, robust scaling 또는 winsorizing을 적용할지 검토해야 한다.

#### 메타데이터별 요인점수 해석 준비
- 군집분석으로 넘어가기 전에 메타데이터별 요인점수 평균/중앙값/표준편차/표본 수를 먼저 비교한다.
- 비교 대상 메타데이터는 다음과 같다.
  - `기후지형유형`
  - `기후권역`
  - `addr_type`
  - `기후지형_번지유형`
  - `발생시간대구분`
  - `산불위험계절`
  - `L1_NAME`
  - `L2_NAME`
  - `원인프록시_500m`
- 주요 검증 질문은 다음과 같다.
  - `영동 해안형`은 `강풍_score`가 높은가?
  - `고지·산간형`은 `산지접근성_score`가 높은가?
  - `임야번지(산)`은 `산지접근성_score`가 높은가?
  - `일반번지`는 `생활권접점_score`가 높은가?
  - `입산활동 발화 후보`는 어떤 요인점수가 높은가?
- `2026-06-08_FA.ipynb`에는 이 비교를 수행하는 4번 분석 셀을 추가했다.
- 아직 4번 분석 셀은 실행하지 않았다.
- 현재까지는 메타데이터별 요인점수 해석을 위한 코드와 출력/저장 구조만 만든 상태이다.
- 4번 분석 결과가 저장되면 README에 메타데이터별 해석 결론을 이어서 작성한다.

#### 4번 분석 코드 추가 내역
- `## 메타데이터별 요인점수 해석` 섹션을 노트북 마지막에 추가했다.
- 이 섹션은 원 부호 요인점수가 아니라 해석 방향을 맞춘 `factor_scores_oriented_source`를 입력으로 사용한다.
- 노트북 현재 세션에 `factor_scores_oriented_source` 객체가 있으면 그대로 사용하고, 없으면 `jsw/Analysis/outputs/2026-06-08_FA_factor_scores_oriented.csv`를 다시 읽도록 했다.
- 분석 입력 요인점수는 다음 5개이다.
  - `생활권접점_score`
  - `선행건조_score`
  - `강풍_score`
  - `강수부족_score`
  - `산지접근성_score`
- 분석 대상 메타데이터는 다음 9개이다.
  - `기후지형유형`
  - `기후권역`
  - `addr_type`
  - `기후지형_번지유형`
  - `발생시간대구분`
  - `산불위험계절`
  - `L1_NAME`
  - `L2_NAME`
  - `원인프록시_500m`

#### 4번 코드가 생성하는 주요 객체
- `metadata_score_source`: 메타데이터별 요인점수 해석용 입력 테이블
- `metadata_factor_score_summary_long`: 메타데이터 그룹 x 요인점수별 long-format 요약표
- `metadata_factor_score_summary_wide`: 메타데이터 그룹별 wide-format 요약표
- `metadata_factor_score_kruskal`: 메타데이터별 요인점수 차이 Kruskal-Wallis 검정표
- `metadata_factor_score_top_groups`: 메타데이터/요인점수별 평균차 상위 그룹 Top 3
- `metadata_factor_score_bottom_groups`: 메타데이터/요인점수별 평균차 하위 그룹 Bottom 3
- `metadata_factor_score_key_questions`: 핵심 질문 검증표
- `metadata_factor_score_cause_profile`: `입산활동 발화 후보`의 요인점수 프로파일

#### 4번 코드가 계산하는 지표
- 각 메타데이터 그룹별로 다음 값을 계산한다.
  - 표본 수 `n`
  - 비중 `비중_pct`
  - 평균
  - 중앙값
  - 표준편차
  - 최소/최대
  - 전체 평균 대비 차이 `평균차`
  - 전체 표준편차 단위 평균차 `평균차_sd`
  - 전체 중앙값 대비 차이 `중앙값차`
- `평균차_sd`는 그룹 평균이 전체 평균에서 몇 표준편차만큼 벗어났는지를 보는 지표로 만들었다.
- 해석 기준은 다음처럼 코드에 명시했다.
  - `평균차_sd >= 0.30`: 해당 요인점수가 상대적으로 높은 그룹
  - `평균차_sd <= -0.30`: 해당 요인점수가 상대적으로 낮은 그룹
  - 그 사이는 전체 평균과 큰 차이가 작다고 판단
- `강수부족_score`는 큰 음수 이상값이 있으므로 평균만 보지 않고 중앙값도 함께 확인하도록 주석을 넣었다.
- `L2_NAME`처럼 표본 수가 작은 세부 그룹은 확정 결론이 아니라 후보 패턴으로만 보도록 해석 기준을 적었다.

#### 4번 코드가 저장하도록 설계한 파일
- 실행 시 `jsw/Analysis/outputs/` 아래에 다음 파일을 저장하도록 했다.
  - `2026-06-08_FA_metadata_factor_score_summary_long.csv`
  - `2026-06-08_FA_metadata_factor_score_summary_wide.csv`
  - `2026-06-08_FA_metadata_factor_score_kruskal.csv`
  - `2026-06-08_FA_metadata_factor_score_top_groups.csv`
  - `2026-06-08_FA_metadata_factor_score_bottom_groups.csv`
  - `2026-06-08_FA_metadata_factor_score_key_questions.csv`
  - `2026-06-08_FA_metadata_factor_score_cause_profile.csv`

#### 4번 출력 표시 보완
- 메타데이터별 요인점수 요약은 그룹 수와 문자열이 길어서 Pandas 기본 출력에서 말줄임표로 잘릴 수 있다.
- 이를 피하기 위해 `print_full_table()` 헬퍼 함수를 추가했다.
- 각 주요 표는 `display()` 출력과 함께 `to_string()` 기반 전체 텍스트 출력도 같이 나오도록 했다.
- 전체 출력 대상은 다음이다.
  - 전체 요인점수 요약
  - Kruskal-Wallis 검정표
  - 메타데이터별 요인점수 요약표
  - 메타데이터/요인별 평균차 상위 그룹 Top 3
  - 메타데이터/요인별 평균차 하위 그룹 Bottom 3
  - 핵심 질문 검증표
  - 입산활동 후보 요인점수 프로파일

#### 4번 핵심 질문 검증 코드
- 핵심 질문 검증표에는 다음 질문을 코드로 직접 정의했다.
  - `영동 해안형`은 `강풍_score`가 높은가?
  - `고지·산간형`은 `산지접근성_score`가 높은가?
  - `임야번지(산)`은 `산지접근성_score`가 높은가?
  - `일반번지`는 `생활권접점_score`가 높은가?
- 각 질문에 대해 해당 그룹의 `n`, 평균, 중앙값, 평균차, `평균차_sd`, 그룹 내 평균 순위, 중앙값 순위, 판정을 계산하도록 했다.
- `입산활동 발화 후보`는 별도 프로파일 표로 만들어 5개 요인점수 중 어떤 요인이 상대적으로 높은지 정렬해서 확인하도록 했다.

#### 4번 메타데이터별 요인점수 실행 결과 해석
- `2026-06-08_FA.ipynb`의 4번 메타데이터별 요인점수 해석 셀을 실행했고, 결과 CSV와 노트북 출력표가 생성되었다.
- 핵심 질문 검증 결과는 다음과 같다.

| 질문 | 결과 |
| --- | --- |
| `영동 해안형`은 `강풍_score`가 높은가? | 높음. 평균차_sd `+0.6118`, 중앙값차_sd `+0.8810` |
| `영동` 권역은 `강풍_score`가 높은가? | 높음. 평균차_sd `+0.6118`, 중앙값차_sd `+0.8810` |
| `고지·산간형`은 `산지접근성_score`가 높은가? | 높음. 평균차_sd `+0.9341`, 중앙값차_sd `+0.9808` |
| `임야번지(산)`은 `산지접근성_score`가 높은가? | 높음. 평균차_sd `+0.4678`, 중앙값차_sd `+0.4110` |
| `일반번지`는 `생활권접점_score`가 높은가? | 높음. 평균차_sd `+0.3338`, 중앙값차_sd `+0.3272` |
| `산불위험계절(11~5월)`은 `선행건조_score`가 높은가? | 전체 평균과 큰 차이는 작음. 평균차_sd `+0.1965` |
| `산불위험계절(11~5월)`은 `강수부족_score`가 높은가? | 전체 평균과 큰 차이는 작음. 평균차_sd `-0.0184` |

- 따라서 FA 요인점수는 EDA에서 기대한 `영동 해안형 = 강풍`, `고지·산간형 = 산지접근성`, `임야번지(산) = 산지접근성`, `일반번지 = 생활권접점` 구조와 대체로 일치한다.
- `기후지형_번지유형` 조합에서 패턴이 가장 선명했다.
  - `영동 해안형 / 임야번지(산)`: `강풍_score +0.8607`, `선행건조_score +0.6992`
  - `고지·산간형 / 임야번지(산)`: `산지접근성_score +1.0709`, `생활권접점_score -1.0460`
  - `영동 해안형 / 일반번지`: `강풍_score +0.5363`, `생활권접점_score +0.4648`
  - `영서 내륙형 / 임야번지(산)`: `산지접근성_score +0.6346`, `생활권접점_score -1.1174`
- `입산활동 발화 후보`는 5개 요인 중 어느 하나도 기준값 `|평균차_sd| >= 0.30`을 넘지 않았다.
  - 가장 높은 축은 `산지접근성_score +0.1827`이지만 약한 경향 수준이다.
  - 따라서 현재 `원인프록시_500m`는 요인점수만으로 강하게 구분되는 메타데이터는 아니다.
- `산불위험계절`은 선행건조나 강수부족보다 `강풍_score`에서 더 눈에 띄는 경향을 보였다.
  - 산불위험계절의 `강풍_score`는 평균차_sd `+0.269`, 중앙값차_sd `+0.377`이다.
  - 선행건조는 평균차_sd `+0.1965`로 약하고, 강수부족은 거의 차이가 없다.
- `강수부족_score`는 큰 음수 이상값이 있어 평균 해석이 흔들릴 수 있다.
  - 전체 최소값은 `-31.9643`이고 첨도는 `866.14`로 매우 크다.
  - 이후 군집분석 전에는 원점수 그대로 사용, robust scaling, winsorizing을 민감도 분석으로 비교한다.

#### 다음 단계 코드 추가: L1/L2 토지피복별 요인점수 상세 해석
- `2026-06-08_FA.ipynb`에 `## 5. L1/L2 토지피복별 요인점수 상세 해석` 섹션을 추가했다.
- 이 섹션은 기존 4번 결과 CSV를 읽어 실행되므로, 앞 셀을 다시 실행하지 않아도 된다.
- 새 코드가 생성하는 주요 객체는 다음과 같다.
  - `landcover_factor_score_summary_long`
  - `landcover_factor_score_summary_wide`
  - `landcover_factor_score_kruskal`
  - `landcover_factor_score_interpretation_candidates`
  - `landcover_group_dominant_factors`
  - `landcover_factor_score_top_bottom_groups`
  - `landcover_factor_score_key_patterns`
- 새 코드가 저장하도록 설계한 파일은 다음과 같다.
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_summary_long.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_summary_wide.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_kruskal.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_interpretation_candidates.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_dominant_factors.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_top_bottom_groups.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_key_patterns.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_L1_NAME_factor_score_heatmap.png`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_L2_NAME_factor_score_heatmap.png`
- 새 코드의 핵심 검증 질문은 다음과 같다.
  - `시가화건조지역`은 `생활권접점_score`가 높은가?
  - `산림지역`은 `산지접근성_score`가 높고 `생활권접점_score`가 낮은가?
  - `도로`, `상업지역`은 `생활권접점_score`가 높은가?
  - `침엽수림`, `활엽수림`은 `산지접근성_score`가 높은가?
  - `밭`은 `생활권접점_score`가 낮은가?
- 이 섹션은 아직 실행하지 않았으며, 실행 후 토지피복별 해석을 정리한 다음 군집분석으로 넘어간다.

#### 5번 L1/L2 토지피복별 요인점수 실행 결과 해석
- `2026-06-08_FA.ipynb`의 `## 5. L1/L2 토지피복별 요인점수 상세 해석` 셀을 실행했고, L1/L2 토지피복별 결과 CSV가 생성되었다.
- 생성된 주요 CSV는 다음과 같다.
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_summary_long.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_summary_wide.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_kruskal.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_interpretation_candidates.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_dominant_factors.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_top_bottom_groups.csv`
  - `jsw/Analysis/outputs/2026-06-08_FA_landcover_factor_score_key_patterns.csv`

##### 토지피복별 요인점수 검정 결과
- Kruskal-Wallis 검정은 모든 L1/L2 토지피복-요인점수 조합에서 FDR 0.05 기준 유의했다.
- 다만 표본 수가 커서 p-value만으로 해석하지 않고, 효과크기(`epsilon_sq`)와 `최대_abs_평균차_sd`를 함께 기준으로 삼았다.

| 메타데이터 | 요인점수 | epsilon_sq | 최대_abs_평균차_sd | 해석 |
| --- | ---: | ---: | ---: | --- |
| `L2_NAME` | `생활권접점_score` | 0.7127 | 1.4896 | 토지피복과 가장 강하게 연결되는 핵심 축 |
| `L1_NAME` | `생활권접점_score` | 0.6697 | 1.3738 | 대분류에서도 매우 강한 차이 |
| `L2_NAME` | `산지접근성_score` | 0.1144 | 1.3076 | 산림·산지 세부 유형 구분에 의미 있음 |
| `L1_NAME` | `산지접근성_score` | 0.1035 | 0.6530 | 산림지역과 비산림지역을 구분하는 보조 축 |
| `L2_NAME` | `강수부족_score` | 0.0926 | 0.2759 | 통계적으로 유의하지만 해석 기준 `0.30` 미만 |
| `L2_NAME` | `강풍_score` | 0.0442 | 0.9125 | 일부 소분류에서 차이가 있으나 토지피복 고유 효과보다 지역·기후 혼합 가능성 큼 |
| `L2_NAME` | `선행건조_score` | 0.0253 | 0.8015 | 소표본 그룹 영향이 커 보조 해석에 한정 |
| `L1_NAME` | `강풍_score` | 0.0069 | 0.4874 | L1 대분류 기준에서는 강풍 차이가 약함 |

##### L1_NAME 대분류 해석
- `시가화건조지역`은 `n=1,129`, 비중 `42.28%`이며 `생활권접점_score 평균차_sd +0.824`, 중앙값차_sd `+0.622`로 가장 뚜렷한 생활권 접점형이다.
- `산림지역`은 `n=437`, 비중 `16.37%`이며 `생활권접점_score -1.374`, `산지접근성_score +0.607`로 생활권과 멀고 산지성이 강하다.
- `농업지역`은 `n=250`, 비중 `9.36%`이며 `생활권접점_score -0.486`으로 도시 접점보다는 농촌·외곽형 성격이다.
- `나지`는 `생활권접점_score -0.304`로 낮은 편이지만 핵심 유형으로 단정하기에는 약하다.
- `초지`는 평균 기준 `생활권접점_score -0.255`로 기준값 미만이지만, 중앙값차_sd는 `-0.602`라 생활권 접점이 낮은 사례가 많을 수 있다.
- `습지`는 주요 요인 모두 `|평균차_sd| < 0.30`이라 뚜렷한 요인 특성이 약하다.
- `미분류`는 `강풍_score +0.487`, `산지접근성_score -0.653`이지만 분류 의미가 불명확하므로 핵심 해석에서 제외한다.

##### L2_NAME 세분류 해석
- 생활권 접점형 세분류는 `도로`, `상업지역`, `주거지역`, `공공시설지역`, `문화·체육·휴양지역`이다.
  - `도로`: `n=847`, `생활권접점_score +0.918`, 중앙값차_sd `+0.663`
  - `상업지역`: `n=145`, `생활권접점_score +0.575`, `산지접근성_score -0.308`
  - `주거지역`: `n=50`, `생활권접점_score +0.541`
  - `공공시설지역`: `n=33`, `생활권접점_score +0.497`
  - `문화·체육·휴양지역`: `n=53`, `생활권접점_score +0.478`, `강풍_score -0.913`
- 산림·산지형 세분류는 `활엽수림`, `침엽수림`, `혼효림`, 일부 `밭`, `자연나지`이다.
  - `활엽수림`: `n=170`, `생활권접점_score -1.490`, `산지접근성_score +0.636`, `선행건조_score +0.347`
  - `침엽수림`: `n=217`, `생활권접점_score -1.427`, `산지접근성_score +0.525`
  - `혼효림`: `n=50`, `생활권접점_score -0.749`, `산지접근성_score +0.860`, `강풍_score -0.714`
  - `밭`: `n=203`, `생활권접점_score -0.575`, 중앙값차_sd `-0.787`
  - `자연나지`: `n=11`, `산지접근성_score +1.308`, `생활권접점_score -0.506`이나 표본 수가 작아 후보 패턴으로만 둔다.
- `인공초지`는 `n=558`로 표본 수는 크지만 모든 평균차가 `|0.30|` 미만이라 특정 요인형을 대표하기보다는 중간 또는 혼합 유형에 가깝다.
- `인공나지`는 `생활권접점_score -0.291`, `강수부족_score -0.276`으로 기준값에 근접하지만 넘지 않아 강한 해석축으로 두지 않는다.
- `과수원`은 `선행건조_score +0.744`, `강풍_score +0.636`이지만 `n=12`라 소표본 후보 패턴이다.

##### 요인별 토지피복 해석
- `생활권접점_score`는 토지피복을 가장 잘 설명하는 축이다.
  - 상위 그룹: `도로 +0.918`, `상업지역 +0.575`, `주거지역 +0.541`, `공공시설지역 +0.497`, `문화·체육·휴양지역 +0.478`
  - 하위 그룹: `활엽수림 -1.490`, `침엽수림 -1.427`, `혼효림 -0.749`, `밭 -0.575`, `자연초지 -0.584`
- `산지접근성_score`는 산림 세부 유형을 설명하는 보조 축이다.
  - 상위 그룹: `혼효림 +0.860`, `활엽수림 +0.636`, `침엽수림 +0.525`, `자연나지 +1.308`
  - 하위 그룹: `상업지역 -0.308`, `논 -0.325`, `문화·체육·휴양지역 -0.447`, `미분류 -0.653`
- `강풍_score`는 토지피복 핵심 축으로 보기 어렵다.
  - `과수원 +0.636`, `자연나지 +0.405`, `미분류 +0.487`은 모두 표본 수가 작거나 의미가 불명확하다.
  - 강풍은 토지피복보다 `영동 해안형`, `영동 권역`, `기후지형_번지유형`에서 해석하는 것이 더 적절하다.
- `선행건조_score`와 `강수부족_score`도 토지피복 주해석축은 아니다.
  - `활엽수림 +0.347`은 산림 내 건조 경향으로 보조 해석할 수 있다.
  - `강수부족_score`는 주요 그룹 대부분이 `|평균차_sd| < 0.30`이라 토지피복별 차이보다는 이상값과 시기별 강수 사건의 영향을 먼저 점검해야 한다.

##### 토지피복 해석 결론
- 토지피복별 결과는 EDA에서 확인한 공간적 발화 환경과 대체로 일치한다.
- 도로·시가화·상업·주거 계열은 `생활권접점_score`가 높아 인간 활동 접점과 가까운 산불 유형으로 해석된다.
- 산림지역과 침엽수림·활엽수림·혼효림은 `생활권접점_score`가 낮고 `산지접근성_score`가 높아 산림·산지형 산불 유형으로 해석된다.
- 농업지역과 밭은 생활권 접점이 낮은 농촌·외곽형으로 나타나며, 일반 도시 접점형과는 구분된다.
- 강풍·건조·강수부족은 토지피복보다는 기후지형유형, 기후권역, 계절, 시간창 변수에서 해석하는 것이 더 타당하다.
- 따라서 토지피복은 군집분석 입력 변수로 직접 넣기보다, 군집 결과를 해석하는 메타데이터로 사용하는 것이 적절하다.
