# 머신러닝 학습 로그

## 1. 문서 목적

이 문서는 `jsw/Analysis/new_machine_learning/머신러닝_학습_계획.md`에 따라 실제로 수행한 학습 과정, 산출물, 평가지표와 단계별 판단을 기록한다.

각 단계에서는 다음 원칙을 지킨다.

- 모델을 한 종류씩 실행하고 결과를 별도 파일로 저장한다.
- development 데이터의 outer OOF 예측으로 모델과 피처셋을 비교한다.
- threshold는 outer-validation 정답을 직접 보고 정하지 않고 inner OOF에서 정한다.
- lockbox는 최종 모델·피처셋·calibration·threshold가 확정되기 전까지 사용하지 않는다.
- 실행 전 계획과 실행 후 결과를 구분하여 기록한다.

---

## Step 1. 여러 단일 모델 피처셋 screening

### Step 1 상태

- 작성일: 2026-06-22
- 상태: 실행 전 계획
- 목적: 앙상블 전에 여러 머신러닝 모델을 각각 독립적으로 학습하고 모델·피처셋 조합별 성능을 비교
- 모델 단위: 각 알고리즘을 서로 결합하지 않은 단일 모델

Step 1에서 실행할 단일 머신러닝 모델은 다음 6개다.

| model_name | 역할 |
| --- | --- |
| `HistGradientBoosting` | sklearn 기반 비선형 boosting 기준 |
| `LightGBM` | 빠른 boosting 및 불균형 처리 |
| `XGBoost` | 규제가 가능한 boosting 비교 |
| `CatBoost` | 범주형 변수가 포함된 데이터 비교 |
| `RandomForest` | bagging 계열 기준 |
| `ExtraTrees` | 무작위성이 큰 bagging 비교 |

여기서 `단일 모델`은 하나의 알고리즘만 실행한다는 뜻이 아니다. 위 6개 모델을 각각 따로 학습하며, 모델들의 예측값을 평균·가중평균·stacking 등으로 결합하지 않는다는 뜻이다. 단일 모델 결과가 확보된 뒤에만 후속 단계에서 앙상블의 추가효과를 비교한다.

Step 1에서는 각 모델의 기본 설정과 사전에 정한 불균형 처리 설정만 비교한다. 모든 피처셋을 동일한 기본 모델 조건에서 공정하게 screening하기 위해 하이퍼파라미터 튜닝은 하지 않는다.

하이퍼파라미터 튜닝은 Step 2에서 수행한다. Step 1에서 탈락한 전체 192개 조합을 모두 튜닝하지는 않되, 성능 상위 조합만 너무 좁게 튜닝하지도 않는다. Step 2는 `주 튜닝 후보`와 `모델별 구제 튜닝 후보`를 분리해 진행한다.

SHAP 기반 변수 제거, 모델 결합, calibration과 lockbox 평가도 Step 1에서는 수행하지 않는다.

### Step 1-1. 입력 데이터와 분할

- 입력 데이터: `data/학습데이터/최종_머신러닝_학습데이터.csv`
- 목적변수: `Target`
- 개발 데이터: 모델·피처셋 비교와 OOF 예측 생성에 사용
- lockbox: Step 1에서 접근 및 평가 금지
- outer CV: strict group 5-fold
- inner CV: 각 outer-train 내부의 strict group 4-fold
- 분할 단위: 계획서에서 정의한 `date_exposure_component_group`

모든 피처셋과 불균형 설정은 동일한 split manifest를 사용한다. 조합마다 fold를 새로 만들지 않는다.

다음 변수는 모델 입력 `X`에서 제외한다.

- `Target`
- `샘플ID`, `기준시각`, `기상셀ID`
- `샘플유형`, `실험안`, `시간샘플링방식`, `공간층`
- `fire_id`, `원본_fire_id`, `후보점ID`, `source_fire_id`
- `모델링_그룹ID`
- `캐나다지수_기준날짜`, `캐나다지수_정책`
- `위도`, `경도`
- 그 밖에 변수사전에서 `X 제외`로 지정한 변수

`샘플유형`은 모델 입력에는 사용하지 않고 `Target_0A`, `Target_0B1`, `Target_0B2` subgroup 평가에만 사용한다.

### Step 1-2. 비교할 피처셋

각 단일 모델에 다음 세 실험 축을 완전 교차한 동일한 16개 피처셋을 적용한다.

| 실험 축 | 선택지 | 개수 |
| --- | --- | ---: |
| 날씨·공간 | `WS_CORE`, `WS_ALL` | 2 |
| 캐나다지수 | `CANADA_NONE`, `CANADA_CORE`, `CANADA_LONG`, `CANADA_ALL` | 4 |
| 토지피복 | `LC_NONE`, `LC_USED` | 2 |

```text
2 × 4 × 2 = 16개 피처셋
```

피처셋 ID는 다음과 같다.

1. `WS_CORE__CANADA_NONE__LC_NONE`
2. `WS_CORE__CANADA_CORE__LC_NONE`
3. `WS_CORE__CANADA_LONG__LC_NONE`
4. `WS_CORE__CANADA_ALL__LC_NONE`
5. `WS_CORE__CANADA_NONE__LC_USED`
6. `WS_CORE__CANADA_CORE__LC_USED`
7. `WS_CORE__CANADA_LONG__LC_USED`
8. `WS_CORE__CANADA_ALL__LC_USED`
9. `WS_ALL__CANADA_NONE__LC_NONE`
10. `WS_ALL__CANADA_CORE__LC_NONE`
11. `WS_ALL__CANADA_LONG__LC_NONE`
12. `WS_ALL__CANADA_ALL__LC_NONE`
13. `WS_ALL__CANADA_NONE__LC_USED`
14. `WS_ALL__CANADA_CORE__LC_USED`
15. `WS_ALL__CANADA_LONG__LC_USED`
16. `WS_ALL__CANADA_ALL__LC_USED`

`INDEXED_FFMC_REFERENCE`는 6개 단일 머신러닝 모델의 16개 주 피처셋에 포함하지 않는다. 선행연구 재현용 단독 로지스틱 기준선으로 별도 실행한다.

### Step 1-3. 불균형 처리 설정

Target 1 비율이 약 9.1%이므로 다음 두 설정을 비교한다.

| imbalance_option | 설정 |
| --- | --- |
| `none` | class weight 미적용 |
| `balanced` | `class_weight="balanced"` |

각 단일 모델의 비교 단위는 다음과 같다.

```text
16개 피처셋 × 2개 불균형 설정 = 32개 조합
```

6개 단일 모델 전체의 주 비교 조합은 다음과 같다.

```text
6개 모델 × 16개 피처셋 × 2개 불균형 설정 = 192개 조합
```

기존 `샘플가중치`를 적용하는 실험은 기본 class weight 결과를 확인한 뒤 별도 민감도 실험으로 진행한다. Step 1에서는 SMOTE를 사용하지 않는다.

### Step 1-4. 전처리와 모델 설정

전처리는 각 outer-train 안에서만 학습하여 validation 정보가 들어오지 않도록 pipeline으로 구성한다.

- 수치형 변수: 숫자형 변환과 결측 처리
- 범주형 변수: 학습 fold에서만 범주 인코딩
- 월·시간·풍향: 계획서에서 정의한 sin·cos 변수 사용
- 거리 변수: 기본적으로 `log1p` 변수를 사용하고 원본과 중복 투입하지 않음
- 토지피복: 범주형 표현과 이진 flag를 동시에 중복 투입하지 않음
- 변수 선택과 전처리 fitting: 반드시 train fold 내부에서만 수행

각 모델의 하이퍼파라미터는 사전에 정한 기본 설정으로 고정한다. 이 단계에서는 모델·피처셋의 넓은 비교가 목적이므로 피처셋마다 서로 다른 튜닝 결과를 적용하지 않는다.

모델별 고정 설정과 random seed는 실행 스크립트와 각 모델의 `run_manifest.json`에 저장한다.

### Step 1-5. 학습과 OOF 예측 생성

각 `모델 × 피처셋 × 불균형 설정` 조합에서 다음 순서로 실행한다.

1. outer 5-fold를 순회한다.
2. outer-train 데이터만 사용해 전처리와 모델을 학습한다.
3. 학습에 사용하지 않은 outer-validation의 산불위험 예측값을 생성한다.
4. 5개 fold의 예측값을 합쳐 development 전체 OOF 예측을 만든다.
5. 각 표본에 OOF 예측이 정확히 한 번 존재하는지 검사한다.

순위 성능용 outer 모델의 예정 학습 횟수는 다음과 같다.

```text
6개 모델 × 16개 피처셋 × 2개 불균형 설정 × outer 5-fold = 960회
```

정직한 threshold 평가를 위해 각 outer fold에서는 추가로 다음 절차를 수행한다.

1. outer-train을 inner 4-fold로 나눈다.
2. inner OOF 예측으로 threshold 후보를 정한다.
3. outer-train 전체로 모델을 다시 학습한다.
4. 미리 정한 threshold를 outer-validation에 적용한다.
5. outer-validation의 실제 recall, precision, FN과 selected rate를 기록한다.

inner OOF threshold 산출에는 `192 × outer 5 × inner 4 = 3,840회`의 추가 학습이 필요하다. outer 모델 960회를 합치면 Step 1의 예정 fit 횟수는 최대 4,800회다. 모델별 조기 실패나 지원하지 않는 불균형 옵션이 있을 수 있으므로 실행 로그에는 예정 횟수와 별도로 실제 성공·실패 fit 수와 소요시간을 기록한다.

### Step 1-6. 저장할 예측 데이터

모든 결과는 다음 폴더 아래에 저장한다.

```text
jsw/Analysis/new_machine_learning/outputs/step1_single_models/
```

모델별 하위 폴더는 만들지 않는다. 모든 파일을 `step1_single_models` 폴더에 저장하고 파일명 앞부분에 모델명을 포함한다.

#### OOF 예측

각 단일 모델의 development OOF 예측은 다음 규칙으로 별도 CSV에 저장한다.

```text
oof__{model_name}__{feature_set}__{imbalance_option}.csv
```

예:

```text
oof__xgboost__WS_CORE__CANADA_CORE__LC_USED__balanced.csv
oof__random_forest__WS_ALL__CANADA_NONE__LC_NONE__none.csv
```

각 파일은 최소한 다음 열을 포함한다.

| 열 | 설명 |
| --- | --- |
| `sample_id` | 표본 식별자 |
| `outer_fold` | 예측에 사용된 outer fold |
| `y_true` | 실제 Target |
| `y_prob` | OOF 산불위험 예측값 |
| `feature_set` | 피처셋 ID |
| `model_name` | 단일 모델 이름 |
| `imbalance_option` | `none` 또는 `balanced` |
| `sample_type` | subgroup 평가용 표본유형 |
| `climate_type` | 기후지형유형 |
| `group_id` | 누수 방지 그룹 ID |

예측값은 임의로 반올림하지 않고 저장한다.

#### 요약 데이터

다음 CSV도 별도로 저장한다.

| 파일 | 내용 |
| --- | --- |
| `summary__{model_name}.csv` | 해당 모델의 32개 조합 OOF 성능 비교 |
| `fold_metrics__{model_name}.csv` | 해당 모델의 조합별·fold별 성능 |
| `threshold_metrics__{model_name}.csv` | 해당 모델의 운영점과 threshold 지표 |
| `subgroup_metrics__{model_name}.csv` | 해당 모델의 Target 0A·0B1·0B2 및 기후지형유형별 성능 |
| `top_risk_metrics__{model_name}.csv` | 해당 모델의 상위 5·10·20·30% 위험구간 포착 성능 |
| `validation_checks__{model_name}.csv` | 해당 모델의 누수, 누락, 중복, NaN·inf 검사 |
| `run_manifest__{model_name}.json` | 해당 모델의 변수 목록, 설정, seed와 실행시간 |
| `all_single_models_summary.csv` | 6개 모델의 192개 조합 통합 비교표 |

Step 1에서는 lockbox 예측 파일을 생성하지 않는다.

### Step 1-7. 계산할 평가지표

#### 전체 OOF 순위·확률 지표

- AUPRC
- AUROC
- Brier score
- log loss
- fold별 AUPRC 평균·표준편차
- group bootstrap 95% CI

#### Threshold 운영점

- `fixed_0.50`
- `best_f1`
- `best_f2`
- `recall_ge_0.70`
- `recall_ge_0.90`
- `recall_ge_0.95`

각 운영점에서 다음 값을 저장한다.

- threshold
- TP, FP, FN, TN
- recall, precision, specificity
- false positive rate
- F1, F2
- balanced accuracy, MCC
- selected_n, selected_rate
- FP per TP

#### Subgroup 평가

- `Target_1 vs Target_0A` AUPRC
- `Target_1 vs Target_0B1` AUPRC
- `Target_1 vs Target_0B2` AUPRC
- 기후지형유형별 AUPRC와 recall

#### Top-risk 평가

- 상위 5%, 10%, 20%, 30%
- 선택 표본 수
- 실제 산불 포착 수
- capture rate
- precision
- lift

### Step 1-8. 결과 판단 규칙

Step 1에서는 단일 지표만으로 모델이나 피처셋을 탈락시키지 않는다.

필수 통과 조건은 다음과 같다.

- split 누수 0건
- OOF 예측 누락·중복 0건
- 예측값 NaN·inf 0건
- 모든 outer fold에 Target 1 존재
- `recall_ge_0.70` 운영점 산출 가능

통과 조합은 다음 순서로 비교한다.

1. 전체 OOF AUPRC
2. `Target_1 vs Target_0A` AUPRC
3. fold별 성능 안정성
4. recall 70%에서 precision
5. recall 70%에서 selected rate와 FN
6. top 10%·20% 산불 포착률
7. Brier score와 log loss

최고 AUPRC와 차이가 `0.005` 이내인 피처셋들은 성능이 유사한 후보군으로 묶는다. 성능이 유사하면 다음 원칙을 적용한다.

- `WS_CORE`와 `WS_ALL` 성능이 같으면 변수 수가 적은 `WS_CORE` 우선
- 캐나다지수 추가 전후 성능이 같으면 캐나다지수를 사용하지 않는 단순 모델 우선
- `CANADA_CORE`와 `CANADA_ALL` 성능이 같으면 `CANADA_CORE` 우선
- 토지피복 추가효과가 없으면 `LC_NONE` 우선
- 특정 fold에서만 좋은 조합은 우선순위를 낮춤

Step 1 결과로 각 단일 모델의 상위 피처셋과 전체 상위 `모델 × 피처셋 × 불균형 설정` 조합을 선정한다. 성능이 명확하게 낮거나 fold 안정성이 떨어지는 조합은 다음 튜닝 단계에서 제외한다.

앙상블은 Step 1의 단일 모델 성능보다 실제로 개선되는지를 후속 단계에서 비교한다. 따라서 Step 1 결과는 이후 앙상블 성능의 기준선이 된다.

### Step 1-9. 실행 후 로그에 작성할 내용

실행이 완료되면 이 문서에 다음 내용을 추가한다.

1. 실행 스크립트와 실행 일시
2. 데이터 행 수, development 행 수와 양성률
3. 실제 사용된 split manifest
4. 총 192개 주 조합의 성공·실패 수와 실제 fit 횟수
5. 모델별 32개 조합 성능표
6. 6개 단일 모델 통합 성능표
7. 모델별 최고 조합과 전체 상위 조합
8. 상위 조합의 threshold별 평가지표
9. subgroup 및 top-risk 결과
10. 피처셋별 캐나다지수·토지피복 추가효과
11. 다음 단계 유지·탈락 모델·피처셋과 판단 근거
12. 오류, 경고, 제한사항

아래에 2026-06-22 Step 1 실행 결과를 기록한다.

### Step 1-10. 실행 결과

#### 실행 정보

- 실행 스크립트: `jsw/Analysis/new_machine_learning/step1_single_models.py`
- 입력 데이터: `data/학습데이터/최종_머신러닝_학습데이터.csv`
- 결과 폴더: `jsw/Analysis/new_machine_learning/outputs/step1_single_models/`
- 실행 시작 시각: 2026-06-22 13:29:44
- 마지막 결과 저장 시각: 2026-06-22 14:32:50
- 전체 행 수: 17,045
- Target 1: 1,553
- Target 0: 15,492
- 양성률: 9.11%
- outer CV: `모델링_그룹ID` 기준 strict group 5-fold
- inner CV: 각 outer-train 내부 strict group 4-fold
- planned fit 수: 4,800회
- 생성된 OOF 예측 파일 수: 192개

의존성 점검 결과 `sklearn`, `lightgbm`, `xgboost`, `catboost`는 모두 사용 가능했다.

실시간 진행 표시는 이후 실행부터 `tqdm` 막대바를 사용하도록 스크립트를 수정했다. 기본 실행에서는 전체 fit 진행률과 현재 모델 fit 진행률을 막대바로 표시한다. `tqdm`이 없거나 막대바가 불편한 환경에서는 `--no-progress-bar` 옵션으로 기존 텍스트 로그를 사용할 수 있다.

#### 실행 성공 여부와 검증

| 항목 | 결과 |
| --- | ---: |
| 전체 조합 | 192 |
| 성공 조합 | 192 |
| 실패 조합 | 0 |
| outer group leakage | 0 |
| OOF 예측 누락 | 0 |
| OOF 예측 중복 | 0 |
| 예측확률 NaN | 0 |
| 예측확률 inf | 0 |
| Target 1 없는 fold | 0 |

따라서 Step 1 결과는 누수·누락·실패 없이 정상 산출된 것으로 본다.

이 표의 의미는 성능 수치 이전에 결과를 신뢰할 수 있는지 확인하는 것이다. 192개 조합이 모두 성공했고 group leakage와 OOF 누락이 없으므로, 이후 AUPRC·threshold·subgroup 비교는 동일한 5-fold OOF 조건에서 나온 값으로 비교 가능하다. 단, 이 단계는 아직 lockbox 평가가 아니므로 최종 일반화 성능으로 해석하지 않는다.

#### 모델별 최고 조합

| 순위 | model | 최고 feature_set | imbalance | AUPRC | AUROC | Brier | log loss | fold AUPRC std |
| ---: | --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| 1 | `random_forest` | `WS_CORE__CANADA_ALL__LC_USED` | balanced | 0.6675 | 0.9112 | 0.0522 | 0.1942 | 0.0091 |
| 2 | `lightgbm` | `WS_ALL__CANADA_ALL__LC_USED` | none | 0.6580 | 0.9124 | 0.0500 | 0.1760 | 0.0089 |
| 3 | `extra_trees` | `WS_ALL__CANADA_ALL__LC_NONE` | balanced | 0.6567 | 0.8998 | 0.0584 | 0.2215 | 0.0098 |
| 4 | `hist_gradient_boosting` | `WS_ALL__CANADA_ALL__LC_USED` | none | 0.6406 | 0.9078 | 0.0515 | 0.1806 | 0.0076 |
| 5 | `catboost` | `WS_ALL__CANADA_ALL__LC_USED` | none | 0.5606 | 0.8849 | 0.0578 | 0.2027 | 0.0107 |
| 6 | `xgboost` | `WS_ALL__CANADA_ALL__LC_USED` | none | 0.5416 | 0.8805 | 0.0588 | 0.2050 | 0.0079 |

이 표는 각 알고리즘이 Step 1 기본 preset에서 도달한 최고 성능을 비교한 것이다. 1차 screening 기준 최고 조합은 `random_forest + WS_CORE__CANADA_ALL__LC_USED + balanced`다. AUPRC가 0.6675로 가장 높고 fold 간 표준편차도 0.0091로 작다. 산불 발생 표본이 적은 자료에서는 AUPRC를 우선 지표로 보는 것이 맞으므로, RandomForest는 Step 2의 1순위 튜닝 대상이다.

다만 확률 품질만 보면 `lightgbm + WS_ALL__CANADA_ALL__LC_USED + none`이 Brier 0.0500, log loss 0.1760으로 가장 좋다. 이 말은 LightGBM이 산불 순위화뿐 아니라 예측확률의 calibration 측면에서도 좋은 출발점일 수 있다는 뜻이다. 따라서 Step 2에서는 AUPRC 1위 모델만 튜닝하지 말고 RandomForest와 LightGBM을 함께 유지한다.

ExtraTrees는 AUPRC가 LightGBM과 거의 비슷하지만 Brier와 log loss가 더 나쁘다. 즉 순위화는 강하지만 확률값은 덜 안정적일 수 있다. HistGradientBoosting은 상위 3개보다 낮지만 sklearn 기반 boosting 기준선으로 의미가 있다. XGBoost와 CatBoost는 기본 preset 성능이 낮지만, 두 모델은 하이퍼파라미터 민감도가 큰 편이므로 여기서 완전히 탈락시키지는 않고 Step 2에서 짧은 구제 튜닝으로 확인한다.

#### 전체 상위 조합

| 순위 | model | feature_set | imbalance | AUPRC | AUROC | Brier | log loss |
| ---: | --- | --- | --- | ---: | ---: | ---: | ---: |
| 1 | `random_forest` | `WS_CORE__CANADA_ALL__LC_USED` | balanced | 0.6675 | 0.9112 | 0.0522 | 0.1942 |
| 2 | `random_forest` | `WS_ALL__CANADA_ALL__LC_USED` | balanced | 0.6663 | 0.9066 | 0.0522 | 0.1951 |
| 3 | `lightgbm` | `WS_ALL__CANADA_ALL__LC_USED` | none | 0.6580 | 0.9124 | 0.0500 | 0.1760 |
| 4 | `extra_trees` | `WS_ALL__CANADA_ALL__LC_NONE` | balanced | 0.6567 | 0.8998 | 0.0584 | 0.2215 |
| 5 | `extra_trees` | `WS_CORE__CANADA_ALL__LC_NONE` | balanced | 0.6559 | 0.9011 | 0.0626 | 0.2318 |
| 6 | `random_forest` | `WS_CORE__CANADA_ALL__LC_USED` | none | 0.6558 | 0.9037 | 0.0521 | 0.1884 |
| 7 | `random_forest` | `WS_ALL__CANADA_ALL__LC_USED` | none | 0.6556 | 0.9004 | 0.0527 | 0.1907 |
| 8 | `random_forest` | `WS_ALL__CANADA_LONG__LC_USED` | balanced | 0.6554 | 0.9045 | 0.0534 | 0.1980 |
| 9 | `lightgbm` | `WS_CORE__CANADA_ALL__LC_USED` | none | 0.6547 | 0.9128 | 0.0502 | 0.1762 |
| 10 | `lightgbm` | `WS_ALL__CANADA_ALL__LC_USED` | balanced | 0.6542 | 0.9105 | 0.0773 | 0.2477 |

이 표는 모델별 최고만 보는 것이 아니라 전체 `모델 × 피처셋 × 불균형 설정` 조합 중 상위권이 어떤 구조로 형성되는지 보여준다. 상위권은 RandomForest, LightGBM, ExtraTrees에 집중되어 있으며, 상위 10개 중 9개가 `CANADA_ALL`을 포함한다. `CANADA_ALL`이 아닌 조합은 `random_forest + WS_ALL__CANADA_LONG__LC_USED + balanced` 1개뿐이다.

1위와 2위의 AUPRC 차이는 0.0012로 매우 작다. 따라서 `WS_CORE`가 1위라고 해서 `WS_ALL`을 바로 버리지는 않는다. 다만 `WS_CORE`가 더 단순하고 AUPRC도 가장 높으므로, 최종 후보가 비슷하면 단순한 `WS_CORE`를 우선하는 근거가 된다.

상위 10개 안에 `none`과 `balanced`가 모두 섞여 있다. 따라서 class weight는 전체적으로 평균 효과가 나쁘더라도 특정 모델에서는 도움이 된다. Step 2에서는 불균형 옵션을 일괄 적용하지 않고 후보 조합별로 유지한다.

#### 최고 조합의 threshold 운영점

대상 조합: `random_forest + WS_CORE__CANADA_ALL__LC_USED + balanced`

full OOF 전체에서 threshold를 정했을 때의 운영점은 다음과 같다.

| operating point | threshold | recall | precision | selected rate | TP | FP | FN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| fixed 0.50 | 0.5000 | 0.3973 | 0.8903 | 4.07% | 617 | 76 | 936 |
| best F1 | 0.3655 | 0.5319 | 0.6726 | 7.20% | 826 | 402 | 727 |
| best F2 | 0.2267 | 0.7663 | 0.3705 | 18.84% | 1,190 | 2,022 | 363 |
| recall ≥ 0.70 | 0.2611 | 0.7006 | 0.4292 | 14.87% | 1,088 | 1,447 | 465 |
| recall ≥ 0.90 | 0.1316 | 0.9002 | 0.2529 | 32.43% | 1,398 | 4,130 | 155 |
| recall ≥ 0.95 | 0.0818 | 0.9504 | 0.2021 | 42.85% | 1,476 | 5,828 | 77 |

inner OOF에서 threshold를 정하고 outer-validation에 적용한 더 보수적인 `recall_ge_0.70` 결과는 다음과 같다.

| 기준 | recall | precision | selected rate | TP | FP | FN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| inner OOF threshold → outer validation | 0.7199 | 0.4060 | 16.16% | 1,118 | 1,636 | 435 |

보고서에서는 full OOF threshold 값만 단독으로 쓰기보다, threshold 선택 과정이 낙관적으로 보이지 않도록 inner OOF 기반 결과도 함께 제시하는 것이 적절하다.

이 표에서 중요한 점은 기본 threshold 0.50을 사용하면 precision은 0.8903으로 높지만 recall이 0.3973에 그친다는 것이다. 산불을 놓치면 안 된다는 목적에는 부적합하다. 따라서 이 문제에서는 `0.50` 고정 threshold를 최종 운영 기준으로 쓰면 안 된다.

`recall ≥ 0.70` 운영점은 전체 표본의 약 14.87%를 위험 표본으로 선택하면서 실제 산불의 70.06%를 포착한다. 더 보수적인 inner OOF threshold 방식에서는 selected rate가 16.16%, precision이 0.4060으로 약간 낮아지지만 recall은 0.7199로 목표를 넘는다. 이 값이 실제 보고서에서 가장 설명하기 쉬운 운영점이다.

`recall ≥ 0.90` 이상으로 올리면 FN은 크게 줄지만 selected rate가 32.43% 이상으로 증가한다. 즉 산불 누락을 더 줄일 수는 있지만 점검 대상이 크게 늘어난다. 이 결과는 모델이 하나의 정답 분류기가 아니라 위험도 기반 screening 도구라는 점을 보여준다.

#### 최고 조합의 top-risk 포착 성능

| 상위 위험구간 | 선택 표본 수 | 산불 포착 수 | capture rate | precision | lift |
| ---: | ---: | ---: | ---: | ---: | ---: |
| top 5% | 853 | 695 / 1,553 | 0.4475 | 0.8148 | 8.94 |
| top 10% | 1,705 | 932 / 1,553 | 0.6001 | 0.5466 | 6.00 |
| top 20% | 3,409 | 1,209 / 1,553 | 0.7785 | 0.3546 | 3.89 |
| top 30% | 5,114 | 1,361 / 1,553 | 0.8764 | 0.2661 | 2.92 |

이 표는 threshold를 하나로 정하지 않고 위험도 순위 자체가 유용한지 확인하는 결과다. top 5%만 보아도 전체 산불의 44.8%를 포착하고, top 10%에서 60.0%, top 20%에서 77.8%를 포착한다. 이는 모델 점수가 실제 산불 위험을 높은 순서로 정렬하는 데 의미가 있음을 보여준다.

운영 관점에서는 두 가지 방식이 가능하다. 하나는 `recall ≥ 0.70`처럼 목표 recall을 정하고 threshold를 선택하는 방식이고, 다른 하나는 하루 또는 지역별 점검 가능량에 맞춰 top 10%·20%를 우선 점검하는 방식이다. 보고서에는 두 방식을 모두 제시하면 threshold 선택의 임의성을 줄일 수 있다.

#### 최고 조합의 subgroup 결과

| subgroup | AUPRC | AUROC | 해석 |
| --- | ---: | ---: | --- |
| `Target_1 vs Target_0A` | 0.6760 | 0.8494 | 가장 어려운 hard-negative 비교 |
| `Target_1 vs Target_0B1` | 0.9424 | 0.9704 | 상대적으로 쉬운 비발생군 |
| `Target_1 vs Target_0B2` | 0.9666 | 0.9769 | 가장 쉬운 배경 비발생군 |
| 고지·산간형 | 0.6106 | 0.8924 | 중간 수준 |
| 영동 해안형 | 0.7728 | 0.9382 | 가장 강함 |
| 영서 내륙형 | 0.5930 | 0.8941 | 가장 약함 |

이 표는 전체 성능이 어디에서 강하고 어디에서 약한지 보여준다. `Target_0B1`, `Target_0B2`는 AUPRC가 0.94 이상으로 매우 높지만, 이 둘은 상대적으로 쉬운 비발생군이다. 반면 `Target_0A`는 실제 산불과 시간·공간 조건이 더 비슷한 hard-negative에 가까우므로 AUPRC 0.6760이 더 중요한 검증 결과다.

기후지형유형별로는 영동 해안형 성능이 가장 높고 영서 내륙형이 가장 낮다. 이는 모델이 모든 권역에서 동일하게 강한 것은 아니라는 뜻이다. Step 2 이후 최종 후보를 고를 때 전체 AUPRC가 높더라도 특정 권역 성능이 크게 약해지면 우선순위를 낮춰야 한다.

#### 피처 축별 효과

동일한 모델·다른 축 조건을 맞춰 pairwise로 비교한 AUPRC 변화는 다음과 같다.

| 비교 축 | 평균 AUPRC 변화 | 중앙값 변화 | 좋아진 pair 수 | 해석 |
| --- | ---: | ---: | ---: | --- |
| `CANADA_CORE - CANADA_NONE` | +0.0194 | +0.0161 | 48 / 48 | 캐나다지수 핵심만 넣어도 일관된 개선 |
| `CANADA_LONG - CANADA_NONE` | +0.0294 | +0.0269 | 48 / 48 | 장기 건조지수도 예측정보 있음 |
| `CANADA_ALL - CANADA_NONE` | +0.0402 | +0.0370 | 48 / 48 | 가장 강하고 일관된 개선 |
| `LC_USED - LC_NONE` | +0.0164 | +0.0207 | 80 / 96 | 토지피복은 대체로 도움 |
| `WS_ALL - WS_CORE` | +0.0057 | +0.0037 | 72 / 96 | 전체 날씨·공간 변수의 추가효과는 작음 |
| `balanced - none` | -0.0106 | -0.0090 | 32 / 96 | class weight는 평균적으로 불리하지만 일부 모델에는 도움 |

캐나다지수는 이번 Step 1에서 가장 일관적인 추가효과를 보였다. 특히 `CANADA_ALL`은 모든 pair에서 `CANADA_NONE`보다 AUPRC가 높았다. 따라서 캐나다지수를 제외하는 방향은 현재 결과로는 우선순위가 낮다.

토지피복도 평균적으로 도움이 되지만 모든 경우에 항상 좋은 것은 아니다. 반면 `WS_ALL`은 평균 개선폭이 작고 최고 조합도 `WS_CORE`를 사용하므로, 약한 보조 날씨·공간 변수를 반드시 많이 넣을 필요는 없다.

class weight의 경우 평균적으로 `balanced`가 불리했다. 그러나 최고 조합은 `random_forest + balanced`였으므로, 불균형 옵션은 모델별로 판단한다.

이 표는 Step 2 피처셋 범위를 줄이는 근거로 사용한다. `CANADA_ALL`의 평균 개선폭이 가장 크고 모든 pair에서 개선됐으므로, Step 2의 주 튜닝 후보는 `CANADA_ALL` 중심으로 잡는다. 반대로 `WS_ALL`의 평균 개선폭은 작기 때문에 모든 후보를 `WS_ALL`로 확장할 필요는 없다. `WS_CORE`와 `WS_ALL`은 상위 후보 일부에서만 비교한다.

토지피복은 평균적으로 도움이 되지만 `extra_trees`의 최고 조합처럼 `LC_NONE`이 더 좋은 경우도 있다. 따라서 Step 2에서 토지피복은 무조건 고정하지 않고, 상위 조합에서 이미 좋았던 형태를 유지한다.

#### Step 2 후보

Step 2 하이퍼파라미터 튜닝 후보는 두 갈래로 나눈다.

1. 주 튜닝 후보: Step 1에서 실제 성능이 높았던 `모델 × 피처셋 × 불균형 설정`
2. 모델별 구제 튜닝 후보: 기본 preset에서는 낮았지만 튜닝 민감도가 큰 모델의 최고 조합

전체 192개 조합을 모두 튜닝하지는 않는다. 계산량이 지나치게 크고, Step 1에서 낮았던 피처셋까지 모두 튜닝하면 선택 과정이 과도하게 복잡해진다. 반대로 상위 3~5개 조합만 튜닝하면 XGBoost나 CatBoost처럼 기본 preset에 불리한 모델을 너무 빨리 탈락시킬 수 있다. 따라서 주 튜닝과 구제 튜닝을 분리한다.

주 튜닝 후보는 다음을 우선한다.

| 우선순위 | 후보 | 이유 |
| ---: | --- | --- |
| 1 | `random_forest + WS_CORE__CANADA_ALL__LC_USED + balanced` | Step 1 전체 1위, fold 안정성 양호 |
| 2 | `random_forest + WS_ALL__CANADA_ALL__LC_USED + balanced` | 1위와 AUPRC 차이 0.0012로 거의 동일, WS_ALL 민감도 확인 |
| 3 | `lightgbm + WS_ALL__CANADA_ALL__LC_USED + none` | AUPRC 3위, AUROC·Brier·log loss가 가장 좋음 |
| 4 | `extra_trees + WS_ALL__CANADA_ALL__LC_NONE + balanced` | bagging 계열 대안, 토지피복 없이도 강함 |
| 5 | `hist_gradient_boosting + WS_ALL__CANADA_ALL__LC_USED + none` | 성능은 다소 낮지만 sklearn boosting 기준선으로 유지 가능 |

구제 튜닝 후보는 다음을 별도로 둔다.

| 후보 | Step 1 최고 AUPRC | 구제 튜닝 이유 |
| --- | ---: | --- |
| `xgboost + WS_ALL__CANADA_ALL__LC_USED + none` | 0.5416 | boosting 계열이며 하이퍼파라미터 민감도가 큼 |
| `catboost + WS_ALL__CANADA_ALL__LC_USED + none` | 0.5606 | 기본 preset 성능은 낮지만 범주형·불균형 설정 튜닝 여지 확인 |

구제 튜닝은 주 튜닝보다 짧게 수행한다. 목적은 최종 후보를 강제로 늘리는 것이 아니라, 기본 preset 때문에 불리했던 모델인지 확인하는 것이다. 구제 튜닝 후에도 AUPRC, recall 70% precision, Target_0A 성능이 상위 후보와 큰 차이를 보이면 Step 3 앙상블 후보에서는 제외한다.

#### Step 1 결론

Step 1 기준으로는 `random_forest + WS_CORE__CANADA_ALL__LC_USED + balanced`가 가장 강한 단일 모델 후보이다. 다만 LightGBM은 확률 품질이 가장 좋으므로 최종 후보에서 제외하지 않는다.

피처셋 측면에서는 캐나다 산불지수 전체(`CANADA_ALL`)의 추가효과가 가장 명확하다. 토지피복은 대체로 도움이 되며, 전체 날씨·공간 변수(`WS_ALL`)는 추가효과가 작다. 따라서 Step 2에서는 `CANADA_ALL` 중심으로 튜닝하되, `WS_CORE`와 `WS_ALL`, `LC_USED`와 `LC_NONE`의 차이를 일부 후보에서 유지해 민감도를 확인한다.

---

## Step 2 단일 모델 하이퍼파라미터 튜닝

Step 2에서는 Step 1의 모든 조합을 튜닝하지 않는다. 대신 `주 튜닝 후보`와 `모델별 구제 튜닝 후보`를 분리한다.

### Step 2-1. 튜닝 대상

#### 주 튜닝 후보

주 튜닝 후보는 최종 모델 후보가 될 가능성이 높은 조합이다. Optuna trial 수를 상대적으로 많이 배정한다.

| candidate_id | 모델 | feature_set | imbalance | 역할 |
| --- | --- | --- | --- | --- |
| `TUNE_RF_CORE_ALL_LC_BAL` | `random_forest` | `WS_CORE__CANADA_ALL__LC_USED` | balanced | Step 1 전체 1위 |
| `TUNE_RF_ALL_ALL_LC_BAL` | `random_forest` | `WS_ALL__CANADA_ALL__LC_USED` | balanced | 1위와 거의 동일한 WS_ALL 후보 |
| `TUNE_LGBM_ALL_ALL_LC_NONE` | `lightgbm` | `WS_ALL__CANADA_ALL__LC_USED` | none | 확률 품질 최상위 후보 |
| `TUNE_ET_ALL_ALL_NO_LC_BAL` | `extra_trees` | `WS_ALL__CANADA_ALL__LC_NONE` | balanced | bagging 대안 후보 |
| `TUNE_HGB_ALL_ALL_LC_NONE` | `hist_gradient_boosting` | `WS_ALL__CANADA_ALL__LC_USED` | none | sklearn boosting 기준 후보 |

#### 구제 튜닝 후보

구제 튜닝 후보는 Step 1 기본 preset 성능은 낮았지만 튜닝으로 개선될 가능성을 한 번 확인하기 위한 조합이다. trial 수를 줄이고, 개선폭이 작으면 Step 3에서 제외한다.

| candidate_id | 모델 | feature_set | imbalance | 목적 |
| --- | --- | --- | --- | --- |
| `RESCUE_XGB_ALL_ALL_LC_NONE` | `xgboost` | `WS_ALL__CANADA_ALL__LC_USED` | none | XGBoost가 기본 preset 때문에 낮았는지 확인 |
| `RESCUE_CAT_ALL_ALL_LC_NONE` | `catboost` | `WS_ALL__CANADA_ALL__LC_USED` | none | CatBoost 튜닝 민감도 확인 |

### Step 2-2. 튜닝 방식

- 튜닝 방법: Optuna
- split: Step 1과 동일한 outer 5-fold 유지
- 튜닝 데이터: 각 outer-train 내부 데이터
- inner CV: 각 outer-train 내부 strict group 4-fold
- 주 objective: inner OOF AUPRC 최대화
- 보조 지표:
  - `recall_ge_0.70`에서 precision
  - `Target_1 vs Target_0A` AUPRC
  - fold별 AUPRC 표준편차
  - Brier score와 log loss
- outer-validation: 하이퍼파라미터 선택에 사용하지 않음
- lockbox: Step 2에서 사용 금지

Step 2의 튜닝된 모델은 Step 1의 기본 설정 모델과 동일한 outer CV에서 다시 평가한다. 튜닝 후 성능이 실제로 개선됐는지 비교하고, 개선되지 않은 경우 기본 설정 모델을 유지한다.

### Step 2-3. 주 튜닝과 구제 튜닝의 차이

| 구분 | 대상 | trial 수 | 판단 기준 |
| --- | --- | ---: | --- |
| 주 튜닝 | Step 1 상위권 조합 | 많게 | 최종 후보 선발 |
| 구제 튜닝 | XGBoost, CatBoost 최고 조합 | 적게 | 튜닝 후에도 경쟁 가능한지 확인 |

구제 튜닝 모델이 Step 2 후에도 다음 조건을 만족하지 못하면 후속 앙상블 후보에서 제외한다.

- AUPRC가 주 후보와 큰 차이를 보이지 않을 것
- `recall_ge_0.70` precision이 지나치게 낮지 않을 것
- `Target_0A` 성능이 개선될 것
- fold별 성능 편차가 과도하지 않을 것

### Step 2-4. 저장할 결과

Step 2 결과는 `jsw/Analysis/new_machine_learning/outputs/step2_tuned_single_models/`에 저장한다.

저장 파일은 다음을 포함한다.

- `optuna_trials__{candidate_id}.csv`
- `selected_params__{candidate_id}.json`
- `oof__{candidate_id}.csv`
- `summary__step2_tuned_candidates.csv`
- `fold_metrics__step2_tuned_candidates.csv`
- `threshold_metrics__step2_tuned_candidates.csv`
- `subgroup_metrics__step2_tuned_candidates.csv`
- `top_risk_metrics__step2_tuned_candidates.csv`
- `validation_checks__step2_tuned_candidates.csv`

Step 2 완료 후에는 Step 1 기본 preset과 Step 2 tuned 모델을 같은 표에서 비교한다. 튜닝된 모델이 Step 1보다 개선되지 않으면 tuned 모델을 채택하지 않고 Step 1 기본 모델을 유지한다.

### Step 2-5. 실행 중 계산량 점검과 중간 판단

2026-06-22 실행 중 확인한 Step 2의 실제 계산량은 예상보다 크다. 현재 스크립트의 기본 설정은 주 튜닝 후보에 대해 `outer 5-fold`, 각 outer-train 내부 `inner 4-fold`, 각 outer fold별 `Optuna 40 trials`를 사용한다.

따라서 주 튜닝 후보 1개는 다음과 같은 학습량을 가진다.

```text
5 outer folds × 40 trials × 4 inner folds = 800회 inner 학습
800회 inner 학습 + 5회 outer 최종 학습 = 후보 1개당 약 805회 fit
```

구제 튜닝 후보는 `15 trials`를 사용하므로 다음과 같다.

```text
5 outer folds × 15 trials × 4 inner folds = 300회 inner 학습
300회 inner 학습 + 5회 outer 최종 학습 = 후보 1개당 약 305회 fit
```

현재 후보 7개를 모두 기본 설정으로 끝까지 실행하면 전체 fit 수는 대략 다음 규모다.

```text
주 튜닝 후보 5개 × 805회 = 4,025회
구제 튜닝 후보 2개 × 305회 = 610회
전체 약 4,635회 fit
```

이 방식은 하이퍼파라미터 선택 편향을 줄이고 outer OOF 성능을 정직하게 비교한다는 장점이 있다. 최종 단일 모델을 엄밀하게 비교해야 할 때는 좋은 구조다. 그러나 이후 앙상블까지 진행할 계획이라면, 모든 후보를 동일한 강도의 full nested tuning으로 끝까지 밀어붙이는 것은 탐색 단계에서는 비효율적일 수 있다.

2026-06-22 15:17 실행에서는 `TUNE_RF_CORE_ALL_LC_BAL`이 먼저 완료되었다.

| candidate_id | 상태 | 메모 |
| --- | --- | --- |
| `TUNE_RF_CORE_ALL_LC_BAL` | 완료 | 5개 outer fold 완료, elapsed `00:31:37`, `status=OK` |
| `TUNE_RF_ALL_ALL_LC_BAL` | 진행 중 | 같은 RandomForest 계열이며 feature 수가 더 많아 fold당 시간이 더 길 수 있음 |

`TUNE_RF_CORE_ALL_LC_BAL`은 feature 40개를 사용하고, `TUNE_RF_ALL_ALL_LC_BAL`은 feature 58개를 사용한다. 두 후보는 모두 RandomForest이며 피처셋만 다르므로, 앙상블 관점에서는 예측 패턴이 매우 비슷할 가능성이 있다. 따라서 두 RandomForest 후보를 모두 full tuning으로 길게 유지하는 것은 단일 모델 민감도 확인에는 의미가 있지만, 앙상블 base learner 다양성 확보 관점에서는 우선순위가 낮다.

실행 중간에 후보를 끊으면 해당 후보의 `oof__{candidate_id}.csv`, `selected_params__{candidate_id}.json`, 통합 summary가 완성 저장되지 않을 수 있다. 따라서 이미 진행 중인 후보는 가능하면 다음 로그가 뜬 직후 중지하는 것이 가장 깔끔하다.

```text
CANDIDATE DONE TUNE_RF_ALL_ALL_LC_BAL | elapsed=... | status=OK
```

이후에는 남은 후보를 모두 같은 강도로 계속 실행하기보다, Step 2의 목적을 `최고 단일 모델 확인`과 `앙상블 base learner 후보 확보`로 분리해 실행 강도를 조정한다.

#### 2026-06-22 중단 이후 재개 방법

`TUNE_RF_CORE_ALL_LC_BAL`, `TUNE_RF_ALL_ALL_LC_BAL`, `TUNE_LGBM_ALL_ALL_LC_NONE`은 각각 40 trials와 outer 5-fold를 완료했고 OOF, Optuna trial, outer fold별 selected parameter 파일이 저장되었다. 다음 후보인 ExtraTrees는 outer fold 실행 도중 중지했으므로 완료 산출물로 취급하지 않고 처음부터 축소 설정으로 다시 실행한다.

기존 `step2_tuned_single_models.py`를 별도 스크립트로 복제하지 않고 `--resume-existing` 기능을 추가해 이어서 실행한다. 이 옵션은 기존 통합 CSV를 먼저 읽고, OOF·Optuna trials·selected parameter와 validation이 완전한 후보는 자동으로 건너뛰며, 새 후보 결과만 통합표에 추가 또는 교체한다.

남은 후보의 재개 명령은 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step2_tuned_single_models.py' `
  --resume-existing `
  --candidate-ids TUNE_ET_ALL_ALL_NO_LC_BAL TUNE_HGB_ALL_ALL_LC_NONE RESCUE_XGB_ALL_ALL_LC_NONE RESCUE_CAT_ALL_ALL_LC_NONE `
  --primary-trials 20 `
  --rescue-trials 10
```

이 실행에서 ExtraTrees와 HistGradientBoosting은 outer fold별 20 trials, XGBoost와 CatBoost는 outer fold별 10 trials를 사용한다. outer 5-fold와 inner 4-fold는 기존 Step 2와 동일하게 유지하므로 OOF 비교 틀은 바뀌지 않는다. 완료된 RF와 LGBM은 다시 학습하지 않는다.

### Step 2-6. 앙상블 전제의 Step 2 수정 방향

이후 단계에서 여러 모델을 결합하는 앙상블을 진행한다면 Step 2는 단순히 AUPRC 1위 단일 모델을 찾는 단계가 아니다. Step 2는 다음 두 목적을 동시에 가져야 한다.

1. 최고 성능 단일 모델 후보를 확인한다.
2. 앙상블에 넣을 서로 다른 base learner 후보의 OOF 예측을 확보한다.

앙상블에서는 단일 모델 AUPRC가 조금 낮더라도 다른 모델이 놓친 산불을 잡거나, top-risk 구간이 다르게 형성되는 모델이 유용할 수 있다. 따라서 Step 2의 후보 판단 기준에는 단일 성능뿐 아니라 예측 다양성도 포함해야 한다.

#### 실행 강도 조정안

현재 완료 또는 진행 중인 RandomForest 후보는 다음처럼 처리한다.

| 후보 | 처리 방향 |
| --- | --- |
| `TUNE_RF_CORE_ALL_LC_BAL` | 이미 완료된 full tuning 결과를 유지한다. |
| `TUNE_RF_ALL_ALL_LC_BAL` | 이미 진행 중이면 후보 완료 직후 결과를 유지하고 중지한다. 새로 다시 시작한다면 trial 수를 줄이거나 제외한다. |

남은 후보는 full tuning보다 가벼운 설정으로 먼저 실행한다.

| 후보군 | 권장 방향 |
| --- | --- |
| `TUNE_LGBM_ALL_ALL_LC_NONE` | 확률 품질이 좋으므로 중간 이상 강도로 튜닝한다. |
| `TUNE_ET_ALL_ALL_NO_LC_BAL` | bagging 대안으로 유지하되 RandomForest와 중복성이 큰지 확인한다. |
| `TUNE_HGB_ALL_ALL_LC_NONE` | boosting 계열 기준선으로 유지한다. |
| `RESCUE_XGB_ALL_ALL_LC_NONE` | 짧은 구제 튜닝으로 개선 가능성만 확인한다. |
| `RESCUE_CAT_ALL_ALL_LC_NONE` | 짧은 구제 튜닝으로 개선 가능성만 확인한다. |

예시 실행 강도는 다음과 같다.

```text
주 후보: 15~25 trials
구제 후보: 8~10 trials
inner folds: 기본 4 유지 또는 탐색용 3으로 축소 검토
outer folds: Step 1과 비교 가능성을 위해 5 유지
```

단, 최종 보고서에서 단일 모델 우승 후보를 엄밀하게 주장해야 한다면 최종 1~2개 후보는 다시 `outer 5 × inner 4` 조건으로 재실행할 수 있다. 즉 가벼운 Step 2는 후보 압축용이고, 최종 단일 모델 확정용 full nested tuning은 제한된 후보에만 적용한다.

#### 앙상블용 추가 산출물

Step 2 이후 앙상블을 바로 진행하려면 후보별 OOF 파일만으로는 불편하다. 다음 형태의 통합 OOF matrix를 추가로 저장하는 것이 좋다.

```text
oof_matrix__step2_base_candidates.csv
```

필수 열은 다음과 같다.

| 열 | 설명 |
| --- | --- |
| `sample_id` | 표본 식별자 |
| `y_true` | 실제 Target |
| `outer_fold` | OOF fold |
| `sample_type` | subgroup 평가용 표본유형 |
| `climate_type` | 기후지형유형 |
| `pred_{candidate_id}` | 각 후보의 OOF 예측확률 |

또한 앙상블 후보 선정을 위해 다음 진단 표를 추가한다.

| 산출물 | 목적 |
| --- | --- |
| `diversity_correlation__step2_candidates.csv` | 후보 간 OOF 예측 상관계수 확인 |
| `diversity_top_risk_overlap__step2_candidates.csv` | top 5%, 10%, 20% 위험군 overlap 확인 |
| `diversity_unique_capture__step2_candidates.csv` | 특정 후보만 포착한 Target 1 수 확인 |
| `ensemble_base_candidate_selection.csv` | 단일 성능과 다양성을 함께 고려한 앙상블 투입 후보 기록 |

#### Step 2의 threshold 해석

Step 2에서 계산하는 threshold 지표는 단일 후보의 진단용으로 유지한다. 그러나 최종 운영 threshold는 Step 2 단일 모델 기준으로 확정하지 않는다. 앙상블을 진행하면 최종 score의 분포가 단일 모델 score와 달라지므로, 최종 threshold는 앙상블 score가 확정된 뒤 development OOF에서 다시 정한다.

따라서 Step 2 threshold는 다음처럼 해석한다.

```text
Step 2 threshold = 단일 후보 진단용 참고 지표
앙상블 이후 threshold = 최종 운영 기준 후보
lockbox threshold = development에서 고정한 threshold를 그대로 적용한 최종 확인 결과
```

#### Step 2 후보 선택 기준 수정

Step 2 이후 앙상블 base learner 후보는 다음 조건을 함께 보고 선별한다.

- OOF AUPRC가 최고 후보 대비 지나치게 낮지 않을 것
- `Target_1 vs Target_0A` 성능이 무너지지 않을 것
- fold별 AUPRC 편차가 과도하지 않을 것
- top-risk capture가 의미 있을 것
- 기존 상위 후보와 예측 상관이 너무 높지 않거나, unique capture가 있을 것
- `recall_ge_0.70` 운영점에서 FN 패턴이 다른 후보와 보완적일 것

결론적으로 Step 2는 `단일 모델 챔피언 선발전`이 아니라 `최고 단일 모델 후보와 앙상블 base learner 후보를 동시에 만드는 단계`로 운영한다. full nested tuning은 최종 후보에 제한적으로 사용하고, 넓은 후보군은 더 가볍게 돌려 OOF 예측과 다양성 진단을 먼저 확보한다.

### Step 2-7. 실행 결과

#### 실행 정보

- 실행 스크립트: `jsw/Analysis/new_machine_learning/step2_tuned_single_models.py`
- 진단 스크립트: `jsw/Analysis/new_machine_learning/step2_result_diagnostics.py`
- 입력 데이터: `data/학습데이터/최종_머신러닝_학습데이터.csv`
- 결과 폴더: `jsw/Analysis/new_machine_learning/outputs/step2_tuned_single_models/`
- 최초 실행 시작: 2026-06-22 15:17:24
- 축소 설정 재개: 2026-06-22 17:02:53
- 최종 결과 저장: 2026-06-22 17:31:33
- 전체 행 수: 17,045
- Target 1: 1,553
- Target 0: 15,492
- 양성률: 9.11%
- outer CV: `모델링_그룹ID` 기준 strict group 5-fold
- inner CV: 각 outer-train 내부 strict group 4-fold
- 완료 후보 수: 7개
- 성공 후보 수: 7개
- 실패 후보 수: 0개

처음 실행한 RandomForest 2개와 LightGBM은 outer fold별 40 trials를 사용했다. 계산량 점검 후 ExtraTrees와 HistGradientBoosting은 20 trials, XGBoost와 CatBoost는 10 trials로 축소해 재개했다.

실제 탐색량은 다음과 같다.

```text
RF 2개 + LGBM: 3 × (5 outer × 40 trials × 4 inner + 5 outer fit) = 2,415 fits
ET + HGB: 2 × (5 outer × 20 trials × 4 inner + 5 outer fit) = 810 fits
XGB + CatBoost: 2 × (5 outer × 10 trials × 4 inner + 5 outer fit) = 410 fits
전체 약 3,635 fits
```

후보별 학습시간의 합은 약 2시간 4분 9초다. 최초 실행부터 최종 저장까지의 실제 시간 구간은 중단과 재개 사이의 대기시간을 포함해 약 2시간 14분이다.

#### 실행 성공 여부와 검증

| 항목 | 결과 |
| --- | ---: |
| 전체 후보 | 7 |
| 성공 후보 | 7 |
| 실패 후보 | 0 |
| 생성된 후보별 OOF 파일 | 7 |
| 생성된 selected parameter 파일 | 7 |
| OOF matrix 행 수 | 17,045 |
| outer group leakage | 0 |
| OOF 예측 누락 | 0 |
| OOF 예측 중복 | 0 |
| 예측확률 NaN | 0 |
| 예측확률 inf | 0 |
| Target 1 없는 fold | 0 |

7개 후보의 OOF를 `sample_id` 기준으로 결합한 `oof_matrix__step2_base_candidates.csv`도 17,045행으로 완성되었다. 중복 sample_id와 결측 예측은 모두 0건이다. 따라서 Step 2 후보들은 동일한 outer OOF 표본 기준으로 비교할 수 있다.

단, 후보별 Optuna 탐색량은 40, 20, 10 trials로 다르다. 각 후보의 OOF 성능 자체는 유효하지만, 모든 알고리즘에 동일한 탐색 기회를 부여한 완전 대칭 실험은 아니다. 특히 XGBoost와 CatBoost는 짧은 구제 튜닝 결과라는 점을 유지한다.

#### 튜닝된 단일 모델 성능 순위

| 순위 | candidate | trials/fold | AUPRC | AUROC | Brier | log loss | fold AUPRC std | 시간 |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | `TUNE_LGBM_ALL_ALL_LC_NONE` | 40 | 0.6932 | 0.9121 | 0.0491 | 0.2443 | 0.0137 | 14:36 |
| 2 | `TUNE_RF_ALL_ALL_LC_BAL` | 40 | 0.6618 | 0.9045 | 0.0561 | 0.2063 | 0.0116 | 49:17 |
| 3 | `TUNE_RF_CORE_ALL_LC_BAL` | 40 | 0.6606 | 0.9068 | 0.0546 | 0.1971 | 0.0123 | 31:37 |
| 4 | `RESCUE_XGB_ALL_ALL_LC_NONE` | 10 | 0.6606 | 0.9085 | 0.0496 | 0.1771 | 0.0122 | 02:47 |
| 5 | `RESCUE_CAT_ALL_ALL_LC_NONE` | 10 | 0.6560 | 0.9068 | 0.0504 | 0.1795 | 0.0250 | 10:14 |
| 6 | `TUNE_HGB_ALL_ALL_LC_NONE` | 20 | 0.6480 | 0.9098 | 0.0508 | 0.1791 | 0.0111 | 06:46 |
| 7 | `TUNE_ET_ALL_ALL_NO_LC_BAL` | 20 | 0.6436 | 0.8904 | 0.0599 | 0.2245 | 0.0149 | 08:52 |

Step 2의 단일 모델 1위는 LightGBM이다. AUPRC 0.6932로 2위 RandomForest의 0.6618보다 0.0314 높다. AUROC도 0.9121로 가장 높고, Brier score도 0.0491로 가장 낮다. 따라서 순위화와 전체적인 제곱오차 기준에서는 LightGBM이 명확한 우승 후보다.

2위부터 4위까지는 차이가 매우 작다. RF_ALL 0.6618, RF_CORE 0.6606, XGBoost 0.6606이므로 이 구간에서는 AUPRC 소수점 차이만으로 우열을 강하게 주장하지 않는다. 이 후보들은 확률 품질, fold 안정성, hard-negative, 다른 모델과의 보완성을 함께 봐야 한다.

#### Step 1 기본 preset 대비 튜닝 효과

| candidate | Step1 AUPRC | Step2 AUPRC | 변화 | Brier 변화 | log loss 변화 | 판단 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `TUNE_LGBM_ALL_ALL_LC_NONE` | 0.6580 | 0.6932 | +0.0352 | -0.0009 | +0.0683 | 순위 성능 크게 개선, log loss 악화 |
| `RESCUE_XGB_ALL_ALL_LC_NONE` | 0.5416 | 0.6606 | +0.1190 | -0.0093 | -0.0279 | 구제 튜닝 성공 |
| `RESCUE_CAT_ALL_ALL_LC_NONE` | 0.5606 | 0.6560 | +0.0954 | -0.0074 | -0.0232 | 크게 개선됐지만 fold 불안정 |
| `TUNE_HGB_ALL_ALL_LC_NONE` | 0.6406 | 0.6480 | +0.0074 | -0.0007 | -0.0015 | 소폭 개선 |
| `TUNE_RF_ALL_ALL_LC_BAL` | 0.6663 | 0.6618 | -0.0045 | +0.0039 | +0.0112 | tuned 모델 미채택 |
| `TUNE_RF_CORE_ALL_LC_BAL` | 0.6675 | 0.6606 | -0.0069 | +0.0024 | +0.0028 | tuned 모델 미채택 |
| `TUNE_ET_ALL_ALL_NO_LC_BAL` | 0.6567 | 0.6436 | -0.0131 | +0.0015 | +0.0030 | tuned 모델 미채택 |

튜닝 효과는 알고리즘별로 달랐다. LightGBM, XGBoost, CatBoost, HistGradientBoosting 같은 boosting 계열은 AUPRC가 개선됐다. 특히 XGBoost와 CatBoost는 기본 preset의 낮은 성능이 알고리즘 자체의 한계라기보다 하이퍼파라미터 민감도 영향이 컸음을 보여준다.

반대로 RandomForest 2개와 ExtraTrees는 Step 1 기본 preset보다 낮아졌다. 따라서 Step 2 tuned 결과를 무조건 채택하지 않는다. RandomForest 대표는 Step 1의 `random_forest + WS_CORE__CANADA_ALL__LC_USED + balanced` AUPRC 0.6675를 유지하고, tuned RF는 하이퍼파라미터 민감도 참고 결과로만 남긴다. ExtraTrees도 단일 성능 기준으로는 Step 1 기본 preset을 유지하는 것이 맞다.

#### LightGBM의 확률값 주의사항

LightGBM은 AUPRC와 Brier score가 가장 좋지만 log loss는 0.2443으로 7개 후보 중 가장 나쁘다. Step 1 LightGBM의 log loss 0.1760보다도 크게 악화됐다.

OOF 확률 분포를 추가 점검한 결과는 다음과 같다.

| 항목 | LightGBM |
| --- | ---: |
| 실제 양성률 | 0.0911 |
| 평균 예측확률 | 0.0542 |
| 예측확률 중앙값 | 0.000203 |
| 90% 분위수 | 0.0542 |
| 99% 분위수 | 0.9993 |
| Target 1인데 확률 0.01 미만 | 366건 |
| Target 0인데 확률 0.99 초과 | 4건 |

즉 LightGBM은 많은 표본을 0에 매우 가깝게, 일부 표본을 1에 매우 가깝게 배치하는 공격적인 확률 분포를 만들었다. 이 구조는 위험 순위와 top-risk 포착에는 유리하지만, 확신을 가지고 틀린 표본을 크게 벌점 주는 log loss에는 불리하다.

따라서 현재 LightGBM의 `y_prob`를 그대로 실제 산불 발생확률로 해석하지 않는다. Step 2에서 확정할 수 있는 것은 LightGBM이 가장 강한 위험 순위 단일 모델이라는 점이다. 실제 확률 해석과 최종 threshold는 앙상블 확정 후 Step 5 calibration에서 다시 다룬다.

#### LightGBM threshold 운영점

| operating point | threshold | recall | precision | selected rate | TP | FP | FN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| fixed 0.50 | 0.500000 | 0.4572 | 0.8744 | 4.76% | 710 | 102 | 843 |
| best F1 | 0.135807 | 0.5621 | 0.7150 | 7.16% | 873 | 348 | 680 |
| best F2 | 0.019979 | 0.7199 | 0.4472 | 14.67% | 1,118 | 1,382 | 435 |
| recall ≥ 0.70 | 0.024070 | 0.7006 | 0.4628 | 13.79% | 1,088 | 1,263 | 465 |
| recall ≥ 0.90 | 0.001220 | 0.9002 | 0.2389 | 34.33% | 1,398 | 4,453 | 155 |
| recall ≥ 0.95 | 0.000300 | 0.9504 | 0.1875 | 46.17% | 1,476 | 6,394 | 77 |

기본 threshold 0.50에서는 precision이 0.8744로 높지만 산불 843건을 놓친다. 산불 누락을 줄이는 screening 목적에는 사용할 수 없다.

`recall ≥ 0.70` 운영점에서는 전체 표본의 13.79%를 선별해 산불의 70.06%를 포착한다. precision은 0.4628로 후보 중 가장 높고, 같은 recall을 달성하기 위한 selected rate도 가장 낮다.

`recall ≥ 0.90`으로 높이면 FN은 155건으로 줄지만 전체의 34.33%를 점검해야 한다. 이 운영점에서는 HistGradientBoosting이 precision 0.2469, selected rate 33.22%로 LightGBM보다 조금 효율적이다. 즉 LightGBM이 모든 recall 구간에서 항상 최적은 아니며, HGB가 높은 recall 구간에서 보완 가능성을 보인다.

#### 후보별 recall 운영 효율

모든 후보가 동일하게 1,088건의 Target 1을 포착하도록 맞춘 `recall ≥ 0.70` 운영점 비교다.

| 후보 | precision | selected rate | FP | 판단 |
| --- | ---: | ---: | ---: | --- |
| LightGBM | 0.4628 | 13.79% | 1,263 | 가장 효율적 |
| XGBoost | 0.4285 | 14.90% | 1,451 | 확률 품질이 좋은 2차 후보 |
| HGB | 0.4186 | 15.25% | 1,511 | 안정적 boosting 기준 |
| CatBoost | 0.4178 | 15.28% | 1,516 | 성능은 유사하나 fold 불안정 |
| RF_ALL tuned | 0.4070 | 15.68% | 1,585 | tuned RF 중 우세 |
| RF_CORE tuned | 0.3966 | 16.09% | 1,655 | Step1 기본 RF보다 우선할 근거 없음 |
| ExtraTrees | 0.3704 | 17.23% | 1,849 | 단일 운영 효율 최하위 |

이 표는 동일한 recall에서 얼마나 적은 경보로 산불을 포착하는지를 보여준다. LightGBM은 XGBoost보다 188개, ExtraTrees보다 586개 적은 false positive로 같은 수의 산불을 포착한다.

#### top-risk 포착 성능

| 후보 | top 5% capture | top 10% capture | top 20% capture | top 30% capture |
| --- | ---: | ---: | ---: | ---: |
| LightGBM | 46.94% | 62.40% | 78.56% | 87.51% |
| RF_ALL tuned | 44.49% | 60.33% | 75.72% | 86.99% |
| XGBoost | 43.92% | 60.08% | 75.92% | 87.12% |
| RF_CORE tuned | 44.88% | 59.18% | 76.56% | 87.19% |
| CatBoost | 44.11% | 59.63% | 77.08% | 86.48% |
| HGB | 42.63% | 59.05% | 77.27% | 87.57% |
| ExtraTrees | 45.07% | 58.27% | 73.34% | 83.97% |

LightGBM은 top 5%에서 729건, top 10%에서 969건, top 20%에서 1,220건의 산불을 포착해 좁은 고위험 구간에서 가장 강했다. top 10%의 precision은 0.5683이고 lift는 6.24다.

top 30%에서는 HGB가 1,360건, LightGBM이 1,359건으로 HGB가 1건 더 많이 포착했다. 차이는 매우 작지만, 높은 coverage 구간에서는 HGB의 위험 순위가 보완적으로 작동할 수 있음을 보여준다.

#### hard-negative와 기후지형유형 성능

`Target_1 vs Target_0A`는 실제 산불과 시간·공간 조건이 유사한 어려운 음성군이므로 전체 AUPRC보다 더 중요한 보조 검증이다.

| 후보 | Target_0A AUPRC | Target_0A AUROC |
| --- | ---: | ---: |
| LightGBM | 0.7041 | 0.8539 |
| RF_ALL tuned | 0.6709 | 0.8413 |
| RF_CORE tuned | 0.6702 | 0.8438 |
| ExtraTrees | 0.6699 | 0.8394 |
| XGBoost | 0.6691 | 0.8430 |
| CatBoost | 0.6640 | 0.8388 |
| HGB | 0.6556 | 0.8401 |

LightGBM은 hard-negative 비교에서도 가장 높다. 전체 성능 향상이 쉬운 `Target_0B1`, `Target_0B2`만 잘 구분해서 생긴 결과가 아니라, 가장 어려운 `Target_0A`에서도 개선이 유지된다.

LightGBM의 기후지형유형별 결과는 다음과 같다.

| 기후지형유형 | n | AUPRC | AUROC | Brier |
| --- | ---: | ---: | ---: | ---: |
| 영동 해안형 | 6,157 | 0.7818 | 0.9374 | 0.0395 |
| 고지·산간형 | 2,435 | 0.6417 | 0.8924 | 0.0524 |
| 영서 내륙형 | 8,453 | 0.6343 | 0.8976 | 0.0552 |

영동 해안형은 매우 강하지만 영서 내륙형과 고지·산간형은 AUPRC가 약 0.64 수준이다. 따라서 최종 모델은 전체 성능만 제시하지 않고 권역별 운영 성능과 selected rate를 계속 확인해야 한다.

#### fold 안정성

| 후보 | fold AUPRC 범위 | 표준편차 | 판단 |
| --- | --- | ---: | --- |
| HGB | 0.6320 ~ 0.6624 | 0.0111 | 가장 안정적 |
| RF_ALL tuned | 0.6639 ~ 0.6916 | 0.0116 | 안정적 |
| XGBoost | 0.6422 ~ 0.6739 | 0.0122 | 안정적 |
| RF_CORE tuned | 0.6622 ~ 0.6890 | 0.0123 | 안정적 |
| LightGBM | 0.6742 ~ 0.7091 | 0.0137 | 모든 fold에서 상위 성능 유지 |
| ExtraTrees | 0.6382 ~ 0.6781 | 0.0149 | 후반 fold에서 약화 |
| CatBoost | 0.6172 ~ 0.6845 | 0.0250 | 가장 불안정 |

LightGBM은 표준편차가 최저는 아니지만 가장 낮은 fold에서도 AUPRC 0.6742로 다른 후보의 전체 평균보다 높다. 따라서 특정 fold 하나가 전체 1위를 만든 결과는 아니다.

CatBoost는 fold 1에서 0.6172, fold 4에서 0.6845로 변동폭이 크다. 짧은 10 trials의 영향도 있을 수 있지만, 현재 결과만으로는 1차 앙상블 핵심 base로 쓰기 어렵다.

#### 선택된 하이퍼파라미터의 특징

exact parameter는 후보별 `selected_params__{candidate_id}.json`에 outer fold 단위로 저장되어 있다. 주요 패턴은 다음과 같다.

| 모델 | outer fold별 선택 패턴 |
| --- | --- |
| LightGBM | `n_estimators=400~500`, `learning_rate=0.071~0.105`, `num_leaves=55~89`, 정규화는 대체로 매우 약함 |
| XGBoost | `n_estimators=150~500`, `max_depth=5~8`, `learning_rate=0.032~0.088`, subsample과 column sampling 사용 |
| CatBoost | `iterations=100~500`, `depth=6~8`; fold별 iteration 차이가 큼 |
| HGB | `max_iter=80~360`, `learning_rate=0.029~0.081`, `max_leaf_nodes=50~63` |
| RandomForest | `max_depth=24` 또는 제한 없음, `bootstrap=false`, fold별 leaf/split 규제가 달라짐 |
| ExtraTrees | `n_estimators=280~480`, `bootstrap=false`, `max_features`와 leaf 규제가 fold별로 달라짐 |

LightGBM은 많은 트리와 약한 정규화를 선택해 AUPRC를 높였지만 확률이 0과 1에 몰리는 현상도 함께 나타났다. 이는 Step 5에서 calibration을 반드시 비교해야 하는 근거다.

#### 모델 간 OOF 다양성

모델 간 Spearman 순위상관의 주요 값은 다음과 같다.

| 후보 조합 | Spearman | 해석 |
| --- | ---: | --- |
| ExtraTrees - HGB | 0.6580 | 가장 다른 순위 패턴 |
| LightGBM - ExtraTrees | 0.7483 | 다양성이 큼 |
| LightGBM - RF_CORE tuned | 0.7970 | 중간 수준 보완성 |
| LightGBM - RF_ALL tuned | 0.8397 | 비교적 유사 |
| LightGBM - HGB | 0.8738 | 유사한 boosting 패턴 |
| LightGBM - CatBoost | 0.8749 | 유사한 boosting 패턴 |
| XGBoost - HGB | 0.9022 | 상당히 유사 |
| XGBoost - CatBoost | 0.9163 | 중복성이 큼 |
| RF_CORE tuned - RF_ALL tuned | 0.9193 | 두 RF를 함께 넣을 필요가 낮음 |
| LightGBM - XGBoost | 0.9197 | 순위는 매우 유사하지만 확률 품질 역할은 다름 |

두 RandomForest 후보는 Pearson 0.9509, Spearman 0.9193으로 거의 중복이다. 둘을 동시에 앙상블에 넣는 것보다 성능이 더 좋은 한 개만 사용하는 것이 적절하다.

ExtraTrees는 단일 성능이 낮지만 LightGBM과 Spearman 0.7483으로 가장 다른 후보 중 하나다. 따라서 ExtraTrees는 단일 우승 후보가 아니라 다양성 전용 예비 base로 의미가 있다.

#### LightGBM이 놓친 산불의 추가 포착

LightGBM과 다른 후보의 top 10% 위험군을 합쳤을 때 결과는 다음과 같다.

| 추가 후보 | 해당 후보 단독 포착 | LightGBM 단독 포착 | union 포착 | LightGBM 대비 추가 포착 |
| --- | ---: | ---: | ---: | ---: |
| RF_CORE tuned | 919 | 969 | 1,058 | +89 |
| RF_ALL tuned | 937 | 969 | 1,055 | +86 |
| ExtraTrees | 905 | 969 | 1,047 | +78 |
| HGB | 917 | 969 | 1,045 | +76 |
| CatBoost | 926 | 969 | 1,044 | +75 |
| XGBoost | 933 | 969 | 1,031 | +62 |

LightGBM 단독 top 10%는 전체 산불의 62.40%를 포착한다. RF_CORE tuned와의 단순 union은 1,058건, 즉 68.13%까지 올라간다. 이것이 곧바로 평균 앙상블 성능을 보장하는 것은 아니지만, LightGBM이 놓친 산불을 다른 알고리즘이 실제로 포착하고 있음을 보여준다.

동일한 `recall ≥ 0.70` threshold에서도 후보마다 포착한 Target 1이 완전히 같지 않았다. LightGBM이 놓친 산불 중 RF_CORE tuned가 130건, ExtraTrees가 121건, RF_ALL tuned가 107건, HGB가 103건, CatBoost가 97건, XGBoost가 85건을 각각 추가 포착했다. 따라서 Step 4에서 단일 AUPRC만으로 base를 고르면 이 보완성을 잃을 수 있다.

Step 1 기본 RF_CORE와 tuned LightGBM을 별도로 비교하면 Spearman은 0.8252였다. top 10%에서 기본 RF는 LightGBM이 놓친 산불 86건을 추가 포착했고, 두 후보의 union은 1,055건이었다. tuned RF보다 Step 1 기본 RF의 단일 성능이 높으므로 실제 앙상블에서는 Step 1 기본 RF를 우선한다.

#### Step 3·4로 넘길 후보

| 후보 | 결정 | 다음 단계 역할 |
| --- | --- | --- |
| tuned LightGBM | 선택 | 단일 모델 1위, Step 3 주 ablation 대상, Step 4 핵심 base |
| Step 1 기본 RF_CORE | 선택 | 가장 좋은 RandomForest 버전, bagging 대표 base |
| tuned XGBoost | 선택 | log loss 최저, 구제 튜닝 성공, 확률 품질 보조 base |
| tuned ExtraTrees | 예비 | 단일 성능은 낮지만 다양성 확인용 base |
| tuned HGB | 예비 | 높은 recall 운영점 보조, 안정성 기준 후보 |
| tuned CatBoost | 보류 | fold 불안정성과 XGBoost 중복성 때문에 1차 조합 제외 |
| tuned RF_CORE | 교체 | Step 1 기본 RF_CORE보다 낮아 tuned 버전 미채택 |
| tuned RF_ALL | 제외 | Step 1보다 낮고 RF_CORE와 중복 |

Step 3 피처 ablation은 모든 후보에 반복하지 않는다. 우선 LightGBM을 주 대상으로 하고, 알고리즘 계열 비교가 필요할 때 Step 1 기본 RF_CORE와 XGBoost 또는 ExtraTrees를 제한적으로 추가한다. Step 1 기본 RF를 ablation할 때는 Optuna parameter JSON이 아니라 Step 1의 고정 preset을 그대로 적용해 같은 outer split에서 다시 학습한다.

Step 4의 1차 앙상블 입력은 `LightGBM + Step1 RF_CORE + XGBoost`로 시작한다. 단순 평균과 기하평균에서 개선이 작으면 다양성이 큰 ExtraTrees를 추가한 조합을 비교한다. HGB는 recall 90% 운영점 또는 top 30% 포착 개선을 확인하는 예비 후보로 둔다.

#### Step 2 결론

Step 2의 최고 단일 위험 순위 모델은 `TUNE_LGBM_ALL_ALL_LC_NONE`이다. AUPRC 0.6932, Target_0A AUPRC 0.7041, top 10% 산불 포착률 62.40%, recall 70% selected rate 13.79%로 주요 기준에서 가장 강했다.

다만 LightGBM의 raw 확률은 극단적이고 log loss가 나쁘므로 아직 최종 산불 발생확률 모델로 확정하지 않는다. 현재 단계에서 채택하는 것은 LightGBM의 순위 성능이며, calibration과 최종 threshold는 앙상블 확정 후 다시 평가한다.

튜닝은 boosting 계열에 효과적이었고 RandomForest와 ExtraTrees에는 효과가 없었다. 따라서 최종 후보는 무조건 Step 2 tuned 버전만 쓰지 않고 Step 1과 Step 2 중 알고리즘별 최선의 버전을 사용한다.

다음 단계는 제한적 Step 3 피처 ablation이다. 이후 Step 4에서 OOF 예측을 조합해 단일 LightGBM보다 실제로 개선되는 앙상블이 있는지 확인한다. 앙상블 개선이 작거나 불안정하면 LightGBM 단일 모델을 최종 후보로 유지한다.

---

## Step 3 제한적 피처 ablation

원래 학습 계획에서 Step 3에 해당하는 단계는 앙상블이 아니라 피처 ablation이다. 피처 ablation은 최종 후보 모델에서 특정 변수 묶음을 제거하고 다시 OOF 성능을 비교해, 어떤 정보 축이 실제 예측 성능에 기여하는지 확인하는 실험이다.

다만 Step 2의 계산량이 이미 크기 때문에 Step 3를 모든 모델과 모든 후보에 대해 full nested tuning으로 반복하지 않는다. Step 3는 다음 목적에 맞게 제한적으로 수행한다.

1. 최종 후보가 특정 변수 묶음에 과하게 의존하는지 확인한다.
2. 캐나다지수, 토지피복, 기상, 지형·거리, 계절·시간 축의 실제 기여도를 보고서에 설명할 수 있게 한다.
3. 성능이 거의 유지되는 더 단순한 피처셋이 있는지 확인한다.
4. 앙상블에 넣을 후보들이 서로 다른 정보 축을 사용하는지 확인한다.

### Step 3-1. 실행 대상

Step 3는 Step 2 결과가 나온 뒤 다음 후보에만 수행한다.

| 대상 | 기준 |
| --- | --- |
| 단일 모델 1위 후보 | Step 2 OOF AUPRC, Target_0A, top-risk, fold 안정성을 종합해 선정 |
| 앙상블 base 대표 후보 | 서로 다른 알고리즘 계열에서 2~3개 이내 선정 |
| 필요 시 보조 후보 | 단일 성능은 낮아도 unique capture가 큰 후보 |

RandomForest 후보가 2개 모두 남더라도 둘 다 ablation하지 않는다. 둘의 예측이 비슷하면 `RF_CORE` 또는 `RF_ALL` 중 Step 2 결과가 더 좋은 하나만 ablation한다.

### Step 3-2. ablation 실험 목록

기본 ablation은 다음 변수 묶음 단위로 수행한다.

| ablation_id | 제거 또는 제한 내용 | 목적 |
| --- | --- | --- |
| `DROP_CANADA_ALL` | 캐나다 산불지수 전체 제거 | 캐나다지수의 총 기여도 확인 |
| `DROP_LANDCOVER` | 토지피복 변수 제거 | 토지피복이 실제 예측에 필요한지 확인 |
| `DROP_WEATHER` | 날씨 변수 제거 | 단기 기상 정보의 기여도 확인 |
| `DROP_TERRAIN_DISTANCE` | 지형·거리·접근성 변수 제거 | 공간 조건의 기여도 확인 |
| `DROP_SEASON_TIME` | 계절·월·시간 변수 제거 | 시간적 패턴 의존도 확인 |

캐나다지수는 내부 중복이 크므로, 단일 모델 1위 후보에 한해 다음 민감도도 확인한다.

| ablation_id | 내용 |
| --- | --- |
| `CANADA_FWI_ONLY` | FWI만 사용 |
| `CANADA_FFMC_FWI` | FFMC + FWI |
| `CANADA_CORE_EDA` | FFMC + ISI + FWI |
| `CANADA_LONG_TERM_SENSITIVITY` | DMC + DC + BUI |
| `CANADA_LEAVE_ONE_INDEX_OUT` | 캐나다지수 7개를 하나씩 제거 |

### Step 3-3. 학습 방식

Step 3는 피처셋이 바뀌므로 기존 Step 2 OOF 확률값만 조합해서 처리할 수 없다. 변수 묶음을 제거하면 모델 입력이 달라지므로, 해당 ablation 피처셋으로 OOF 예측을 새로 생성해야 한다.

하지만 하이퍼파라미터 튜닝까지 매번 새로 크게 돌리지는 않는다.

우선순위는 다음과 같다.

1. Step 2에서 선택된 후보의 outer fold별 best parameter를 재사용한다.
2. 같은 outer split에서 ablated feature set으로만 다시 학습해 OOF 예측을 만든다.
3. 기준 모델 대비 AUPRC, Target_0A, top-risk, recall 70% 운영점이 얼마나 변하는지 본다.
4. ablation 모델이 기준 모델보다 좋아지는 예외적 결과가 나오면, 그때만 제한적 Optuna 재튜닝을 검토한다.

여기서 재사용하는 best parameter는 Step 2 출력 파일인 `selected_params__{candidate_id}.json`의 `selected_params_by_outer_fold`에 저장된 값이다. Step 2는 fitted model 객체 자체를 저장하는 단계가 아니므로, Step 3에서 Step 2 모델을 그대로 불러와 변수만 제거하는 방식은 아니다. 각 ablation 실험은 같은 outer split을 유지하되, fold별로 저장된 Step 2 best parameter를 적용해 새 피처셋으로 pipeline을 다시 fit하고 validation fold의 OOF 확률을 다시 만든다.

예를 들어 `TUNE_RF_CORE_ALL_LC_BAL`의 fold 0 ablation은 `selected_params__TUNE_RF_CORE_ALL_LC_BAL.json` 안의 fold 0 parameter를 읽고, fold 0의 outer-train에서 ablated feature set으로 모델을 새로 학습한 뒤 fold 0 validation에 예측한다. 따라서 Step 3를 실행하려면 해당 base candidate의 Step 2 후보 실행이 `CANDIDATE DONE`까지 완료되어 `selected_params__*.json`과 `oof__*.csv`가 저장되어 있어야 한다.

즉 Step 3의 기본 목적은 “최적 성능 탐색”이 아니라 “정보 축의 기여도 확인”이다. 따라서 Step 2와 같은 full tuning을 반복하지 않는다.

### Step 3-4. 실행 코드와 예상시간

Step 3 실행 스크립트는 다음 파일로 구현한다.

```text
jsw/Analysis/new_machine_learning/step3_feature_ablation.py
```

기본 실행 대상은 Step 2 단일 모델 1위인 `TUNE_LGBM_ALL_ALL_LC_NONE`이다. 기본 실행에는 다음 16개 실험이 포함된다.

```text
핵심 변수군 제거 5개
+ 캐나다지수 축소 조합 4개
+ 캐나다지수 leave-one-out 7개
= 총 16개 ablation
```

각 실험은 outer 5-fold에서만 다시 학습하므로 기본 fit 수는 다음과 같다.

```text
16 experiments × 5 outer folds = 80 fits
```

Optuna inner CV는 실행하지 않는다. Step 2에서 저장된 LightGBM의 outer fold별 best parameter를 그대로 적용하고, 피처 목록만 바꿔 outer-train에서 다시 fit한다.

전체 실행 전 구성만 확인하는 명령은 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step3_feature_ablation.py' --check-config
```

기본 LightGBM 전체 ablation 실행 명령은 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step3_feature_ablation.py'
```

핵심 5개 변수군 제거만 먼저 실행하려면 다음처럼 제한할 수 있다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step3_feature_ablation.py' --ablation-ids core
```

2026-06-22 smoke test에서는 `DROP_CANADA_ALL × outer fold 0` 한 번의 LightGBM fit과 예측이 약 1.5초에 완료됐다. OOF 3,397행에 대해 예측 누락, sample_id 중복, NaN, inf, group leakage가 모두 0이었다.

현재 환경에서 기본 80 fits의 예상시간은 약 2~5분이며, CSV 집계와 시스템 부하를 포함하면 보수적으로 3~8분 정도로 본다. Step 2처럼 수십 분 이상 걸리는 구조는 아니다.

스크립트는 ablation별 OOF 파일이 완성되어 있으면 기본적으로 건너뛴다. 중간에 중지하더라도 완료된 ablation은 유지되며 같은 명령으로 재개할 수 있다. 기존 결과까지 다시 계산하려는 경우에만 `--overwrite`를 사용한다.

추가 base 모델은 필요할 때만 다음 후보 중 선택한다.

```text
STEP1_RF_CORE_ALL_LC_BAL
RESCUE_XGB_ALL_ALL_LC_NONE
TUNE_ET_ALL_ALL_NO_LC_BAL
TUNE_HGB_ALL_ALL_LC_NONE
```

예를 들어 LightGBM과 Step 1 기본 RandomForest에 핵심 ablation만 수행하려면 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step3_feature_ablation.py' `
  --base-candidate-ids TUNE_LGBM_ALL_ALL_LC_NONE STEP1_RF_CORE_ALL_LC_BAL `
  --ablation-ids core
```

다만 기본 원칙은 LightGBM 16개 실험을 먼저 완료하고, 결과 해석상 알고리즘 비교가 필요한 변수군만 다른 base 모델에 제한적으로 추가하는 것이다.

### Step 3-5. 저장할 결과

Step 3 결과는 다음 폴더에 저장한다.

```text
jsw/Analysis/new_machine_learning/outputs/step3_feature_ablation/
```

저장 파일은 다음을 기본으로 한다.

| 파일 | 내용 |
| --- | --- |
| `ablation_manifest.csv` | 실행한 기준 후보, ablation_id, 제거 변수 묶음, feature 수 |
| `oof__{base_candidate_id}__{ablation_id}.csv` | ablation별 OOF 예측 |
| `summary__step3_feature_ablation.csv` | 기준 모델 대비 성능 변화 |
| `threshold_metrics__step3_feature_ablation.csv` | 단일 후보 진단용 threshold 지표 |
| `top_risk_metrics__step3_feature_ablation.csv` | top-risk capture 변화 |
| `subgroup_metrics__step3_feature_ablation.csv` | Target_0A와 기후지형유형별 변화 |

Step 3 결과 해석에서는 다음을 우선한다.

- `DROP_CANADA_ALL`에서 성능이 크게 떨어지면 캐나다지수 기여도가 크다고 본다.
- `DROP_LANDCOVER`에서 성능 변화가 작으면 토지피복은 설명 보조 또는 특정 모델 보조로 해석한다.
- 특정 축을 제거했는데 성능이 좋아지면, 그 축은 과적합 또는 잡음 가능성이 있으므로 Step 4 앙상블 후보에서 제외 또는 축소 피처셋 후보로 검토한다.
- ablation 결과는 변수의 인과효과가 아니라 예측 기여도로 해석한다.

### Step 3-6. 실행 결과

#### 실행 정보

- 실행 시작: 2026-06-22 18:02:30
- 실행 종료: 2026-06-22 18:05:20
- 실제 소요시간: 약 2분 50초
- 기준 모델: `TUNE_LGBM_ALL_ALL_LC_NONE`
- 기준 피처 수: 58개
- ablation 실험 수: 16개
- outer fold: 5개
- 실제 fit 수: 80회
- Optuna 재튜닝: 없음
- 완료된 ablation OOF 파일: 16개

#### 실행 성공 여부와 검증

| 항목 | 결과 |
| --- | ---: |
| baseline 포함 평가 결과 | 17 |
| 완료된 ablation | 16 |
| 실패 ablation | 0 |
| 각 OOF 행 수 | 17,045 |
| OOF 예측 누락 | 0 |
| sample_id 중복 | 0 |
| 예측확률 NaN | 0 |
| 예측확률 inf | 0 |
| outer group leakage | 0 |
| Target 1 없는 fold | 0 |

모든 ablation은 Step 2와 동일한 outer split을 사용했고, 각 outer fold에서는 Step 2가 선택한 해당 fold의 LightGBM parameter를 그대로 적용했다. 따라서 차이는 Optuna 탐색 차이가 아니라 주로 입력 피처 구성 변화에서 발생한다.

단, Step 2 parameter는 58개 전체 피처 기준으로 선택된 값이다. 각 ablation에 최적인 parameter를 새로 찾은 것은 아니므로 이 결과는 “해당 피처축을 제거했을 때 기존 tuned LightGBM이 얼마나 영향을 받는가”를 보는 민감도 분석이다.

#### 핵심 변수군 제거 결과

| ablation | 남은 피처 | AUPRC | ΔAUPRC | AUROC | Brier | log loss | AUPRC 하락 fold |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `BASELINE` | 58 | 0.6932 | - | 0.9121 | 0.0491 | 0.2443 | - |
| `DROP_SEASON_TIME` | 54 | 0.6876 | -0.0056 | 0.9080 | 0.0492 | 0.2476 | 3 / 5 |
| `DROP_TERRAIN_DISTANCE` | 47 | 0.6774 | -0.0158 | 0.9053 | 0.0503 | 0.2466 | 5 / 5 |
| `DROP_LANDCOVER` | 48 | 0.6713 | -0.0219 | 0.8965 | 0.0506 | 0.2604 | 5 / 5 |
| `DROP_CANADA_ALL` | 51 | 0.6652 | -0.0280 | 0.8974 | 0.0509 | 0.2503 | 5 / 5 |
| `DROP_WEATHER` | 33 | 0.6627 | -0.0305 | 0.9114 | 0.0513 | 0.2219 | 5 / 5 |

AUPRC 기준 기여 순서는 `날씨`, `캐나다지수`, `토지피복`, `지형·거리`, `계절·시간` 순이다. 날씨, 캐나다지수, 토지피복, 지형·거리 제거는 모든 fold에서 AUPRC가 하락해 우연한 특정 fold 결과로 보기 어렵다.

계절·시간 변수는 제거 시 AUPRC가 0.0056 하락하지만 2개 fold에서는 거의 같거나 소폭 좋아졌다. 따라서 다른 축보다 기여가 작고 조건부 의존성이 높다. 그래도 기준 모델에서 제거할 정도의 명확한 이득은 없다.

#### 날씨 제거 결과의 상반된 의미

날씨 변수 25개를 제거하면 AUPRC는 0.6932에서 0.6627로 가장 크게 하락하고 top 5% 산불 포착 수도 729건에서 688건으로 줄었다. 특히 고지·산간형 AUPRC는 0.6417에서 0.5839, 영서 내륙형은 0.6343에서 0.5998로 하락했다.

반면 log loss는 0.2443에서 0.2219로 개선됐고, `recall ≥ 0.90` 운영점에서는 precision 0.2496, selected rate 32.86%로 baseline의 precision 0.2389, selected rate 34.33%보다 좋아졌다.

이 결과는 날씨 변수가 고위험 표본을 맨 위로 정렬하는 데는 중요하지만 LightGBM의 확률을 더 극단적으로 만드는 역할도 한다는 뜻이다. 날씨를 최종 모델에서 제거할 근거는 아니며, Step 5 calibration에서 raw 확률을 보정해야 한다는 근거로 사용한다.

#### 캐나다지수 축소 및 leave-one-out 결과

| ablation | 피처 수 | AUPRC | ΔAUPRC | Brier | log loss | fold std |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `CANADA_DROP_ISI` | 57 | 0.6966 | +0.0034 | 0.0488 | 0.2407 | 0.0092 |
| `CANADA_DROP_FWI` | 57 | 0.6962 | +0.0030 | 0.0491 | 0.2409 | 0.0158 |
| `CANADA_DROP_FFMC` | 57 | 0.6954 | +0.0022 | 0.0490 | 0.2426 | 0.0129 |
| `CANADA_DROP_DMC` | 57 | 0.6947 | +0.0015 | 0.0488 | 0.2425 | 0.0132 |
| `CANADA_LONG_TERM_SENSITIVITY` | 54 | 0.6940 | +0.0008 | 0.0489 | 0.2392 | 0.0093 |
| `BASELINE` | 58 | 0.6932 | - | 0.0491 | 0.2443 | 0.0137 |
| `CANADA_DROP_FFMC_10D_MEAN` | 57 | 0.6932 | -0.0000 | 0.0493 | 0.2433 | 0.0119 |
| `CANADA_DROP_BUI` | 57 | 0.6919 | -0.0013 | 0.0493 | 0.2451 | 0.0133 |
| `CANADA_DROP_DC` | 57 | 0.6907 | -0.0025 | 0.0493 | 0.2429 | 0.0129 |
| `CANADA_FWI_ONLY` | 52 | 0.6782 | -0.0150 | 0.0499 | 0.2460 | 0.0149 |
| `CANADA_CORE_EDA` | 54 | 0.6781 | -0.0151 | 0.0503 | 0.2462 | 0.0103 |
| `CANADA_FFMC_FWI` | 53 | 0.6765 | -0.0167 | 0.0502 | 0.2479 | 0.0108 |

전체 캐나다지수를 제거하면 성능이 크게 하락하지만, 모든 7개 지수를 동시에 유지하는 것이 최선은 아니었다. `ISI`, `FWI`, `FFMC`, `DMC`를 하나씩 제거한 모델은 baseline보다 AUPRC가 소폭 높았다. 반대로 `DC` 또는 `BUI` 제거는 성능이 하락했다.

이 패턴은 현재 LightGBM에서 `DC`와 `BUI`가 상대적으로 독자적인 정보를 제공하고, `ISI`, `FWI`, `FFMC` 일부는 이미 날씨와 다른 캐나다지수에 포함된 정보와 중복될 가능성을 보여준다. `FFMC_10일평균` 제거는 거의 차이가 없어 영향이 중립적이었다.

단, 이는 변수의 물리적 중요도나 인과효과를 의미하지 않는다. 다른 변수들을 조건으로 둔 특정 tuned LightGBM 내부에서의 예측 중복성 결과다.

#### 작은 개선의 안정성 검증

상위 축소 모델은 baseline과 동일한 5개 fold에서 비교했고, `group_id` 단위 bootstrap 300회를 추가로 수행했다.

| ablation | fold별 ΔAUPRC | 개선 fold | group bootstrap 95% 구간 | baseline Spearman |
| --- | --- | ---: | --- | ---: |
| `CANADA_DROP_ISI` | +0.0046, -0.0004, -0.0056, +0.0092, +0.0070 | 3 / 5 | -0.0010 ~ +0.0080 | 0.9660 |
| `CANADA_DROP_FWI` | -0.0038, +0.0110, -0.0016, +0.0037, +0.0070 | 3 / 5 | -0.0023 ~ +0.0081 | 0.9645 |
| `CANADA_LONG_TERM_SENSITIVITY` | +0.0012, -0.0015, -0.0057, +0.0081, +0.0068 | 3 / 5 | -0.0051 ~ +0.0077 | 0.9522 |

세 후보 모두 bootstrap 구간이 0을 포함한다. 또한 16개 ablation 중 가장 좋은 결과를 사후 선택한 것이므로 AUPRC +0.003 수준을 확정적 개선으로 해석하면 안 된다.

따라서 Step 3만으로 `CANADA_DROP_ISI`를 새로운 단일 챔피언으로 교체하지 않는다. Step 2 baseline을 기본 LightGBM으로 유지하고, `CANADA_DROP_ISI`는 Step 4에서 같은 앙상블 구성을 한 번 대체 비교하는 민감도 후보로 둔다.

#### threshold 운영점 비교

| 후보 | recall 70% precision | selected rate | FP | recall 90% precision | selected rate | FP |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `CANADA_DROP_ISI` | 0.4698 | 13.59% | 1,228 | 0.2469 | 33.22% | 4,264 |
| `CANADA_DROP_FWI` | 0.4626 | 13.80% | 1,264 | 0.2501 | 32.80% | 4,192 |
| `CANADA_LONG_TERM_SENSITIVITY` | 0.4564 | 13.99% | 1,296 | 0.2487 | 32.98% | 4,224 |
| `BASELINE` | 0.4628 | 13.79% | 1,263 | 0.2389 | 34.33% | 4,453 |
| `DROP_WEATHER` | 0.4321 | 14.77% | 1,430 | 0.2496 | 32.86% | 4,203 |

`CANADA_DROP_ISI`는 recall 70%에서 baseline보다 false positive가 35건 적고, `CANADA_DROP_FWI`는 recall 90%에서 false positive가 261건 적다. 다만 이 threshold는 full OOF 전체에서 선택한 진단값이므로 최종 운영 threshold로 확정하지 않는다.

#### top-risk 포착 변화

| 후보 | top 5% 포착 | top 10% 포착 | top 20% 포착 |
| --- | ---: | ---: | ---: |
| `BASELINE` | 729 | 969 | 1,220 |
| `CANADA_DROP_ISI` | 733 | 980 | 1,227 |
| `CANADA_DROP_FWI` | 729 | 978 | 1,227 |
| `CANADA_LONG_TERM_SENSITIVITY` | 733 | 979 | 1,212 |
| `DROP_WEATHER` | 688 | 934 | 1,210 |
| `DROP_CANADA_ALL` | 717 | 928 | 1,157 |

`CANADA_DROP_ISI`가 top 5%, 10%, 20%에서 baseline보다 각각 4건, 11건, 7건 더 포착해 가장 균형적인 축소 후보였다. 그러나 top-risk 구간별 최고 후보는 top 5%에서 `CANADA_DROP_DMC`, top 10%에서 `CANADA_DROP_FFMC`, top 20%에서 `CANADA_DROP_ISI`와 `CANADA_DROP_FWI`로 서로 달랐다. 즉 한 개의 축소 모델이 모든 구간을 일관되게 압도하지는 않았다.

#### hard-negative와 기후지형유형

| 후보 | Target_0A AUPRC | 고지·산간형 AUPRC | 영동 해안형 AUPRC | 영서 내륙형 AUPRC |
| --- | ---: | ---: | ---: | ---: |
| `BASELINE` | 0.7041 | 0.6417 | 0.7818 | 0.6343 |
| `CANADA_DROP_ISI` | 0.7065 | 0.6490 | 0.7834 | 0.6388 |
| `CANADA_DROP_FWI` | 0.7064 | 0.6459 | 0.7807 | 0.6394 |
| `CANADA_LONG_TERM_SENSITIVITY` | 0.7036 | 0.6323 | 0.7804 | 0.6407 |
| `DROP_CANADA_ALL` | 0.6749 | 0.6223 | 0.7590 | 0.5992 |
| `DROP_WEATHER` | 0.6716 | 0.5839 | 0.7628 | 0.5998 |

`CANADA_DROP_ISI`는 hard-negative와 세 기후지형유형 모두에서 baseline보다 AUPRC가 높았다. `CANADA_DROP_FWI`는 고지·산간형과 영서 내륙형에서는 개선됐지만 영동 해안형에서 0.0011 하락했다. 이 때문에 축소 후보 중에서는 `CANADA_DROP_ISI`를 우선한다.

토지피복 제거는 Target_0A AUPRC에는 0.7041에서 0.7027로 변화가 작았지만 `Target_1 vs Target_0B1`은 0.9332에서 0.8681, `Target_1 vs Target_0B2`는 0.9617에서 0.9212로 크게 하락했다. 즉 토지피복은 hard-negative 구분보다 배경 비발생군과 산불을 구분하는 데 더 크게 기여한다.

#### Step 4 전달 결정

| 후보 | 결정 | Step 4 역할 |
| --- | --- | --- |
| `BASELINE` | 기본 선택 | 기존 tuned LightGBM 대표 |
| `CANADA_DROP_ISI` | 대체 비교 | baseline을 교체한 동일 앙상블 민감도 실험 |
| `CANADA_DROP_FWI` | 예비 | 높은 recall 90% 운영점 보조 |
| `CANADA_LONG_TERM_SENSITIVITY` | 예비 | 54개 피처 단순화와 log loss 개선 후보 |
| `DROP_WEATHER` | 진단 전용 | calibration과 높은 recall 민감도 참고 |
| 핵심 변수군 제거 모델 | 제외 | 성능 하락 확인용 설명 결과 |

baseline과 `CANADA_DROP_ISI`의 Spearman 상관이 0.966이므로 두 모델을 동시에 앙상블 base로 넣지는 않는다. Step 4에서는 먼저 다음 두 구성을 별도로 비교한다.

```text
기본 조합: LightGBM BASELINE + Step1 RF_CORE + tuned XGBoost
민감도 조합: LightGBM CANADA_DROP_ISI + Step1 RF_CORE + tuned XGBoost
```

두 조합의 개선이 비슷하면 Step 2에서 사전에 확정한 baseline을 우선한다. `CANADA_DROP_ISI` 조합이 fold-aware OOF에서 일관되게 더 좋을 때만 축소 LightGBM을 채택한다.

#### Step 3 결론

LightGBM 성능에는 날씨, 캐나다지수, 토지피복, 지형·거리 정보가 모두 필요했다. 이 중 날씨와 캐나다지수 제거의 AUPRC 하락이 가장 컸다. 계절·시간 축의 기여도는 상대적으로 작았다.

캐나다지수 전체는 유용하지만 단기 지수 일부는 날씨 변수 및 다른 지수와 중복된다. `ISI` 제거 모델이 가장 높은 AUPRC와 가장 균형적인 운영 지표를 보였지만, 개선폭은 작고 bootstrap 구간이 0을 포함하므로 확정적 우승으로 보지 않는다.

따라서 추가 Optuna 튜닝이나 다른 모델 전체 ablation은 현재 필요하지 않다. Step 4 앙상블로 넘어가 baseline LightGBM과 `CANADA_DROP_ISI` 대체안을 같은 조건에서 비교하는 것이 다음 순서다.

---

## Step 4 앙상블

Step 4의 앙상블은 Step 2와 Step 3에서 생성한 development OOF 예측값을 사용해 진행한다. 여기서 중요한 원칙은 다음과 같다.

```text
개발 단계 앙상블 비교 = Step 2/3에서 저장한 각 행별 OOF 확률값을 조합
최종 lockbox 적용 = 선택된 base 모델을 development 전체로 다시 학습한 뒤 lockbox 확률을 생성하고 같은 조합식을 적용
```

고정 평균, 기하평균, rank 평균, 최대값 조합은 base 모델을 다시 학습하지 않는다. 이미 Step 2와 Step 3에서 각 표본에 대해 “그 표본을 학습에 사용하지 않은 모델”이 낸 outer OOF 확률값을 저장했기 때문이다. 이 OOF 확률값을 후보별로 가로로 붙인 matrix를 만들고 고정 조합식을 즉시 평가한다.

반면 Optuna 가중치와 stacking은 같은 outer OOF에서 meta parameter를 선택하고 평가할 때 생기는 편향을 막기 위해 nested 방식으로 진행한다. 각 outer fold의 outer-train 내부에서 inner 4-fold base OOF를 새로 만들기 때문에 base 모델을 다시 fitting한다. 다만 Step 2처럼 하이퍼파라미터를 다시 탐색하는 것은 아니며, Step 2에서 확정한 parameter 또는 Step 1 preset으로 총 100회의 inner base fit만 수행한다.

단, 최종 lockbox 평가를 할 때는 lockbox 표본에 대한 base model 확률이 필요하다. 이때는 선택된 base 모델들을 development 전체로 다시 학습해 lockbox 예측확률을 만든 뒤, Step 4에서 확정한 앙상블 조합식을 그대로 적용한다. lockbox 결과를 보고 조합식이나 threshold를 다시 바꾸지 않는다.

### Step 4-1. 입력 데이터

Step 4의 기본 입력은 Step 2에서 만든 다음 파일이다.

```text
oof_matrix__step2_base_candidates.csv
```

이 파일은 후보별 OOF 예측을 한 행에 모은 matrix다.

| 열 | 설명 |
| --- | --- |
| `sample_id` | 표본 식별자 |
| `y_true` | 실제 Target |
| `outer_fold` | OOF fold |
| `sample_type` | subgroup 평가용 표본유형 |
| `climate_type` | 기후지형유형 |
| `pred_{candidate_id}` | 각 base 후보의 OOF 예측확률 |

Step 3에서 ablation 후보가 실제로 좋아지거나 중요한 비교 후보가 되면, Step 3 OOF 예측도 별도 후보 열로 추가할 수 있다. 그러나 설명용 ablation 결과를 모두 앙상블 후보에 넣지는 않는다.

### Step 4-2. base learner 선택

앙상블에는 모든 Step 2 후보를 무조건 넣지 않는다. base learner는 다음 기준으로 3~5개 정도만 선택한다.

- 단일 OOF AUPRC가 상위권일 것
- Target_0A 성능이 지나치게 낮지 않을 것
- fold별 성능 편차가 과도하지 않을 것
- top 10%, 20% capture가 의미 있을 것
- 다른 후보와 예측 상관이 너무 높지 않거나 unique capture가 있을 것
- 같은 알고리즘 계열의 거의 중복 후보는 하나만 남길 것

예상되는 1차 후보군은 다음과 같다.

| 계열 | 후보 예시 | 역할 |
| --- | --- | --- |
| RandomForest | `TUNE_RF_CORE_ALL_LC_BAL` 또는 `TUNE_RF_ALL_ALL_LC_BAL` 중 1개 | 강한 bagging 기준 |
| LightGBM | `TUNE_LGBM_ALL_ALL_LC_NONE` | 확률 품질과 boosting 대표 |
| ExtraTrees | `TUNE_ET_ALL_ALL_NO_LC_BAL` | RandomForest와 다른 bagging 변형 |
| HistGradientBoosting | `TUNE_HGB_ALL_ALL_LC_NONE` | sklearn boosting 기준 |
| XGBoost/CatBoost | rescue 결과가 좋아질 때만 | 보조 후보 |

### Step 4-3. 먼저 비교할 단순 앙상블

가장 먼저 학습 없는 단순 조합을 비교한다. 이 단계는 base 모델 재학습도, Optuna도 필요 없다.

| ensemble_id | 방식 | 설명 |
| --- | --- | --- |
| `mean_top2` | 산술평균 | 상위 2개 후보 확률 평균 |
| `mean_top3` | 산술평균 | 상위 3개 후보 확률 평균 |
| `mean_diverse3` | 산술평균 | 성능과 다양성을 함께 고려한 3개 평균 |
| `geomean_diverse3` | 기하평균 | 후보들이 동시에 높게 본 표본을 강하게 올림 |
| `rank_average_diverse3` | rank 평균 | 확률 크기보다 순위 조합에 집중 |
| `max_diverse3` | 최대값 | 한 모델이라도 강하게 위험으로 본 표본을 올림. recall 보조 진단용 |

이 중 `rank_average`는 ranking score로만 취급한다. rank 평균은 AUPRC와 top-risk capture에는 유용할 수 있지만, 값 자체가 확률로 해석되기 어렵다. 따라서 최종 threshold와 calibration 후보는 원칙적으로 `mean` 또는 `geomean` 계열에서 고른다.

### Step 4-4. Optuna 가중치 앙상블을 쓸 경우

앙상블 가중치를 Optuna로 찾는 것은 가능하다. 다만 같은 OOF matrix 전체에서 가중치를 고르고 같은 OOF matrix 전체로 성능을 평가하면 개발 성능이 과대평가될 수 있다.

따라서 Optuna 가중치 앙상블은 다음 조건에서만 사용한다.

1. 단순 평균 또는 기하평균이 단일 최고 모델보다 개선되는 조짐이 있을 것
2. base 후보가 3~5개 이내일 것
3. fold-aware 방식으로 가중치 선택 편향을 줄일 것

fold-aware Optuna 절차는 다음과 같다.

```text
1. outer_fold 0을 검증 fold로 둔다.
2. 나머지 4개 fold의 OOF matrix에서 Optuna로 ensemble weight를 찾는다.
3. 찾은 weight를 outer_fold 0의 base prediction에 적용해 ensemble score를 만든다.
4. outer_fold 1~4에 대해서도 같은 과정을 반복한다.
5. 5개 fold의 ensemble score를 합쳐 ensemble OOF 성능을 계산한다.
```

최종 구현에서는 위의 단순 fold-aware OOF 재사용 방식보다 더 엄격한 nested 방식을 사용한다. outer-validation을 완전히 남겨 둔 상태에서 outer-train 내부에 inner 4-fold를 만들고 base 모델을 고정 parameter로 다시 fitting해 inner OOF를 생성한다. Optuna는 이 inner OOF에서만 조합 가중치를 선택하고, 선택된 weight를 기존 outer-validation base prediction에 적용한다. 하이퍼파라미터 재탐색은 없으므로 Step 2 tuning보다는 계산량이 훨씬 작다.

가중치 탐색 대상은 다음으로 제한한다.

| ensemble_id | Optuna가 조정하는 값 |
| --- | --- |
| `weighted_mean` | 후보별 비음수 weight, 합계 1 |
| `weighted_geomean` | clipping된 확률의 log 평균 weight |
| `weighted_rank_average` | 후보별 rank weight, ranking 진단용 |

Optuna objective는 기본적으로 AUPRC를 사용하되, 다음 보조 지표를 함께 저장한다.

- `recall_ge_0.70`에서 precision과 selected rate
- Target_0A AUPRC
- fold별 AUPRC 표준편차
- top 10%, 20% capture
- Brier score와 log loss

Step 4에서 Optuna는 base 모델 하이퍼파라미터를 다시 튜닝하는 것이 아니다. 이미 만들어진 base 예측값을 어떤 비율로 섞을지만 정한다.

#### Step 4-4 보충. Optuna 사용 여부와 Step 2 full tuning의 관계

Step 4에서 Optuna 가중치 앙상블은 사용할 수 있지만, 기본 출발점은 아니다. 앙상블은 다음 순서로 진행한다.

1. 먼저 Optuna 없이 단순 평균, 기하평균, rank 평균을 비교한다.
2. 단순 조합이 단일 최고 모델보다 좋아지는 조짐이 있을 때만 Optuna로 조합 가중치를 찾는다.
3. Optuna 자체는 base 모델 하이퍼파라미터를 다시 튜닝하지 않는다.
4. 다만 정직한 nested 평가를 위해 outer-train 내부에서 고정 parameter base 모델을 다시 fitting해 inner OOF를 만든다.
5. Optuna는 이 inner OOF 예측확률의 비율만 조정하고, outer-validation은 weight 선택에 사용하지 않는다.

따라서 최고의 앙상블 조합을 찾기 위해 Step 2의 모든 후보를 full nested tuning으로 돌릴 필요는 없다. 앙상블 성능은 모든 base 모델이 극한까지 튜닝되었는지보다, 각 base 모델이 충분한 성능을 가지면서 서로 다른 오류 패턴과 top-risk 포착 패턴을 가지는지에 더 크게 좌우된다.

Step 2의 역할은 모든 모델을 끝까지 세게 튜닝하는 것이 아니라, 다음 조건을 만족하는 base 후보의 OOF 예측을 확보하는 것이다.

- 단일 성능이 충분히 높을 것
- 서로 다른 알고리즘 계열을 포함할 것
- fold별 성능이 안정적일 것
- 다른 후보와 예측 상관이 너무 높지 않을 것
- unique capture 또는 top-risk 보완성이 있을 것

`XGBoost`와 `CatBoost`는 Step 2 성능이 낮게 나오더라도 무조건 Step 4에서 사용하는 것은 아니다. 낮은 단일 성능을 가진 후보가 앙상블에 들어갈 수 있는 경우는 다음 조건 중 일부를 만족할 때다.

- 다른 상위 모델과 OOF 예측 상관이 낮다.
- 다른 모델이 놓친 Target 1을 일부 포착한다.
- top 10%, top 20% 위험군 overlap이 낮다.
- `recall_ge_0.70` 운영점에서 FN 패턴이 다르다.
- 특정 subgroup 또는 기후지형유형에서 보완적이다.

반대로 AUPRC가 낮고, unique capture도 없고, 다른 모델과 비슷하게 틀리면 Step 4 조합 후보에서 제외한다. “모든 조합을 찾는다”는 말은 모든 약한 모델까지 강제로 넣는다는 뜻이 아니라, 통과 기준을 만족한 3~5개 base 후보 안에서 조합을 비교한다는 뜻이다.

실무적인 운영 규칙은 다음과 같다.

```text
Step 2: 모든 모델 full tuning이 아니라, 대표 base 후보의 OOF 예측 확보
Step 4-1: 단순 평균/기하평균/rank 평균 먼저 비교
Step 4-2: 단순 조합 개선이 보이면 fold-aware Optuna로 weight만 탐색
Step 4-3: XGBoost/CatBoost는 diversity나 unique capture가 있을 때만 포함
```

### Step 4-5. stacking 사용 조건

stacking은 base 예측확률을 입력으로 하는 작은 meta model을 학습하는 방식이다. 예를 들어 logistic regression이 다음 입력을 받아 최종 score를 만든다.

```text
pred_RF, pred_LGBM, pred_ET, pred_HGB
```

stacking은 표현력이 더 크지만 과적합 위험도 크다. 현재 표본 수와 양성 비율을 고려하면, Step 4에서는 stacking을 기본 채택하지 않는다. 다음 조건을 만족할 때만 비교한다.

- 단순 평균/기하평균보다 fold-aware OOF 성능이 안정적으로 좋아질 것
- meta model은 logistic regression 같은 단순 모델로 제한할 것
- base 후보 수를 3~5개 이내로 제한할 것
- lockbox를 보기 전에 stacking 채택 여부를 확정할 것

stacking도 가중치 앙상블과 같은 nested inner OOF를 재사용한다. 따라서 stacking을 위해 base fit을 추가로 더 하지는 않지만, meta model 학습용 inner OOF를 만들기 위한 100회의 고정 parameter base fit에는 포함된다. 최종 lockbox 적용 시에는 development 전체로 재학습한 base 모델들이 만든 lockbox 확률을 meta model에 넣는다.

### Step 4-6. 앙상블 선택 기준

앙상블은 단일 최고 모델보다 반드시 좋아야 채택한다. 단일 모델 성능이 충분하고 앙상블 개선이 작거나 불안정하면 단일 모델을 최종 후보로 유지한다.

앙상블 채택 기준은 다음과 같다.

- 단일 최고 모델보다 OOF AUPRC가 개선될 것
- `recall_ge_0.70` 운영점에서 precision이 개선되거나 selected rate가 줄어들 것
- top 10%, 20% capture가 개선될 것
- Target_0A 성능이 악화되지 않을 것
- fold별 성능이 특정 fold 하나에만 의존하지 않을 것
- Brier score와 log loss가 크게 나빠지지 않을 것
- 조합 방식이 보고서에서 설명 가능할 것

개선폭이 아주 작으면 더 단순한 모델을 우선한다. 예를 들어 앙상블 AUPRC가 단일 모델보다 `0.001`만 높고 threshold 운영점이 비슷하면 단일 모델을 유지한다. 반대로 AUPRC 개선이 작아도 top-risk capture나 recall 70% selected rate가 의미 있게 좋아지면 앙상블을 후보로 남긴다.

### Step 4-7. 저장할 결과

Step 4 결과는 다음 폴더에 저장한다.

```text
jsw/Analysis/new_machine_learning/outputs/step4_ensemble/
```

저장 파일은 다음을 기본으로 한다.

| 파일 | 내용 |
| --- | --- |
| `ensemble_input_oof_matrix.csv` | Step 2/3 base 후보 OOF matrix |
| `base_candidate_diagnostics.csv` | base 후보 단일 성능과 diversity 진단 |
| `ensemble_manifest.csv` | 비교한 앙상블 방식, 사용 후보, weight, score_type |
| `oof__{ensemble_id}.csv` | 앙상블별 development OOF score |
| `summary__step4_ensemble.csv` | 단일 모델과 앙상블 성능 비교 |
| `threshold_metrics__step4_ensemble.csv` | 앙상블 score 기준 threshold 진단 |
| `top_risk_metrics__step4_ensemble.csv` | top-risk capture 비교 |
| `subgroup_metrics__step4_ensemble.csv` | Target_0A와 기후지형유형별 비교 |
| `selected_ensemble_candidate.json` | Step 5로 넘길 최종 후보와 선택 이유 |

Step 4 완료 후 Step 5에서 calibration과 최종 threshold를 진행한다. 최종 운영 threshold는 Step 4에서 선택된 최종 score에 대해 development OOF에서 다시 정한다.

### Step 4-8. 구현 확정 사양

Step 4 실행 스크립트는 다음 파일로 구현한다.

```text
jsw/Analysis/new_machine_learning/step4_ensemble.py
```

기존 Step 4 계획의 base 후보를 Step 2·3 실제 결과에 맞춰 다음처럼 확정한다.

| stream_id | 실제 입력 |
| --- | --- |
| `lgbm_baseline` | Step 2 tuned LightGBM baseline OOF |
| `lgbm_drop_isi` | Step 3 `CANADA_DROP_ISI` OOF |
| `rf_step1` | Step 1 기본 RF_CORE OOF |
| `et_step1` | Step 1 기본 ExtraTrees OOF |
| `xgb_tuned` | Step 2 tuned XGBoost OOF |
| `hgb_tuned` | Step 2 tuned HGB OOF, 고정 조합 진단 전용 |

Step 1 ExtraTrees는 tuned ExtraTrees보다 단일 AUPRC가 높고, `LightGBM + RF + ET` 기하평균의 사전 계산에서도 Step 1 ET 사용 시 AUPRC가 0.6977로 tuned ET 사용 시 0.6960보다 높았다. 따라서 Step 4 ExtraTrees 대표는 Step 1 기본 preset을 사용한다.

가중치와 stacking은 기존 outer OOF 전체에 직접 학습하지 않는다. outer fold별 outer-train 안에서 inner 4-fold base OOF를 새로 생성한 뒤 meta parameter를 선택하고, 남겨둔 outer-validation에서 최종 ensemble OOF를 만든다.

```text
5 inner base streams × 5 outer folds × 4 inner folds = 100 base fits
```

고정 조합은 30개, 단일 기준은 6개, nested weighted 후보는 12개, nested stacking 후보는 8개다. 총 56개 score를 같은 outer OOF 기준으로 비교한다.

사전 회귀검사 기준은 다음과 같다.

| 조합 | 예상 AUPRC |
| --- | ---: |
| baseline LightGBM + Step1 RF 산술평균 | 약 0.6964 |
| `DROP_ISI` LightGBM + Step1 RF + Step1 ET 기하평균 | 약 0.7007 |

실행 전 구성 점검 명령은 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step4_ensemble.py' --check-config
```

smoke test는 inner CV 특성상 inner 1-fold를 사용할 수 없으므로 `outer 1 × inner 2`로 수행한다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step4_ensemble.py' `
  --max-outer-folds 1 `
  --inner-splits 2 `
  --weight-trials 20 `
  --bootstrap-iterations 20 `
  --output-dir 'scratch\step4_smoke_test' `
  --no-progress-bar
```

전체 실행 명령은 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step4_ensemble.py'
```

Step 4에서는 lockbox에 접근하지 않고 calibration도 수행하지 않는다. 선택 결과는 `selected_ensemble_candidate.json`으로 Step 5에 전달한다.

### Step 4-9. 전체 실행 결과와 해석

#### 실행 개요

Step 4 전체 실행은 2026-06-22 18:43:48에 시작해 18:52:34에 끝났으며 총 545.5초, 약 9분 6초가 걸렸다.

| 항목 | 결과 |
| --- | ---: |
| development 표본 수 | 17,045 |
| 양성 수 | 1,553 |
| outer fold | 5 |
| inner fold | 4 |
| nested inner base fit | 100 |
| Optuna trial | outer fold당 300 |
| bootstrap 반복 | 1,000 |
| 평가한 전체 score | 56 |
| lockbox 접근 | 없음 |
| calibration 수행 | 없음 |

56개 score는 단일 기준 6개, 고정 조합 30개, nested weighted 조합 12개, nested stacking 조합 8개로 구성됐다.

#### 데이터 누수와 구조 검증

다음 구조 검사가 모두 통과했다.

| 검사 | 결과 |
| --- | ---: |
| outer 입력 `sample_id` 중복 | 0 |
| outer base prediction 결측 | 0 |
| inner manifest 표본 중복 | 0 |
| inner OOF 표본 중복 | 0 |
| inner base prediction 결측 | 0 |
| outer train-validation group 교집합 | 0 |
| inner train-validation group 교집합 | 0 |
| 선택 weight 합계 위반 | 0 |

고정 회귀검사도 사전 예상값을 정확히 재현했다.

| 조합 | 실제 AUPRC | 판정 |
| --- | ---: | --- |
| baseline LightGBM + Step1 RF 산술평균 | 0.696426 | 통과 |
| `DROP_ISI` LightGBM + Step1 RF + Step1 ET 기하평균 | 0.700652 | 통과 |

따라서 split, OOF 결합, 고정 조합 계산은 계획대로 재현됐다.

#### 최종 자동 선택 후보

자동 선택 결과는 다음 nested stacking이다.

```text
ensemble_id = stack__drop_isi__lgbm_rf_et__logit
base streams = CANADA_DROP_ISI LightGBM + Step1 RandomForest + Step1 ExtraTrees
input transform = clipped logit
meta model = StandardScaler + L2 LogisticRegression(C=1)
```

| 지표 | 선택 stacking | 최고 단일 `DROP_ISI` LGBM | 차이 |
| --- | ---: | ---: | ---: |
| AUPRC | **0.702583** | 0.696573 | **+0.006010** |
| AUROC | **0.919449** | 0.914196 | +0.005254 |
| Brier | **0.045289** | 0.048826 | -0.003537 |
| log loss | **0.164587** | 0.240681 | -0.076094 |
| Target_0A AUPRC | **0.710095** | 0.706457 | +0.003638 |
| top 10% capture | 0.625885 | **0.631037** | -0.005151 |
| top 20% capture | **0.799742** | 0.790084 | +0.009659 |

Brier는 단일 모델보다 약 7.2%, log loss는 약 31.6% 감소했다. 즉 stacking의 가장 뚜렷한 이점은 단순 순위 향상뿐 아니라 확률 score 품질의 개선이다.

paired `group_id` bootstrap 1,000회에서 단일 최고 모델 대비 AUPRC 차이 평균은 `+0.006049`, 95% 구간은 `[+0.002644, +0.009477]`였고 차이가 0보다 클 비율은 1.000이었다. 이 비교에서는 앙상블 개선이 우연한 표본 변동만으로 설명되기 어렵다.

fold별 AUPRC는 `0.7030, 0.7177, 0.7015, 0.6901, 0.7018`이었다. 최고 단일 모델보다 5개 fold 모두 개선됐고, 최악의 fold 차이도 `+0.00145`로 음수가 아니었다. 특정 fold 하나가 전체 개선을 만든 결과가 아니다.

#### 가장 강한 단순 조합과의 비교

가장 강한 고정식은 다음 조합이었다.

```text
fixed__drop_isi__lgbm_rf_et__geomean
= CANADA_DROP_ISI LightGBM, Step1 RF, Step1 ET 확률의 동일가중 기하평균
```

| 지표 | 선택 stacking | 고정 기하평균 | 우세 |
| --- | ---: | ---: | --- |
| AUPRC | **0.702583** | 0.700652 | stacking |
| AUROC | 0.919449 | **0.920039** | 고정식 |
| Brier | **0.045289** | 0.046445 | stacking |
| log loss | **0.164587** | 0.169630 | stacking |
| Target_0A AUPRC | **0.710095** | 0.708230 | stacking |
| top 10% capture | 0.625885 | **0.629749** | 고정식 |
| top 20% capture | **0.799742** | 0.799099 | stacking |
| recall 70% selected rate | 13.570% | **13.365%** | 고정식 |

고정 기하평균도 단일 최고 모델보다 AUPRC가 `+0.004079` 높았다. 단일 모델 대비 bootstrap 95% 구간은 `[+0.000982, +0.006805]`이며 차이가 0보다 클 비율은 0.993이었다.

stacking과 고정식의 AUPRC 차이는 `0.001931`이다. 이는 사전에 정한 “차이가 0.001 미만이면 단순 조합 우선” 기준보다 크므로 자동 규칙은 stacking을 선택했다. 다만 두 후보를 직접 비교한 paired bootstrap 구간은 이번 산출물에 없으므로 stacking이 고정식보다 통계적으로 확실히 우월하다고 해석하지 않는다.

실무적으로는 역할이 조금 다르다.

- stacking은 전체 AUPRC, Target_0A, Brier, log loss, top 20~30% 포착에 더 강하다.
- 고정 기하평균은 구조가 단순하고 AUROC, top 10% 포착, recall 70% selected rate가 조금 더 좋다.
- Step 5의 1차 probability score는 계획대로 stacking을 사용한다.
- 고정 기하평균은 calibration과 threshold 결과가 stacking보다 안정적인지 확인할 단순 challenger로 유지한다.
- 최고 단일 `DROP_ISI` LightGBM도 앙상블의 실제 이득을 확인하는 reference로 유지한다.

#### top-risk 포착 해석

| 후보 | top 5% | top 10% | top 20% | top 30% |
| --- | ---: | ---: | ---: | ---: |
| 최고 단일 `DROP_ISI` LGBM | 733 / 1,553 | **980 / 1,553** | 1,227 / 1,553 | 1,369 / 1,553 |
| 고정 기하평균 | **737 / 1,553** | 978 / 1,553 | 1,241 / 1,553 | 1,391 / 1,553 |
| 선택 stacking | **737 / 1,553** | 972 / 1,553 | **1,242 / 1,553** | **1,397 / 1,553** |

선택 stacking은 최상위 10%만 볼 때는 단일 모델보다 산불 8건을 덜 포착했다. 대신 top 20%에서는 15건, top 30%에서는 28건을 더 포착했다. 따라서 stacking의 개선은 극소수 최상위 위험군을 더 날카롭게 정렬한 결과라기보다, 상위 20~30% 위험 구간에서 놓치는 양성을 줄이고 확률 score를 안정화한 결과로 해석한다.

#### recall 운영점 진단

아래 threshold는 전체 development OOF에서 계산한 진단값이다. Step 5 calibration과 최종 threshold 선택 전이므로 운영 확정값으로 사용하지 않는다.

| 목표 | threshold | recall | precision | 선택률 | TP | FP | FN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| recall 70% | 0.196738 | 0.7006 | 0.4704 | 13.57% | 1,088 | 1,225 | 465 |
| recall 90% | 0.070393 | 0.9002 | 0.2726 | 30.09% | 1,398 | 3,731 | 155 |
| recall 95% | 0.031721 | 0.9504 | 0.1958 | 44.22% | 1,476 | 6,062 | 77 |

산불 미탐지를 강하게 줄이려면 선택 대상이 빠르게 늘어난다. recall 90%에서는 전체의 약 30.1%, recall 95%에서는 약 44.2%를 조사 대상으로 선택해야 한다. Step 5에서는 calibration 후 이 recall-coverage tradeoff를 기준으로 최종 threshold를 정한다.

#### subgroup 해석

| 비교군 | 최고 단일 `DROP_ISI` LGBM | 고정 기하평균 | 선택 stacking |
| --- | ---: | ---: | ---: |
| Target_1 vs Target_0A AUPRC | 0.7065 | 0.7082 | **0.7101** |
| Target_1 vs Target_0B1 AUPRC | 0.9371 | **0.9495** | 0.9485 |
| Target_1 vs Target_0B2 AUPRC | 0.9627 | **0.9714** | 0.9713 |
| 고지·산간형 AUPRC | 0.6490 | 0.6506 | **0.6534** |
| 영동 해안형 AUPRC | 0.7834 | **0.7907** | 0.7901 |
| 영서 내륙형 AUPRC | 0.6388 | 0.6396 | **0.6435** |

선택 stacking은 가장 어려운 hard-negative인 Target_0A와 고지·산간형, 영서 내륙형에서 가장 좋았다. 고정 기하평균은 상대적으로 쉬운 Target_0B1·0B2와 영동 해안형에서 아주 조금 더 높았다. 전체적으로 두 앙상블 모두 단일 LightGBM의 지역·표본유형별 성능을 보완했다.

#### stacking 계수 해석

clipped-logit 입력을 표준화한 뒤 학습한 계수를 보면 LightGBM과 ExtraTrees는 5개 fold 모두 양수였고 크기도 비교적 안정적이었다. RandomForest는 fold 1~3에서는 양수였지만 fold 0과 4에서는 0에 가깝거나 약한 음수였다.

이는 최종 개선의 중심이 `DROP_ISI` LightGBM과 ExtraTrees의 상호 보완에 있고, RandomForest는 fold에 따라 추가적인 보완 신호를 제공한다는 뜻이다. 계수는 `StandardScaler` 이후 값이므로 원래 확률에 대한 직접 가중치나 변수 중요도로 해석하지 않는다.

#### Step 4 최종 결정

1. Step 4의 공식 선택 후보는 `stack__drop_isi__lgbm_rf_et__logit`이다.
2. 선택 후보는 단일 최고 모델보다 AUPRC, 확률 품질, hard-negative 성능이 개선됐고 5개 fold 모두에서 AUPRC가 높았다.
3. `fixed__drop_isi__lgbm_rf_et__geomean`은 단순하고 경쟁력이 높으므로 Step 5의 challenger로 함께 비교한다.
4. `single__lgbm_drop_isi`는 앙상블 이득을 확인할 reference로 유지한다.
5. Step 5에서는 이 probability score들에 대해 calibration을 비교하고, recall 70%, 90%, 95%와 top-risk capture를 함께 보며 최종 운영 threshold를 정한다.
6. 현재 결과는 development nested OOF 결과이며 lockbox 성능이 아니다. lockbox를 보기 전에 calibration 방식과 threshold 선택 규칙을 확정해야 한다.

이번 세션에서 확정한 Step 4 계획은 위 `Step 4-8. 구현 확정 사양`에 반영돼 있다. 기존 초안에 남아 있던 “가중치와 stacking에서는 base 모델을 다시 fitting하지 않는다”는 표현도 실제 구현에 맞게 수정했다. 고정 조합은 기존 outer OOF를 바로 사용하지만, nested 가중치와 stacking은 누수 없는 inner OOF 생성을 위해 100회의 고정 parameter base fit을 수행한다.

---

## Step 5. Calibration 및 최종 threshold 계획

### Step 5 상태

- 작성일: 2026-06-22
- 상태: 실행 전 상세 계획
- 목적: Step 4에서 선택한 raw probability score를 development OOF 기준으로 보정하고, lockbox를 보기 전에 최종 운영 threshold를 고정한다.
- 입력 범위: Step 4 development OOF 예측값만 사용
- lockbox 접근: 금지

Step 5는 모델 구조를 새로 찾는 단계가 아니다. Step 4에서 이미 선택한 score 후보를 대상으로 calibration 방법과 threshold만 결정한다. 따라서 Step 5에서 피처셋, base 모델, ensemble 구성, stacking 구조를 다시 바꾸지 않는다.

현재 Step 1~4 산출물은 17,045행 전체에 대한 development OOF 기준으로 정리돼 있다. Step 5도 동일한 17,045행 development OOF에서만 수행한다. Step 6에서 lockbox 최초 평가를 하려면, 그 전에 별도 lockbox manifest 또는 lockbox 입력 데이터의 출처를 확정해야 한다.

### Step 5-1. Step 4에서 넘겨받는 후보

Step 5에서 비교할 후보는 3개로 제한한다.

| 역할 | candidate_id | 입력 파일 | Step 4 기준 해석 |
| --- | --- | --- | --- |
| 1차 probability 후보 | `stack__drop_isi__lgbm_rf_et__logit` | `outputs/step4_ensemble/oof__stack__drop_isi__lgbm_rf_et__logit.csv` | 공식 선택 후보. AUPRC, Target_0A, Brier, log loss가 가장 좋음 |
| 단순 challenger | `fixed__drop_isi__lgbm_rf_et__geomean` | `outputs/step4_ensemble/oof__fixed__drop_isi__lgbm_rf_et__geomean.csv` | 구조가 단순하고 AUROC, top 10%, recall 70% selected rate가 강함 |
| 단일 reference | `single__lgbm_drop_isi` | `outputs/step4_ensemble/oof__single__lgbm_drop_isi.csv` | 앙상블 이득을 확인하는 기준선 |

Step 4의 공식 선택 후보는 다음과 같다.

```text
selected_ensemble_id = stack__drop_isi__lgbm_rf_et__logit
base streams = lgbm_drop_isi + rf_step1 + et_step1
method = clipped-logit 입력 + StandardScaler + L2 LogisticRegression(C=1)
```

주요 Step 4 OOF 지표는 다음과 같다.

| 후보 | AUPRC | AUROC | Brier | log loss | Target_0A AUPRC | top 10% capture | top 20% capture |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `stack__drop_isi__lgbm_rf_et__logit` | 0.702583 | 0.919449 | 0.045289 | 0.164587 | 0.710095 | 0.625885 | 0.799742 |
| `fixed__drop_isi__lgbm_rf_et__geomean` | 0.700652 | 0.920039 | 0.046445 | 0.169630 | 0.708230 | 0.629749 | 0.799099 |
| `single__lgbm_drop_isi` | 0.696573 | 0.914196 | 0.048826 | 0.240681 | 0.706457 | 0.631037 | 0.790084 |

Step 5에서 후보를 무제한으로 다시 늘리지 않는다. Step 4 전체 56개 score를 다시 탐색하면 calibration 단계가 사실상 ensemble 재선택 단계가 되기 때문이다. 후보 교체는 위 3개 사이에서만 허용한다.

### Step 5-2. Calibration 후보

각 score 후보에 다음 3개 calibration 방법을 적용한다.

| calibration_id | 방법 | 역할 |
| --- | --- | --- |
| `raw` | 보정 없음, identity transform | calibration이 실제로 필요한지 확인하는 기준 |
| `sigmoid` | 1차원 Platt scaling, LogisticRegression 기반 | 매끄럽고 단순한 확률 보정 |
| `isotonic` | IsotonicRegression | 비선형 보정 후보. 과적합 위험은 fold-aware 평가로 확인 |

따라서 비교 단위는 다음과 같다.

```text
3개 score 후보 × 3개 calibration 방법 = 9개 calibrated score 후보
```

calibration에서는 `class_weight="balanced"`를 사용하지 않는다. calibration의 목적은 현재 development 표본 분포에서 score와 실제 Target 비율의 관계를 보정하는 것이므로, class weight를 다시 주면 확률 해석이 왜곡될 수 있다.

확률형 score는 `epsilon = 1e-6`으로 clipping한 뒤 logit 변환이 필요한 곳에만 사용한다. 최종 저장 score는 항상 `[0, 1]` 범위여야 한다.

주의할 점은 여기서의 calibration이 실제 운영 모집단의 산불 발생확률을 뜻하지 않는다는 것이다. 현재 데이터는 Target 0을 설계적으로 추출한 case-control 형태이므로 Step 5의 결과는 “현재 개발 표본 구성에서 보정된 산불위험 점수”로 해석한다. 최종 보고에서도 실제 전신주 전체 모집단의 절대 발생확률이라고 표현하지 않는다.

### Step 5-3. Fold-aware cross-calibration 절차

calibrator를 development OOF 전체에 fit하고 같은 OOF 전체에서 평가하면 calibration 성능이 낙관적으로 보일 수 있다. 따라서 Step 5의 calibration 비교는 outer fold를 보존한 cross-calibration으로 수행한다.

각 `candidate_id × calibration_id` 조합에 대해 다음 절차를 적용한다.

```text
for outer_fold k in 0..4:
    calibration_train = development OOF 중 outer_fold != k
    calibration_valid = development OOF 중 outer_fold == k

    1. calibration_train의 raw_score와 y_true로 calibrator를 fit한다.
    2. fit된 calibrator를 calibration_valid의 raw_score에 적용한다.
    3. calibration_valid의 calibrated_score를 저장한다.

5개 fold의 calibrated_score를 합쳐 cross-calibrated development OOF를 만든다.
```

`raw` 방법은 calibrator를 fit하지 않고 raw score를 그대로 사용하지만, 같은 출력 형식으로 저장한다.

이 방식의 의미는 다음과 같다.

- 각 표본의 calibrated score는 해당 표본의 정답을 보지 않은 calibrator에서 나온다.
- calibration 방법 비교는 lockbox 없이 development OOF 안에서 최대한 정직하게 수행된다.
- 최종 선택 후에는 선택된 calibration 방법을 development OOF 전체에 다시 fit해 Step 6 lockbox 적용용 final calibrator artifact로 저장한다.

### Step 5-4. Threshold 선택과 일반화 감사

Step 5에서 산출할 운영점은 다음 6개다.

| operating_point | 역할 |
| --- | --- |
| `fixed_0.50` | 보정 후 일반 0.5 기준의 진단값 |
| `best_f1` | 기존 분류 기준 참고값 |
| `best_f2` | recall에 더 큰 가중치를 둔 균형점 |
| `recall_ge_0.70` | 기본 운영 후보 |
| `recall_ge_0.90` | 산불 누락을 더 줄이는 고회수 후보 |
| `recall_ge_0.95` | 경보 부담 스트레스 테스트 |

`recall_ge_0.70`, `recall_ge_0.90`, `recall_ge_0.95` threshold는 다음 규칙으로 정한다.

1. 해당 recall 목표 이상을 만족하는 threshold 후보를 찾는다.
2. 그중 precision이 가장 높은 threshold를 선택한다.
3. precision이 같으면 selected_rate가 더 낮은 threshold를 선택한다.
4. 그래도 같으면 더 높은 threshold를 선택한다.

최종 threshold를 고정하기 전에 threshold 선택 편향을 확인하기 위해 fold-aware threshold audit도 수행한다.

```text
for outer_fold k in 0..4:
    train_folds = outer_fold != k
    valid_fold = outer_fold == k

    1. train_folds에서 calibration fit
    2. train_folds의 calibrated score로 recall 목표 threshold 선택
    3. 같은 threshold를 valid_fold에 적용
    4. valid_fold의 실제 recall, precision, FN, selected_rate 저장
```

최종 운영 threshold는 선택된 `candidate_id × calibration_id`의 development OOF 전체에서 한 번 산출한다. Step 6 lockbox에서는 이 threshold를 그대로 적용하고, lockbox 성능을 본 뒤 threshold를 다시 조정하지 않는다.

### Step 5-5. 평가 지표

Calibration 후보 비교는 ranking 성능, 확률 품질, threshold 운영성, subgroup 안정성을 분리해서 본다.

#### Ranking 성능

- AUPRC
- AUROC
- Target_1 vs Target_0A AUPRC
- Target_1 vs Target_0B1 AUPRC
- Target_1 vs Target_0B2 AUPRC
- fold별 AUPRC 평균과 표준편차
- paired group bootstrap 95% CI

sigmoid는 단조 변환이므로 일반적으로 ranking을 바꾸지 않는다. isotonic은 동점 score를 만들 수 있으므로 AUPRC와 top-risk를 반드시 다시 계산한다.

#### 확률 품질

- Brier score
- log loss
- calibration intercept
- calibration slope
- ECE, Expected Calibration Error
- MCE, Maximum Calibration Error
- 10-bin calibration table
- quantile-bin calibration table

Brier와 log loss가 개선되더라도 AUPRC, Target_0A, top-risk capture가 의미 있게 나빠지면 최종 운영 후보로 채택하지 않는다.

#### Threshold 운영성

각 운영점에서 다음 값을 저장한다.

- threshold
- TP, FP, FN, TN
- recall
- precision
- specificity
- false positive rate
- F1
- F2
- balanced accuracy
- MCC
- selected_n
- selected_rate
- FP_per_TP

보고서에서는 특히 `recall_ge_0.70`과 `recall_ge_0.90`을 중심으로 다음 세 값을 반드시 같이 제시한다.

1. 실제 산불을 몇 건 중 몇 건 포착했는가?
2. 그 포착률을 위해 전체의 몇 %를 경보 대상으로 지정했는가?
3. 경보 대상 중 실제 산불 비율은 얼마인가?

#### Top-risk capture

threshold와 별도로 고정 점검 예산 관점의 top-risk 성능도 산출한다.

- top 5%
- top 10%
- top 20%
- top 30%

각 구간에서 선택 표본 수, 산불 포착 수, capture rate, precision, lift를 저장한다.

#### Subgroup 평가

다음 subgroup에서 AUPRC와 주요 threshold 지표를 산출한다.

- `Target_1 vs Target_0A`
- `Target_1 vs Target_0B1`
- `Target_1 vs Target_0B2`
- 기후지형유형
- outer fold
- 가능하면 연도와 12시 이전/이후

특히 Target_0A는 가장 어려운 hard-negative이므로, calibration 후 Target_0A AUPRC가 악화되는지 별도로 확인한다.

### Step 5-6. 최종 score 선택 규칙

Step 5에서 최종 score를 고르는 순서는 다음과 같다.

#### 필수 통과 조건

- 입력 후보의 sample 수가 모두 17,045행일 것
- `sample_id` 중복 0건
- 후보 간 `sample_id`, `outer_fold`, `y_true` 정렬 불일치 0건
- raw score NaN/inf 0건
- calibrated score NaN/inf 0건
- calibrated score가 `[0, 1]` 범위를 벗어나지 않을 것
- 모든 outer fold에 Target 1이 존재할 것
- cross-calibration에서 train fold와 validation fold가 분리될 것
- Step 5 실행 중 lockbox 입력을 읽지 않을 것

#### calibration 방법 선택

같은 score 후보 안에서는 다음 규칙을 적용한다.

1. `raw`보다 Brier와 log loss가 모두 개선되는 calibration을 우선한다.
2. Brier와 log loss 중 하나만 개선되면 ECE, calibration slope, threshold 운영성을 함께 본다.
3. AUPRC가 raw 대비 `0.001` 이상 하락하면 제외한다.
4. Target_0A AUPRC가 raw 대비 `0.002` 이상 하락하면 제외한다.
5. top 20% capture가 raw 대비 `0.5%p` 이상 하락하면 제외한다.
6. `recall_ge_0.70`의 selected_rate가 raw 대비 `0.5%p` 이상 증가하면서 precision 개선이 없으면 제외한다.
7. sigmoid와 isotonic 성능이 비슷하면 sigmoid를 우선한다.
8. 어떤 calibration도 raw보다 실질적으로 낫지 않으면 raw score를 유지한다.

#### 최종 후보 선택

Step 4 공식 선택 후보인 `stack__drop_isi__lgbm_rf_et__logit`을 기본 후보로 둔다. 단순 challenger인 `fixed__drop_isi__lgbm_rf_et__geomean`으로 교체하려면 다음 중 하나를 만족해야 한다.

- AUPRC가 stacking과 `0.002` 이내이고, `recall_ge_0.70` selected_rate가 `0.3%p` 이상 낮거나 precision이 `0.5%p` 이상 높을 것
- AUPRC가 stacking과 `0.002` 이내이고, Brier 또는 log loss가 명확히 더 좋을 것
- top 10% capture가 더 좋고, top 20% capture와 Target_0A AUPRC가 stacking보다 실질적으로 나쁘지 않을 것

`single__lgbm_drop_isi`는 reference로 유지한다. 두 앙상블 후보가 validation check를 통과하지 못하는 경우를 제외하면 단일 모델을 최종 score로 되돌리지 않는다.

### Step 5-7. 저장할 산출물

Step 5 결과는 다음 폴더에 저장한다.

```text
jsw/Analysis/new_machine_learning/outputs/step5_calibration_threshold/
```

저장 파일은 다음을 기본으로 한다.

| 파일 | 내용 |
| --- | --- |
| `candidate_input_manifest.csv` | Step 5 입력 후보와 원본 OOF 파일 목록 |
| `calibration_manifest.csv` | calibration 방법, fold-aware fit 설정, seed |
| `oof__{candidate_id}__{calibration_id}.csv` | cross-calibrated development OOF score |
| `summary__step5_calibration.csv` | 후보별 ranking, Brier, log loss, calibration 지표 요약 |
| `calibration_bins__step5.csv` | calibration curve용 bin별 예측평균과 실제 양성률 |
| `threshold_metrics__step5.csv` | 최종 OOF 기준 운영점별 threshold 지표 |
| `threshold_audit_by_outer_fold__step5.csv` | fold-aware threshold 일반화 감사 결과 |
| `top_risk_metrics__step5.csv` | top 5%, 10%, 20%, 30% 포착 성능 |
| `subgroup_metrics__step5.csv` | Target_0A, 기후지형유형 등 subgroup 성능 |
| `bootstrap__step5_calibration.csv` | 최종 후보와 challenger 간 paired group bootstrap |
| `validation_checks__step5.csv` | 누락, 중복, NaN, fold 분리, lockbox 접근 여부 점검 |
| `selected_probability_score.json` | 최종 score 후보와 calibration 방법 |
| `final_thresholds.json` | Step 6에 그대로 적용할 최종 threshold |
| `final_candidate_manifest.json` | Step 6 lockbox 평가에 넘길 후보, score, threshold, artifact 경로 |
| `run_manifest__step5.json` | 실행 환경, seed, 입력 파일 수정시각, 라이브러리 버전 |

가능하면 다음 plot도 함께 저장한다.

| 파일 | 내용 |
| --- | --- |
| `calibration_curve__{candidate_id}__{calibration_id}.png` | calibration curve |
| `pr_curve__selected_score.png` | 최종 score PR curve |
| `threshold_tradeoff__selected_score.png` | threshold별 recall, precision, selected_rate |
| `top_risk_capture__selected_score.png` | top-risk capture curve |

모든 CSV는 `utf-8-sig`로 저장한다.

### Step 5-8. 구현 및 실행 계획

Step 5 실행 스크립트는 다음 파일로 새로 만든다.

```text
jsw/Analysis/new_machine_learning/step5_calibration_threshold.py
```

스크립트의 주요 옵션은 다음처럼 둔다.

| 옵션 | 기본값 | 목적 |
| --- | --- | --- |
| `--input-dir` | `outputs/step4_ensemble` | Step 4 OOF 입력 폴더 |
| `--output-dir` | `outputs/step5_calibration_threshold` | Step 5 산출물 폴더 |
| `--candidate-ids` | 3개 고정 후보 | 입력 후보 제한 |
| `--calibration-methods` | `raw,sigmoid,isotonic` | calibration 후보 |
| `--bootstrap-iterations` | `1000` | paired group bootstrap |
| `--random-seed` | `20260622` | 재현성 |
| `--no-progress-bar` | 선택 | 진행 막대 비활성화 |
| `--check-config` | 선택 | 입력 파일과 후보 구성만 점검 |

구성 점검 명령은 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step5_calibration_threshold.py' --check-config
```

smoke test는 전체 후보 중 1개와 calibration 2개만 사용해 파일 생성과 검증 로직을 먼저 확인한다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step5_calibration_threshold.py' `
  --candidate-ids 'stack__drop_isi__lgbm_rf_et__logit' `
  --calibration-methods 'raw,sigmoid' `
  --bootstrap-iterations 20 `
  --output-dir 'scratch\step5_smoke_test' `
  --no-progress-bar
```

전체 실행 명령은 다음과 같다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step5_calibration_threshold.py'
```

Step 5 실행이 끝나면 이 로그에 다음 내용을 추가한다.

1. 실행 스크립트와 실행 시각
2. 입력 후보 3개와 calibration 후보 3개 확인
3. validation check 결과
4. calibration별 Brier, log loss, ECE, calibration slope/intercept
5. 후보별 AUPRC, Target_0A AUPRC, top-risk 결과
6. fold-aware threshold audit 결과
7. 최종 선택된 `candidate_id × calibration_id`
8. 최종 `recall_ge_0.70`, `recall_ge_0.90`, `recall_ge_0.95` threshold
9. 최종 운영점별 TP, FP, FN, TN, recall, precision, selected_rate
10. Step 6 lockbox 평가에 넘길 `final_candidate_manifest.json` 경로
11. 제한사항: 현재 calibration은 development 표본 분포 기준이며 실제 운영 발생확률이 아님

### Step 5-9. 실행 결과와 해석

#### 실행 개요

Step 5 전체 실행은 2026-06-22 19:26:39에 시작해 19:27:32에 끝났으며 총 52.6초가 걸렸다.

```powershell
& 'C:\Program Files\Python313\python.exe' 'jsw\Analysis\new_machine_learning\step5_calibration_threshold.py'
```

| 항목 | 결과 |
| --- | ---: |
| 입력 후보 score | 3개 |
| calibration 방법 | 3개 |
| 비교한 후보 조합 | 9개 |
| development OOF 표본 수 | 17,045 |
| Target 1 | 1,553 |
| bootstrap 반복 | 1,000 |
| lockbox 접근 | 없음 |

결과 폴더는 다음이다.

```text
jsw/Analysis/new_machine_learning/outputs/step5_calibration_threshold/
```

주요 산출물은 다음 파일에 저장됐다.

| 파일 | 내용 |
| --- | --- |
| `summary__step5_calibration.csv` | calibration 후보별 전체 성능 요약 |
| `threshold_metrics__step5.csv` | cross-calibrated OOF 기준 threshold 지표 |
| `threshold_audit_by_outer_fold__step5.csv` | fold-aware threshold 일반화 감사 |
| `top_risk_metrics__step5.csv` | top-risk capture |
| `subgroup_metrics__step5.csv` | Target_0A와 기후지형유형별 성능 |
| `selected_probability_score.json` | 최종 score와 calibration 선택 결과 |
| `final_thresholds.json` | Step 6에 고정 적용할 최종 threshold |
| `final_candidate_manifest.json` | Step 6 lockbox 평가 인계 manifest |
| `calibrator__selected_probability_score.pkl` | 최종 sigmoid calibrator artifact |

#### validation 결과

Step 5 입력과 cross-calibrated OOF 산출물 검증은 모두 통과했다.

| 검사 | 결과 |
| --- | ---: |
| 입력 OOF 후보 | 3개 |
| cross-calibrated OOF 후보 | 9개 |
| 후보별 예측 행 수 | 17,045 |
| `sample_id` 중복 | 0 |
| score NaN | 0 |
| score inf | 0 |
| score < 0 | 0 |
| score > 1 | 0 |
| Target 1 없는 fold | 0 |
| lockbox 접근 | 없음 |

따라서 Step 5 결과는 Step 4 development OOF 범위 안에서만 산출됐고, Step 6 lockbox 평가 전까지 모델·calibration·threshold가 고정된 상태다.

#### 최종 선택 결과

최종 선택은 다음 조합이다.

```text
candidate_id = stack__drop_isi__lgbm_rf_et__logit
calibration_id = sigmoid
```

선택 이유는 `default_step4_primary_stack`으로 기록됐다. 즉 Step 4에서 공식 선택된 stacking 후보를 유지하되, raw score 대신 sigmoid calibration을 적용하는 결정이다.

cross-calibrated OOF 기준 주요 지표는 다음과 같다.

| 지표 | 값 |
| --- | ---: |
| AUPRC | 0.702260 |
| AUROC | 0.919165 |
| Brier | 0.045069 |
| log loss | 0.163792 |
| Target_0A AUPRC | 0.709821 |
| ECE equal-width | 0.005562 |
| calibration intercept | -0.003371 |
| calibration slope | 0.998298 |
| recall 70% precision | 0.471274 |
| recall 70% selected rate | 13.58% |
| top 10% capture | 0.625241 |
| top 20% capture | 0.799099 |

sigmoid calibration의 핵심 효과는 raw score의 순위 성능을 거의 유지하면서 확률 품질을 개선한 것이다.

| 비교 | raw stacking | sigmoid stacking | 해석 |
| --- | ---: | ---: | --- |
| AUPRC | 0.702583 | 0.702260 | -0.000324, 사실상 동일한 순위 성능 |
| Target_0A AUPRC | 0.710095 | 0.709821 | -0.000274, hard-negative 성능 유지 |
| Brier | 0.045289 | 0.045069 | 개선 |
| log loss | 0.164587 | 0.163792 | 개선 |
| ECE equal-width | 0.012058 | 0.005562 | calibration 오차 감소 |
| calibration slope | 1.088437 | 0.998298 | 1에 매우 가까워짐 |

따라서 Step 5에서는 raw stacking을 그대로 쓰기보다 sigmoid 보정 score를 최종 probability score로 채택한다. 다만 이 확률은 현재 development 표본 구성에서 보정된 산불위험 점수이며, 실제 운영 모집단의 절대 산불 발생확률로 해석하지 않는다.

#### 후보별 calibration 비교

주요 후보 비교는 다음과 같다.

| 후보 | calibration | 선택 | AUPRC | Brier | log loss | ECE | recall70 precision | recall70 selected rate | top20 capture |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| stacking | sigmoid | 최종 선택 | 0.702260 | 0.045069 | 0.163792 | 0.005562 | 0.471274 | 13.58% | 0.799099 |
| stacking | raw | 보류 | 0.702583 | 0.045289 | 0.164587 | 0.012058 | 0.470385 | 13.57% | 0.799742 |
| stacking | isotonic | 제외 | 0.692765 | 0.045093 | 0.165906 | 0.001982 | 0.450988 | 14.24% | 0.797811 |
| fixed geomean | sigmoid | challenger 내 선택 | 0.699992 | 0.045492 | 0.165135 | 0.008132 | 0.474281 | 13.46% | 0.796523 |
| fixed geomean | raw | 보류 | 0.700650 | 0.046445 | 0.169477 | 0.019538 | 0.477861 | 13.38% | 0.799099 |
| single LGBM | raw | reference 내 선택 | 0.696569 | 0.048826 | 0.240671 | 0.039230 | 0.469775 | 13.59% | 0.790084 |

isotonic은 ECE만 보면 가장 작지만 AUPRC와 Target_0A AUPRC가 크게 떨어지고, recall 70%에서 selected rate가 늘면서 precision도 낮아졌다. 따라서 최종 후보에서 제외한다. 현재 표본 수가 적지는 않지만 양성 비율이 낮고 score 구간이 불균형하므로, isotonic의 계단형 보정이 ranking과 threshold 운영성을 손상한 것으로 본다.

fixed geomean은 여전히 강한 challenger다. 특히 recall 70% 운영점에서는 stacking sigmoid보다 precision이 높고 selected rate가 낮다. 그러나 cross-calibrated AUPRC가 stacking sigmoid보다 `0.002268` 낮고, bootstrap에서도 geomean sigmoid는 stacking sigmoid보다 낮았다. 따라서 자동 선택 규칙상 최종 score는 stacking sigmoid를 유지한다.

#### bootstrap 비교

최종 선택 후보인 `stack__drop_isi__lgbm_rf_et__logit / sigmoid`를 기준으로 paired group bootstrap 1,000회를 수행했다.

| 비교 후보 | ΔAUPRC 평균 | 95% CI | Δ > 0 비율 | 해석 |
| --- | ---: | --- | ---: | --- |
| stacking raw | +0.000327 | [+0.000083, +0.000550] | 0.999 | raw가 ranking만 보면 아주 조금 높음 |
| fixed geomean raw | -0.001603 | [-0.003449, +0.000150] | 0.044 | geomean raw는 근접하지만 우위는 아님 |
| fixed geomean sigmoid | -0.002257 | [-0.004235, -0.000253] | 0.011 | geomean sigmoid는 최종 후보보다 낮음 |
| single LGBM raw | -0.005664 | [-0.009013, -0.002369] | 0.001 | 단일 모델은 앙상블보다 낮음 |
| stacking isotonic | -0.009451 | [-0.012010, -0.007419] | 0.000 | isotonic은 ranking 손실이 큼 |

이 결과는 raw stacking이 순위 지표만 보면 sigmoid보다 미세하게 높다는 점도 보여준다. 그러나 차이는 `+0.000327`로 작고, sigmoid는 Brier, log loss, ECE와 calibration slope를 개선했다. Step 5의 목적이 단순 순위 재선택이 아니라 최종 threshold 적용용 score 확정이므로 sigmoid 선택이 타당하다.

#### 최종 threshold

Step 6에 넘길 threshold는 `final_thresholds.json`에 저장됐다. 이 threshold는 선택된 sigmoid calibrator를 development OOF 전체에 다시 fit한 뒤 산출한 `full_oof_final_calibrator` 기준이다.

| operating point | threshold | recall | precision | selected rate | TP | FP | FN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| fixed 0.50 | 0.500000 | 0.4739 | 0.8649 | 4.99% | 736 | 115 | 817 |
| best F1 | 0.345529 | 0.5525 | 0.7409 | 6.79% | 858 | 300 | 695 |
| best F2 | 0.141548 | 0.7560 | 0.4191 | 16.43% | 1,174 | 1,627 | 379 |
| recall 70% | 0.174110 | 0.7006 | 0.4704 | 13.57% | 1,088 | 1,225 | 465 |
| recall 90% | 0.055533 | 0.9002 | 0.2726 | 30.09% | 1,398 | 3,731 | 155 |
| recall 95% | 0.023089 | 0.9504 | 0.1958 | 44.22% | 1,476 | 6,062 | 77 |

기본 운영점으로 계획한 `recall_ge_0.70`에서는 전체 17,045개 중 2,313개, 즉 13.57%를 고위험으로 지정한다. 이때 Target 1은 1,553건 중 1,088건을 포착하고 465건을 놓친다. 경보 대상 중 실제 산불 비율은 47.04%다.

`recall_ge_0.90`으로 올리면 놓치는 산불은 155건으로 줄지만, 고위험 지정 대상은 5,129개, 전체의 30.09%까지 늘어난다. 이때 precision은 27.26%다.

`recall_ge_0.95`는 산불 누락을 77건으로 줄이지만 전체의 44.22%를 고위험으로 지정해야 한다. 이 운영점은 기본 운영안이라기보다 경보 부담을 보여주는 stress test로 보는 것이 맞다.

#### fold-aware threshold audit

최종 threshold를 정하기 전, 각 outer fold를 validation으로 남기고 나머지 fold에서 threshold를 정한 뒤 validation fold에 적용하는 audit을 수행했다. 이 값은 full OOF threshold보다 일반화 관점에서 더 보수적인 진단이다.

| operating point | recall 평균 | recall 범위 | precision 평균 | selected rate 평균 | fold FN 합계 |
| --- | ---: | --- | ---: | ---: | ---: |
| recall 70% | 0.7012 | 0.6752 ~ 0.7460 | 0.4694 | 13.62% | 464 |
| recall 90% | 0.8982 | 0.8714 ~ 0.9518 | 0.2734 | 30.01% | 158 |
| recall 95% | 0.9491 | 0.9325 ~ 0.9711 | 0.1973 | 43.95% | 79 |

fold-aware audit에서는 `recall_ge_0.70`도 fold별로 항상 70% 이상을 달성하지는 않는다. 최저 fold는 67.52%다. 이는 threshold가 development 전체에서는 recall 목표를 맞추지만, 새로운 평가 fold나 lockbox에서는 실제 recall이 목표값과 달라질 수 있다는 뜻이다.

따라서 Step 6 lockbox에서 recall이 정확히 70% 또는 90%가 아니더라도 threshold를 다시 조정하지 않는다. 개발 OOF에서 정한 threshold를 그대로 적용하고, lockbox에서 실제 달성 recall을 보고해야 한다.

#### top-risk capture

cross-calibrated OOF 기준 최종 선택 score의 top-risk 결과는 다음과 같다.

| top-risk 구간 | 선택 수 | 포착 산불 수 | capture rate | precision | lift |
| --- | ---: | ---: | ---: | ---: | ---: |
| top 5% | 853 | 736 / 1,553 | 0.4739 | 0.8628 | 9.47 |
| top 10% | 1,705 | 971 / 1,553 | 0.6252 | 0.5695 | 6.25 |
| top 20% | 3,409 | 1,241 / 1,553 | 0.7991 | 0.3640 | 4.00 |
| top 30% | 5,114 | 1,396 / 1,553 | 0.8989 | 0.2730 | 3.00 |

참고로 최종 sigmoid calibrator를 development OOF 전체에 fit한 score는 raw stacking과 단조 관계이므로 ranking은 Step 4 raw stacking과 동일하다. 이 기준의 top-risk 포착은 top 5% 737건, top 10% 972건, top 20% 1,242건, top 30% 1,397건이다. cross-calibrated OOF의 fold별 보정에서는 fold 간 score scale이 조금 달라져 top-risk 포착 수가 각각 1건 정도 낮게 나타났다.

보고서에서는 threshold 운영점과 top-risk 운영점을 분리해서 제시한다. threshold는 recall 목표 기반 운영이고, top-risk는 점검 예산이 고정됐을 때의 우선순위 운영이다.

#### subgroup 결과

최종 선택 score의 주요 subgroup 성능은 다음과 같다.

| subgroup | n | Target 1 | AUPRC | AUROC | 해석 |
| --- | ---: | ---: | ---: | ---: | --- |
| Target_1 vs Target_0A | 9,291 | 1,553 | 0.7098 | 0.8624 | 가장 중요한 hard-negative 성능 유지 |
| Target_1 vs Target_0B1 | 6,205 | 1,553 | 0.9480 | 0.9728 | 시간·공간 배경 대조군 구분은 매우 높음 |
| Target_1 vs Target_0B2 | 4,655 | 1,553 | 0.9709 | 0.9804 | 상대적으로 쉬운 대조군에서 매우 높음 |
| 고지·산간형 | 2,435 | 222 | 0.6526 | 0.9034 | 세 기후지형유형 중 중간 |
| 영동 해안형 | 6,157 | 561 | 0.7899 | 0.9425 | 가장 높은 지역 성능 |
| 영서 내륙형 | 8,453 | 770 | 0.6430 | 0.9044 | 가장 낮지만 Step 4 수준 유지 |

outer fold별 AUPRC는 `0.7030, 0.7177, 0.7015, 0.6901, 0.7018`로 나타났다. 최저 fold는 fold 3의 0.6901이고 최고 fold는 fold 1의 0.7177이다. Step 4에서 보인 fold 안정성은 calibration 후에도 유지됐다.

#### Step 5 최종 결정

1. Step 5의 최종 probability score는 `stack__drop_isi__lgbm_rf_et__logit / sigmoid`로 확정한다.
2. raw stacking은 AUPRC가 아주 조금 높지만 calibration 품질이 sigmoid보다 낮다.
3. sigmoid는 Brier, log loss, ECE를 개선하고 calibration slope를 1에 가깝게 만들었다.
4. isotonic은 ECE는 작지만 AUPRC, Target_0A AUPRC, threshold 운영성을 손상하므로 제외한다.
5. fixed geomean은 단순하고 recall 70% precision/selected rate가 좋지만 AUPRC와 bootstrap 비교에서 최종 후보를 대체할 정도는 아니다.
6. 최종 threshold는 `final_thresholds.json`에 저장된 값을 Step 6 lockbox에 그대로 적용한다.
7. Step 6에서 lockbox 결과가 기대보다 낮더라도 lockbox를 보고 calibration 방법이나 threshold를 다시 고치지 않는다.
8. 현재 모든 수치는 development OOF 기준이며 lockbox 최종 성능이 아니다.

Step 6으로 넘길 핵심 파일은 다음이다.

```text
jsw/Analysis/new_machine_learning/outputs/step5_calibration_threshold/final_candidate_manifest.json
jsw/Analysis/new_machine_learning/outputs/step5_calibration_threshold/selected_probability_score.json
jsw/Analysis/new_machine_learning/outputs/step5_calibration_threshold/final_thresholds.json
jsw/Analysis/new_machine_learning/outputs/step5_calibration_threshold/calibrator__selected_probability_score.pkl
```

---

## Step 6. Strict lockbox 평가 생략 결정

### Step 6 상태

- 작성일: 2026-06-22
- 상태: 이번 프로젝트 보고서 범위에서는 생략
- 사유: 현재 `new_machine_learning` 실행은 17,045행 전체를 Step 1~5 development OOF로 사용했으므로, 별도 미사용 lockbox가 존재하지 않는다.

Lockbox source 감사 결과는 별도 파일에 보관한다.

```text
jsw/Analysis/new_machine_learning/outputs/step6_lockbox/lockbox_source_audit.md
```

이 감사에서 기존 logistic lockbox manifest와 기존 machine_learning lockbox 결과는 확인했지만, 현재 workflow에 사후 적용하지 않는다. 기존 lockbox 3,413행도 이번 Step 1~5의 OOF 모델 선택·calibration·threshold 결정에 이미 포함됐기 때문이다.

따라서 현재 결과를 다시 돌리지 않고 진행한다. 보고서에서는 Step 6을 독립 단계로 설명하지 않고, 평가 기준이 5-fold OOF임을 짧게 명시한다.

보고서용 최소 문장은 다음과 같다.

> 모든 모델 비교와 최종 threshold는 5-fold out-of-fold 예측을 기준으로 산출했으며, 별도 미사용 lockbox 검증은 이번 범위에서는 수행하지 않았다.

보고서에서는 다음 표현을 피한다.

- “최종 test 성능”
- “lockbox 검증 완료”
- “독립 검증셋에서 확인”
- “일반화 성능이 최종 입증됨”

대신 다음 표현을 사용한다.

- “development OOF 기준”
- “5-fold OOF 평가”
- “최종 개발 성능”
- “미사용 외부 검증은 향후 과제”

이 결정에 따라 현재 머신러닝 결과의 마지막 모델링 단계는 Step 5 calibration 및 threshold 확정으로 본다. 다음 단계는 Step 7 결과 해석이다.

---

## Step 7. 결과 해석

### Step 7 상태

- 작성일: 2026-06-22
- 상태: 결과 해석 작성
- 해석 기준: Step 5에서 확정한 최종 score와 threshold
- 평가 범위: 17,045행 5-fold development OOF

### Step 7-1. 최종 모델의 의미

최종 모델은 산불 발생 여부를 단일 threshold로 확정 판정하는 분류기라기보다, 각 표본에 산불위험 점수를 부여하고 고위험 순위를 만드는 선별 모델이다.

최종 score는 다음 조합이다.

```text
stack__drop_isi__lgbm_rf_et__logit / sigmoid
```

구성은 다음과 같다.

| 구성 요소 | 역할 |
| --- | --- |
| `lgbm_drop_isi` | Step 3에서 선택한 LightGBM 민감도 후보 |
| `rf_step1` | Step 1 RandomForest bagging 기준 |
| `et_step1` | Step 1 ExtraTrees 다양성 보완 |
| stacking | 세 base score를 clipped-logit 변환 후 L2 logistic regression으로 결합 |
| sigmoid calibration | 최종 threshold 적용용 score 보정 |

이 모델의 목적은 “산불을 정확히 맞히는 것”보다 “산불 가능성이 높은 표본을 상위권에 배치하고, recall 목표에 따라 점검 대상을 정하는 것”이다.

### Step 7-2. 전체 성능 해석

최종 score의 5-fold OOF 기준 주요 성능은 다음과 같다.

| 지표 | 값 | 해석 |
| --- | ---: | --- |
| AUPRC | 0.7026 | 양성 비율 9.11%인 불균형 자료에서 산불 표본을 상위 위험도로 정렬하는 성능이 높음 |
| AUROC | 0.9194 | 전체 양성·음성 순위 분리도도 높음 |
| Brier | 0.0450 | sigmoid calibration 후 확률형 score 품질 개선 |
| log loss | 0.1636 | 과도하게 확신한 오답이 줄어든 상태 |
| Target_0A AUPRC | 0.7098 | 가장 어려운 hard-negative에서도 성능 유지 |

Step 4 raw stacking은 AUPRC가 가장 높았고, Step 5 sigmoid calibration은 그 순위 성능을 거의 유지하면서 Brier, log loss, ECE, calibration slope를 개선했다. 따라서 보고서에서는 “최종 score는 stacking ensemble이며, threshold 적용을 위해 sigmoid calibration을 적용했다”고 쓰는 것이 맞다.

단, 현재 자료는 case-control 구조이므로 calibrated score를 실제 운영 모집단의 절대 산불 발생확률로 표현하지 않는다. 보고서에서는 “보정된 산불위험 점수” 또는 “위험도 score”라고 표현한다.

### Step 7-3. 기본 threshold 운영점

기본 운영점은 `recall_ge_0.70`이다.

| 항목 | 값 |
| --- | ---: |
| threshold | 0.174110 |
| TP | 1,088 |
| FP | 1,225 |
| FN | 465 |
| TN | 14,267 |
| recall | 70.06% |
| precision | 47.04% |
| selected_n | 2,313 |
| selected_rate | 13.57% |

해석은 다음과 같다.

> 전체 17,045개 표본 중 2,313개, 즉 13.57%를 고위험으로 지정하면 실제 산불 1,553건 중 1,088건을 포착한다. 이때 놓치는 산불은 465건이며, 고위험으로 지정된 표본 중 실제 산불 비율은 47.04%다.

이 운영점은 “산불을 어느 정도 놓치지 않으면서도 조사 대상을 전체의 약 14%로 제한하는 균형점”으로 해석한다.

### Step 7-4. 고회수 운영점

산불 누락을 더 줄이고 싶으면 `recall_ge_0.90`을 사용할 수 있다.

| 항목 | recall 70% 운영점 | recall 90% 운영점 | recall 95% 운영점 |
| --- | ---: | ---: | ---: |
| threshold | 0.174110 | 0.055533 | 0.023089 |
| recall | 70.06% | 90.02% | 95.04% |
| precision | 47.04% | 27.26% | 19.58% |
| selected_rate | 13.57% | 30.09% | 44.22% |
| TP | 1,088 | 1,398 | 1,476 |
| FN | 465 | 155 | 77 |

recall 90% 운영점은 산불 누락을 465건에서 155건으로 줄인다. 대신 고위험 지정 대상이 2,313개에서 5,129개로 증가하고 precision은 27.26%로 낮아진다.

따라서 recall 90%는 “현장 점검 부담이 크게 늘어도 산불 누락을 더 줄여야 하는 상황”에 적합하다. recall 95%는 전체의 44.22%를 고위험으로 지정해야 하므로 기본 운영안이 아니라 경보 부담을 보여주는 stress test로 본다.

### Step 7-5. Top-risk 운영 해석

점검 가능한 수량이 threshold보다 먼저 정해지는 상황에서는 top-risk 기준이 더 직접적이다.

| 구간 | 선택 수 | 산불 포착 | capture rate | precision | lift |
| --- | ---: | ---: | ---: | ---: | ---: |
| top 5% | 853 | 737 / 1,553 | 47.46% | 86.40% | 9.48 |
| top 10% | 1,705 | 972 / 1,553 | 62.59% | 57.01% | 6.26 |
| top 20% | 3,409 | 1,242 / 1,553 | 79.97% | 36.43% | 4.00 |
| top 30% | 5,114 | 1,397 / 1,553 | 89.95% | 27.32% | 3.00 |

이 결과는 최종 모델이 상위 위험군에 산불 표본을 강하게 집중시킨다는 뜻이다. 특히 top 10%만 보더라도 전체 산불의 62.59%를 포착하고, top 20%에서는 79.97%를 포착한다.

보고서에서는 threshold 운영점과 top-risk 운영점을 분리해서 설명한다.

- threshold 운영점: “recall 목표를 만족하려면 score 기준을 어디에 둘 것인가”
- top-risk 운영점: “점검 예산이 정해졌을 때 상위 몇 %를 볼 것인가”

### Step 7-6. Subgroup 해석

최종 score는 가장 어려운 대조군인 Target_0A에서도 성능을 유지했다.

| subgroup | AUPRC | AUROC | 해석 |
| --- | ---: | ---: | --- |
| Target_1 vs Target_0A | 0.7098 | 0.8624 | 같은 사건·유사 조건의 hard-negative 구분에서도 유효 |
| Target_1 vs Target_0B1 | 0.9480 | 0.9728 | 시간·공간 배경 대조군 구분은 매우 높음 |
| Target_1 vs Target_0B2 | 0.9709 | 0.9804 | 상대적으로 쉬운 대조군에서 매우 높음 |
| 고지·산간형 | 0.6526 | 0.9034 | 지역 성능 중간 |
| 영동 해안형 | 0.7899 | 0.9425 | 가장 높은 지역 성능 |
| 영서 내륙형 | 0.6430 | 0.9044 | 가장 낮지만 전체적으로 유효 |

0B1·0B2 성능이 높은 것은 상대적으로 쉬운 배경 대조군을 잘 구분한다는 뜻이다. 더 중요한 결과는 Target_0A AUPRC가 0.7098로 유지됐다는 점이다. 이는 모델이 단순히 쉬운 비발생 표본만 구분한 것이 아니라, 실제 산불과 유사한 hard-negative에서도 위험 순위를 어느 정도 구분한다는 근거다.

기후지형유형별로는 영동 해안형 성능이 가장 높고, 영서 내륙형과 고지·산간형은 상대적으로 낮다. 최종 보고서에서는 전체 성능만 제시하기보다 지역별 성능 차이가 존재한다고 짧게 언급한다.

### Step 7-7. 보고서용 핵심 문장

2.5페이지 프로젝트 보고서에서는 다음 문장을 중심으로 압축한다.

> 최종 모델은 `DROP_ISI LightGBM + RandomForest + ExtraTrees` stacking ensemble에 sigmoid calibration을 적용한 산불위험 score다. 5-fold OOF 기준 AUPRC는 0.7026, AUROC는 0.9194였으며, 기본 운영점인 recall 70% threshold에서는 전체 표본의 13.57%를 고위험으로 지정해 실제 산불의 70.06%를 포착했다. 더 보수적인 recall 90% 운영점에서는 산불 포착률이 90.02%로 증가하지만 고위험 지정 비율도 30.09%로 늘어난다. 또한 top 10% 위험군만으로 전체 산불의 62.59%, top 20% 위험군으로 79.97%를 포착해 현장 점검 우선순위화에도 활용 가능하다.

제한사항은 한 문장만 붙인다.

> 모든 성능은 5-fold OOF 기준의 개발 성능이며, 별도 미사용 외부 검증은 향후 과제로 남는다.

### Step 7-8. 최종 해석 결론

현재 모델은 “산불 발생확률을 절대값으로 예측하는 모델”이라기보다 “산불위험 우선순위를 만들고 recall 목표에 맞춰 점검 대상을 정하는 선별 모델”로 해석해야 한다.

가장 실용적인 기본 운영안은 `recall_ge_0.70`이다. 이 운영점은 전체의 13.57%를 고위험으로 지정하면서 산불의 70.06%를 포착한다. 산불 누락을 더 줄여야 하는 상황에서는 `recall_ge_0.90`을 사용할 수 있지만, 이 경우 고위험 지정 비율이 30.09%까지 늘어난다.

따라서 최종 보고서는 한 개의 정확도 수치보다 다음 세 값을 중심으로 써야 한다.

1. 실제 산불을 몇 % 포착했는가?
2. 그 포착률을 위해 전체의 몇 %를 고위험으로 지정했는가?
3. 고위험 지정 대상 중 실제 산불 비율은 얼마인가?

이 세 값이 산불 누락 최소화와 현장 점검 부담 사이의 핵심 trade-off를 설명한다.

---

## Step 8. 최종 보고서용 진행 흐름 정리

### Step 8 상태

- 작성일: 2026-06-22
- 상태: 보고서용 흐름 정리
- 목적: Step 1부터 Step 5, 그리고 Step 6 생략 결정까지의 과정을 2.5페이지 내외 프로젝트 보고서에 맞게 압축
- 보고서 기준 성능: 17,045행 5-fold development OOF

Step 8은 새 모델을 학습하는 단계가 아니다. 지금까지 수행한 모델링 흐름을 최종 보고서에 넣을 수 있는 구조로 정리하는 단계다.

보고서에서는 세부 실험을 모두 나열하지 않고, 다음 질문에 답하는 순서로 쓴다.

1. 어떤 데이터와 검증 방식으로 모델을 만들었는가?
2. 어떤 과정을 거쳐 최종 모델을 골랐는가?
3. 최종 모델은 산불을 얼마나 포착하고, 그 대가로 얼마나 많은 표본을 고위험으로 지정하는가?
4. 이 결과를 어떤 한계 안에서 해석해야 하는가?

### Step 8-1. 전체 진행 요약

이번 머신러닝은 기존 결과를 그대로 재사용하지 않고, 시간 누수와 요인점수를 제거한 새 최종 데이터에서 다시 수행했다.

| 단계 | 역할 | 핵심 결과 | 보고서 반영 방식 |
| --- | --- | --- | --- |
| Step 1 | 넓은 단일 모델 screening | 6개 모델 × 16개 피처셋 × 2개 불균형 설정 = 192개 조합 비교 | “여러 단일 모델을 동일 5-fold OOF에서 비교”로 요약 |
| Step 2 | 상위 후보 튜닝 | tuned LightGBM AUPRC 0.6932 | “LightGBM이 가장 강한 단일 boosting 후보”로 요약 |
| Step 3 | feature ablation | `CANADA_DROP_ISI` AUPRC 0.6966 | “ISI 제거 민감도 후보가 단일 후보 중 가장 높음”으로 요약 |
| Step 4 | ensemble | `DROP_ISI LGBM + RF + ET` stacking AUPRC 0.7026 | 최종 모델 구조 설명에 포함 |
| Step 5 | calibration 및 threshold | sigmoid calibration 채택, recall 70% threshold 확정 | 운영 threshold와 top-risk 결과의 핵심 근거 |
| Step 6 | strict lockbox 검토 | 별도 미사용 lockbox 없음 | 본문 독립 절 제외, 제한사항 한 문장만 사용 |

보고서의 핵심 흐름은 다음 한 문장으로 압축할 수 있다.

> 단일 모델 screening과 튜닝, feature ablation을 거쳐 `DROP_ISI LightGBM`을 중심 후보로 삼았고, RandomForest와 ExtraTrees를 결합한 stacking ensemble에 sigmoid calibration을 적용해 최종 산불위험 score와 recall 기반 threshold를 확정했다.

### Step 8-2. Step 1에서 보고할 내용

Step 1은 전체 탐색의 기준선을 만든 단계다.

실험 구조는 다음과 같다.

```text
6개 모델 × 16개 피처셋 × 2개 불균형 설정 = 192개 조합
```

모든 조합은 동일한 5-fold OOF 구조에서 평가했고, 누수·누락·중복 검사는 모두 통과했다.

Step 1 최고 조합은 다음이다.

| 항목 | 값 |
| --- | --- |
| 모델 | RandomForest |
| feature_set | `WS_CORE__CANADA_ALL__LC_USED` |
| imbalance | balanced |
| AUPRC | 0.6675 |
| AUROC | 0.9112 |
| Brier | 0.0522 |

보고서에서는 Step 1을 길게 설명하지 않는다. 다음 정도면 충분하다.

> 먼저 RandomForest, ExtraTrees, LightGBM, XGBoost, CatBoost, HistGradientBoosting을 16개 사전 정의 피처셋과 불균형 처리 옵션에서 비교했다. 192개 조합은 모두 누수·중복 없이 산출됐고, 기본 screening에서는 RandomForest와 LightGBM 계열이 상위권을 형성했다.

### Step 8-3. Step 2에서 보고할 내용

Step 2는 Step 1 상위 후보를 대상으로 튜닝을 수행한 단계다.

최고 단일 tuned 후보는 다음이다.

| 항목 | 값 |
| --- | --- |
| candidate | `TUNE_LGBM_ALL_ALL_LC_NONE` |
| 모델 | LightGBM |
| feature_set | `WS_ALL__CANADA_ALL__LC_USED` |
| imbalance | none |
| AUPRC | 0.6932 |
| AUROC | 0.9121 |
| Brier | 0.0491 |

Step 1의 RandomForest보다 tuned LightGBM의 AUPRC가 높아졌고, Brier도 더 좋았다. 따라서 Step 2 이후의 중심 후보는 LightGBM이 됐다.

보고서용 문장은 다음처럼 쓴다.

> 상위 후보를 튜닝한 결과 LightGBM이 AUPRC 0.6932로 가장 강한 단일 모델이었고, 이후 ablation과 ensemble의 중심 후보로 사용했다.

### Step 8-4. Step 3에서 보고할 내용

Step 3은 feature ablation으로 최종 단일 후보의 변수 민감도를 확인한 단계다.

가장 중요한 결과는 `CANADA_DROP_ISI`다.

| 항목 | 값 |
| --- | ---: |
| 후보 | `CANADA_DROP_ISI` |
| AUPRC | 0.6966 |
| AUROC | 0.9142 |
| Brier | 0.0488 |
| log loss | 0.2407 |

`CANADA_DROP_ISI`는 baseline LightGBM보다 AUPRC가 약 0.0034 높았다. 다만 개선이 모든 기준에서 압도적으로 확정된 것은 아니므로, Step 3에서는 이를 “최종 단일 확정 모델”이라기보다 “Step 4 앙상블의 중심 대체 후보”로 넘겼다.

보고서용 문장은 다음처럼 쓴다.

> Feature ablation에서는 캐나다지수 중 ISI를 제거한 LightGBM 후보가 AUPRC 0.6966으로 가장 높아, 최종 ensemble의 중심 base model로 사용했다.

### Step 8-5. Step 4에서 보고할 내용

Step 4는 단일 모델보다 앙상블이 실질적으로 개선되는지 확인한 단계다.

최종 선택 후보는 다음이다.

```text
stack__drop_isi__lgbm_rf_et__logit
= DROP_ISI LightGBM + RandomForest + ExtraTrees stacking
```

주요 성능은 다음과 같다.

| 지표 | stacking | 최고 단일 `DROP_ISI` LGBM | 차이 |
| --- | ---: | ---: | ---: |
| AUPRC | 0.7026 | 0.6966 | +0.0060 |
| AUROC | 0.9194 | 0.9142 | +0.0053 |
| Brier | 0.0453 | 0.0488 | 개선 |
| Target_0A AUPRC | 0.7101 | 0.7065 | +0.0036 |

paired group bootstrap에서도 단일 최고 모델 대비 AUPRC 개선 평균은 약 +0.0060이었고, 95% 구간이 양수였다. 따라서 Step 4에서는 단일 LightGBM보다 stacking ensemble을 최종 score 후보로 채택했다.

다만 단순 기하평균 ensemble도 강했다.

| 후보 | AUPRC | 특징 |
| --- | ---: | --- |
| stacking | 0.7026 | 전체 AUPRC, Brier, log loss, Target_0A 우세 |
| fixed geomean | 0.7007 | 단순하고 recall 70% selected rate가 약간 좋음 |

보고서에서는 기하평균 challenger까지 길게 다루지 않고, 최종 선택 이유만 짧게 쓴다.

> 최종적으로 `DROP_ISI LightGBM`, RandomForest, ExtraTrees를 결합한 stacking ensemble이 최고 단일 모델보다 AUPRC를 0.0060 개선해 최종 score 후보로 선택됐다.

### Step 8-6. Step 5에서 보고할 내용

Step 5는 최종 score를 threshold 운영에 사용할 수 있도록 calibration하고, recall 기반 threshold를 확정한 단계다.

최종 선택은 다음이다.

```text
score = stack__drop_isi__lgbm_rf_et__logit
calibration = sigmoid
```

sigmoid calibration은 raw stacking의 순위 성능을 거의 유지하면서 calibration 품질을 개선했다.

| 지표 | raw stacking | sigmoid stacking | 해석 |
| --- | ---: | ---: | --- |
| AUPRC | 0.7026 | 0.7023 | 순위 성능 거의 유지 |
| Brier | 0.0453 | 0.0451 | 개선 |
| log loss | 0.1646 | 0.1638 | 개선 |
| ECE | 0.0121 | 0.0056 | 개선 |
| calibration slope | 1.0884 | 0.9983 | 1에 가까워짐 |

최종 운영점은 다음과 같다.

| 운영점 | threshold | recall | precision | selected_rate | TP | FP | FN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| recall 70% | 0.174110 | 70.06% | 47.04% | 13.57% | 1,088 | 1,225 | 465 |
| recall 90% | 0.055533 | 90.02% | 27.26% | 30.09% | 1,398 | 3,731 | 155 |
| recall 95% | 0.023089 | 95.04% | 19.58% | 44.22% | 1,476 | 6,062 | 77 |

보고서에서는 recall 70%를 기본 운영점으로 제시하고, recall 90%는 더 보수적인 운영안으로 제시한다.

보고서용 문장은 다음처럼 쓴다.

> 최종 score에는 sigmoid calibration을 적용했다. 기본 운영점인 recall 70% threshold에서는 전체 표본의 13.57%를 고위험으로 지정해 실제 산불의 70.06%를 포착했고, 더 보수적인 recall 90% 운영점에서는 고위험 지정 비율이 30.09%로 증가하는 대신 산불 포착률이 90.02%로 높아졌다.

### Step 8-7. Step 6은 보고서에서 어떻게 처리할지

Step 6 strict lockbox 평가는 이번 보고서에서 독립 결과로 다루지 않는다.

이유는 단순하다.

1. 현재 Step 1~5가 17,045행 전체를 development OOF로 사용했다.
2. 별도 미사용 lockbox가 현재 workflow에 없다.
3. 기존 logistic lockbox는 현재 결과에 사후 적용하면 독립 test가 아니다.

따라서 보고서에서는 Step 6이라는 이름을 굳이 쓰지 않는다. 대신 제한사항에 다음 한 문장만 넣는다.

> 모든 성능은 5-fold OOF 기준의 개발 성능이며, 별도 미사용 외부 검증은 향후 과제로 남는다.

이 정도면 2.5페이지 프로젝트 보고서에서는 충분하다. Step 6을 길게 설명하면 본문 흐름이 “모델 결과”보다 “검증 설계 예외 설명”에 치우치므로 피한다.

### Step 8-8. 보고서 본문 추천 구조

2.5페이지 분량에서는 다음 구조가 가장 적절하다.

#### 1. 데이터와 평가 방식

포함할 내용:

- 최종 학습데이터 17,045행
- Target 1 1,553행, 양성률 9.11%
- 5-fold OOF 기준 평가
- AUPRC를 주지표로 사용
- 목표는 이진 확정 판정이 아니라 산불위험 선별과 우선순위화

권장 문장:

> 최종 데이터는 17,045개 표본과 1,553개 산불 발생 표본으로 구성됐으며, 모든 모델 비교는 5-fold OOF 기준으로 수행했다. 산불 발생 표본이 적은 불균형 자료이므로 AUPRC를 주지표로 사용했고, 최종 목적은 이진 확정 판정보다 고위험 표본 선별과 점검 우선순위화에 두었다.

#### 2. 모델 개발 흐름

포함할 내용:

- Step 1: 192개 단일 조합 screening
- Step 2: LightGBM 튜닝
- Step 3: `CANADA_DROP_ISI` ablation
- Step 4: stacking ensemble 선택
- Step 5: sigmoid calibration

권장 문장:

> 단일 모델 192개 조합을 비교한 뒤, tuned LightGBM과 `CANADA_DROP_ISI` ablation 후보를 중심으로 ensemble을 구성했다. 최종적으로 `DROP_ISI LightGBM + RandomForest + ExtraTrees` stacking이 최고 단일 모델보다 AUPRC를 개선해 최종 score로 선택됐고, threshold 적용을 위해 sigmoid calibration을 적용했다.

#### 3. 최종 성능과 운영점

포함할 내용:

- AUPRC 0.7026
- AUROC 0.9194
- recall 70% 운영점
- recall 90% 운영점
- top 10%, top 20% capture

권장 문장:

> 최종 score는 5-fold OOF 기준 AUPRC 0.7026, AUROC 0.9194를 보였다. 기본 운영점인 recall 70% threshold에서는 전체 표본의 13.57%를 고위험으로 지정해 실제 산불의 70.06%를 포착했고, recall 90% 운영점에서는 산불 포착률이 90.02%로 높아지는 대신 고위험 지정 비율이 30.09%로 증가했다. 또한 top 10% 위험군만으로 전체 산불의 62.59%, top 20% 위험군으로 79.97%를 포착했다.

#### 4. 해석과 한계

포함할 내용:

- 산불위험 score로 해석
- 절대 발생확률로 과해석 금지
- 5-fold OOF 기준 개발 성능
- 외부/미사용 검증은 향후 과제

권장 문장:

> 이 모델은 절대적인 산불 발생확률을 산출하는 모델이라기보다 산불위험 score를 통해 점검 우선순위를 정하는 선별 모델로 해석해야 한다. 모든 성능은 5-fold OOF 기준의 개발 성능이며, 별도 미사용 외부 검증은 향후 과제로 남는다.

### Step 8-9. 최종 보고서에 넣을 핵심 표

2.5페이지 보고서에서는 표를 많이 넣기 어렵다. 다음 한 개의 표를 우선한다.

| 운영 방식 | 선택 비율 | 산불 포착률 | precision | 해석 |
| --- | ---: | ---: | ---: | --- |
| recall 70% threshold | 13.57% | 70.06% | 47.04% | 기본 운영점 |
| recall 90% threshold | 30.09% | 90.02% | 27.26% | 더 보수적인 운영 |
| top 10% risk | 10.00% | 62.59% | 57.01% | 점검 수량 고정 시 |
| top 20% risk | 20.00% | 79.97% | 36.43% | 점검 수량 확대 시 |

이 표 하나로 모델의 실용적 의미를 가장 잘 설명할 수 있다.

### Step 8-10. 최종 보고서용 결론 문단 초안

다음 문단은 최종 보고서에 거의 그대로 사용할 수 있다.

> 본 머신러닝 모델은 산불 발생 여부를 단순히 이진 분류하기보다, 산불위험 score를 산출해 고위험 표본을 선별하는 것을 목표로 했다. 17,045개 표본을 대상으로 5-fold OOF 평가를 수행했으며, 단일 모델 screening, LightGBM 튜닝, feature ablation, stacking ensemble, sigmoid calibration을 거쳐 `DROP_ISI LightGBM + RandomForest + ExtraTrees` stacking score를 최종 후보로 선택했다. 최종 score는 AUPRC 0.7026, AUROC 0.9194를 보였고, 기본 운영점인 recall 70% threshold에서는 전체 표본의 13.57%를 고위험으로 지정해 실제 산불의 70.06%를 포착했다. 더 보수적인 recall 90% 운영점에서는 포착률이 90.02%로 높아졌지만 고위험 지정 비율도 30.09%로 증가했다. 따라서 본 모델은 산불 누락 최소화와 현장 점검 부담 사이의 trade-off를 조정할 수 있는 위험 선별 도구로 해석하는 것이 적절하다. 모든 성능은 5-fold OOF 기준의 개발 성능이며, 별도 미사용 외부 검증은 향후 과제로 남는다.
